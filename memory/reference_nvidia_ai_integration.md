---
name: reference_nvidia_ai_integration
description: "NVIDIA stack in OTTO-Q. 🚨2026-07-23 OEM re-audit: PROVABLE-TODAY = deterministic_v1 does 99.75% of 84,945 enacted decisions, nemotron 30 (advisory), cuOpt=0 enacted (old '870' purged). Deterministic engine is the workhorse; NVIDIA is advisory overlay. NEVER quote cuOpt-drives-870 or 'central intelligence'. Read the 2026-07-23 block first."
metadata:
  node_type: memory
  type: reference
  originSessionId: b5cc1e94-2201-451a-a46f-c770df70e99a
  modified: 2026-07-25T01:50:46.426Z
---

**🚨 2026-07-23 OEM-DILIGENCE RE-AUDIT (adversarial, live-DB verified — SUPERSEDES every "870 enacted" claim below). THE PROVABLE-TODAY TRUTH an OEM's "show me the query" returns:** `ottoq_decisions` = 84,945 enacted, of which **deterministic_v1 = 84,734 (99.75%)**, **nemotron = 30 (0.035%)**, greedy_constrained 78, reservation_honoured/reassigned 99, **cuopt = 0 (ZERO l2_engine='cuopt', ZERO enacted_action.source='cuopt')**. The old "870 enacted cuOpt" was real at its moment but those runs were PURGED (bounded-run doctrine) → the current ledger attributes 0 to cuOpt.
- **HONEST FRAMING (use this with Chase, OEMs, investors — do NOT overstate):** the **deterministic constrained engine is the workhorse and does essentially all enacted decisions**. cuOpt is an **external PROPOSER that currently enacts nothing** (proposals mostly expire on TTL or are pre-empted seconds earlier by the fast deterministic ranker). Nemotron is an **advisory dial-tuner/auditor** (30 decisions, latest ticks applied nothing / hit HTTP 503 fallback). NEVER say "cuOpt drives ~870 decisions" or "central orchestration intelligence" — the data contradicts both. The genuine, defensible AI story: cuopt_fallback is transparently labeled distinct from cuopt (honest telemetry); Nemotron is a real current NVIDIA model with an auditable applied/queued/rejected trail. The EDGE to pitch is the deterministic orchestration + safety + appointment loop, with NVIDIA as an advisory overlay that can be promoted to enacting once its share is proven under contention.
- **TO MAKE THE AI CLAIM TRUE (not just honest):** cuOpt needs sync/batch enactment (kill the 90s-TTL expiry that zeroes its share) + a persisted run where it demonstrably enacts, then re-quote a real cuopt-enacted count. Until then, quote 0 or "advisory."
- **⭐ 2026-07-24 ENACTMENT PROBE — ARCHITECTURAL ROOT CAUSE FOUND (paced proving run 3d6e4037, 25 ticks with 9s solve windows, proposals landing fresh):** even with perfect pacing, cuOpt enacted 0. Trace: its proposals were PRE-EMPTED by `reservation_honoured` — **the appointment doctrine (AP-3/4/5) moved the decision to BOOKING TIME.** Vehicles arrive with stalls already reserved; tick-time assignment (cuOpt's slot) only sees leftovers/walk-ins ≈ 0. **CONCLUSION: cuOpt's production slot must move to (a) booking-time stall selection inside ottoq_book_appointment, and/or (b) batch re-optimization of the RESERVATION BOOK against the ORCH-3 inbound forecast (re-shuffle future reservations before arrivals).** That's the real "AI drives decisions" build — tick-time pacing tweaks are a dead end. **→ STEP 1 SHIPPED + LIVE-VERIFIED (2026-07-24, `reservation_book_reoptimizer`): the reservation-book RE-OPTIMIZER runs every decide tick (otto_q policy): sweeps live reservations for staleness (en-route vehicle, SoC<45, non-DCFC booking, healthy DCFC now free) → claims new stall FIRST then releases old (never unreserved) → rebuilds timed plan via ottoq_build_workflow_plan → downlinks plan_update='upgraded' → decision receipt with honest source. Solver order: fresh cuOpt proposal for that vehicle wins attribution, else 'reservation_reopt' local floor. VERIFIED run f5c4b3ed: 23 ticks → 9 upgrades (soc 40-44, staging→DCFC), 9/9 downlinks, all local-attributed (cuOpt's instance set doesn't yet cover en-route RESERVED vehicles). NEXT INCREMENT for real cuopt attribution: extend ottoq-cuopt-propose's instance set to en-route reserved vehicles (edge fn change) — then the claim-first hook starts crediting cuopt.**
- **⭐ 2026-07-24 cuOpt COHORT EXTENSION SHIPPED + FIRST REAL ENACTMENT (run a044dab4, 48 ticks): ottoq-cuopt-propose now also proposes for the en-route reserved cohort (soc<45, non-DCFC reservation → free healthy DCFC). Result: cuOpt produced 35 proposals for the run and — for the FIRST TIME on the honest lifecycle — 1 proposal ENACTED with l2_engine='cuopt' on a stall_assignment (the historic 0→>0). So "NVIDIA cuOpt makes an enacted, attributed decision in OTTO-Q" is now TRUE + ledger-proven.** HONEST NUANCE (do not overstate): the 1 cuopt enactment landed via the gate-time stall_assignment path (honour/decide), NOT via the reservation-book reopt swap — those 11 reopt swaps all went to the local 'reservation_reopt' floor (cuopt's fresh proposal for that exact vehicle wasn't pending at the swap instant → timing, not wiring). And 34/35 cuopt proposals expired/superseded — enactment RATE is still low (async TTL + the fast deterministic ranker pre-empts). QUOTABLE: "cuOpt enacts real attributed decisions (proven)"; NOT "cuOpt drives the reservation book" yet. To lift the rate: sync/batch cuopt at the booking + reopt instants (not fire-and-forget), OR widen the reopt claim-window to accept a cuopt proposal that lands within N seconds. **→ 2026-07-24 BOTH DONE + a THIRD, decisive fix found by validation:** (1) `reopt_widen_cuopt_claim_window` — reopt now accepts a RECENT cuopt proposal (status pending|superseded, created ≤3min, target DCFC still free) not just pending-at-instant. (2) **ROOT TRIGGER GAP (why validation run 8c77a99c produced 10 reopt swaps but 0 cuopt proposals): `ottoq_cuopt_refresh` only fired the NVIDIA call when `arrived_at_gate` vehicles existed — it NEVER fired for the en-route cohort, so the extended edge fn was never invoked for them.** FIXED `cuopt_refresh_trigger_enroute_cohort`: refresh now also fires when a low-SoC en-route vehicle holds a slow booking AND a free healthy DCFC exists (exactly when cuOpt has a useful upgrade). This is the completing piece — edge fn proposes for en-route + trigger now fires for en-route + reopt claims it. LESSON: three-layer async pipelines need EACH layer verified (propose-eligibility, trigger-condition, claim-window); a run with the cohort present but 0 proposals = upstream trigger, not the consumer. Verify discipline note: the agent's run auto-completed at sim_clock_end (metronome), not a clean stop — I reset it (stop_and_reset, depot empty). Standing reminder: agent runs still obey bounded-run doctrine; reset on closeout. ALSO FIXED (`proposal_lifecycle_honest_attribution`): decide_tick marked proposals 'enacted' if their entity got ANY enacted decision that tick regardless of engine (the audit's inflated-count mechanism, reproduced live: 4 'enacted'/0 real) — now 'enacted' requires enacted_action.source = proposal.source, else 'superseded'. Historical proposal 'enacted' counts (incl. the 108/7d) are UNRELIABLE pre-fix — trust only post-2026-07-24 statuses.

**⚠️ 2026-07-13 LIVE-AUDIT (prior; its "870 enacted" is now STALE per the 2026-07-23 block above). Canonical: `~/Desktop/OTTO-Q V1/OTTOQ_NVIDIA_INTEGRATION_VERDICT.md`.** Framing as of 2026-07-13:
- **cuOpt IS real + DOES drive decisions** — genuine NVIDIA managed-service call, REAL multi-objective cost matrix (the degenerate all-ones was a DIFFERENT fn, `cuopt-optimize`, in the twin repo, not the live `ottoq-cuopt-propose`), and **870 enacted stall assignments** all-time (via `enacted_action->>'source'='cuopt'`). BUT: (a) **`l2_engine` was NEVER stamped** → a `GROUP BY l2_engine` audit showed 0 cuopt (looked like theater). FIXED 2026-07-13 (`nvidia_01`: BEFORE-INSERT trigger derives l2_engine from enacted source + backfill) → now shows **cuopt=870**; a fresh proof run produced 4 NEW cuopt-labeled decisions (forward-correct). (b) **ZERO on `production_live`** — production is `ottoq_world_advance` (physics-only) + ADVISORY `orchestrate-tick` (recommendations, human-confirmed, nothing auto-actuates); the ENACTING decider (decide_tick+cuOpt) runs ONLY on sim/A-B/test runs. So "cuOpt runs live production autonomously" is FALSE (by-design human-in-loop); "cuOpt powers the autonomous SIM" is true. (c) shield overrides ~62% of cuOpt proposals (only ~20% enact) — unaudited whether legit; (d) async fire-and-forget timing → many proposals still expire unused.
- **Nemotron is real but its ONE real lever was BROKEN.** `ottoq-orchestrator-agent` makes genuine NIM calls (model `nvidia/nemotron-3-ultra-550b-a55b` — the "404" fear is refuted; rationales cite real board numbers), but its `set_policy` failed 8/8 on a schema mismatch (`scope_type='sim_run'` vs constraint `('global','depot','run')`) → drove NOTHING. FIXED 2026-07-13 (`nvidia_02`: `ottoq_policy_set` normalizes sim_run→run + validates; edge fn v13 passes 'run' + stamps `nemotron` provenance). Verified: live invoke returned a real 550B directive, `rejected:[]`, l2_engine='nemotron'. `ottoq-nemotron-copilot`=read-only analyst (writes nothing, by design).
- **`ottoq-assign-optimize`=dead code** (real cuOpt, 0 callers); `ottoq-energy/sequence-optimize`=deterministic+advisory, NOT NVIDIA (misleadingly named).
- OPEN (disclosed, not fixed): shield 62%-override audit; sync/batch cuOpt to raise its share; decide whether production should ever enact cuOpt; wire-or-remove assign-optimize. The "393 enacted/7d" + "72% override" figures below are STALE — trust the live numbers above.

**✅ PRODUCTION DEMO LOOP LIVE + PROVEN (2026-07-15, Chase: "production for twin + OTTO-Q demo, make Nemotron+cuOpt fire, prove functionality w/ accurate comms").** Enacting operator run via `ottoq_sim_run_scenario(deck, seed, 'operator_demo')` on the Nashville prod depot (11111111); the demo metronome advances it. Verified end-to-end on normal_day run 42df8089 (9 ticks): OTTO-Q deciding (620 enacted, shield 0 overrides), **comms lines = 1269 msgs in `ottoq_comms_messages`** (uplink telemetry 860@78ms + ack 242 + downlink command 242@50ms + teleop assist_request/response 38/38 — bidirectional, OEM-typed, real latencies), twin executing the full lifecycle (deploy/charge L2+DCFC/wash-detail-service bays/stage). **8 scenario decks exist** (normal_day + aggressive_fleet_turnover, charger_outage_morning_rush, grid_brownout_at_peak, dr_event_cascade, heat_wave, winter_storm, solar_underperformance). `ottoq_sim_run_scenario` samples the variability profile (`ottoq_variability_instantiate`) = the twin's random-variable sampler.
- **NEMOTRON GAP FIXED (migration `demo_nemotron_advisory_conductor`):** Nemotron only fired in the production cron (4 decisions/30d); now wired into `ottoq_sim_decide_and_dispatch` as an advisory conductor (every 3rd tick, async fire-and-forget, gated to operator/demo runs, NEVER benchmark/production) → fires + produces l2_engine='nemotron' decisions on the demo loop.
- **cuOpt:** fires (415 enacted/7d) but idles on calm normal_day (gate drains in 1 tick) + its async proposals EXPIRE on fast/manual advance → drives only under contention + PROPER PACING. The paced metronome (below) keeps proposals fresh so cuOpt drives.
- **⭐ DEMO SPEED CONTROL (migrations `demo_speed_control_paced_metronome` + `fix_metronome_pacing_from_now`):** `demo_speed_x` col on ottoq_sim_runs (0.25-5x) + `ottoq_set_demo_speed(run,x)` RPC + REWROTE `ottoq_demo_metronome` to be PACED (advance only when due; real-sec-per-tick = 6/speed → 1x=6s, 5x=1.2s → at 5x the world advances 5x more sim-time/real-sec so vehicles ZIP). Fixes both unwatchable-too-fast AND cuOpt proposal expiry. Exposed in `ottoq_api_twin_get_state` (v1.1): demo_speed_x, real_seconds_per_tick, sim_minutes_per_tick(30), next_tick_due_at → the RENDERER reads these to scale motion (sim_min_per_tick / real_sec_per_tick = motion speed). ⚠️ RENDERER BINDING (cockpit A5 master-speed-slider → demo_speed_x) is the remaining FRONTEND piece for vehicles to visibly zip. Bug fixed: advance_tick_world sets next_due=now+30s; metronome must OVERRIDE to now+interval (was compounding to +33s → ~1 tick/min).

**✅ CERT-2 RE-MEASURED (2026-07-15, task #119 done): cuOpt override on stall_assignment is now 0% (891 decisions, fully shield-compatible) — down from 72%→25%→0 across the fixes.** cuOpt fires 83×/7d. The remaining override load is on the GREEDY (deterministic_v1 stall_assignment = 15.2% overridden, ~7161/14d), driven by EN.001.grid_capacity_ceiling + **HW.002.charger_state (reliability)** + **HW.004.stall_single_vehicle (congestion)**. **FRONTIER ASSIGNMENT PLAN (2026-07-15, Chase greenlit "prove the real edge + fold in NVIDIA"):** the 3 deterministic edges I flagged MAP to shield rules — SoC-safety=SLA.001, reliability=HW.002, congestion=HW.004/SLA.002 — which ARE cuOpt's constraint-aware cost-matrix targets. Track A: prove each edge vs a strong baseline (adversarial, per the energy lesson — optimizers often TIE good greedy). Track B: encode HW.002/HW.004/SLA.001 into cuOpt's cost matrix + A/B cuopt vs strong greedy (FR-3). Track C: Nemotron conductor (FR-4). Track D: FR-2 forecaster. Deep audit workflow running on production run 35aa33e3 (4050 ticks) to rank which edges are genuinely provable/audit-surviving BEFORE building. Production runs: 35aa33e3 (4050t), 237029f1 (241t).

---
**[HISTORICAL — 2026-07-01/02, framing since corrected above]** cuOpt in the loop:

- **Operator/scenario loop (what Chase watches):** `ottoq_sim_advance_tick` → `ottoq_cuopt_refresh` (demand-gated, debounced ≤1 NVIDIA call/40s/run; `ottoq.dryrun` GUC keeps MPC forks from real calls) → edge fn `ottoq-cuopt-propose` → rows in **`ottoq_external_proposals`** (source='cuopt' outranks all) → **`ottoq_decide_tick` consumes via `ottoq_l2_external_proposal` COALESCE** (deterministic ranker = fallback only). Last 7d: **393 cuOpt-sourced stall assignments ENACTED**, 1033 shield-overridden (~72% override rate vs the SQL ranker's ~47% — causes: proposal staleness + cuOpt's cost matrix is shield/energy-cap-blind), 678 energy-cap deferred. NVIDIA key present + working (0 fallback ticks in 24h; 180/180 orchestrate responses charge_source="cuopt").
- **Production loop:** cron `ottoq-depot-tick` (*/2 min) → `ottoq_cron_tick` → `ottoq_world_advance` (physics) + `ottoq-orchestrate-tick` (cuOpt full-depot plan, ~7s incl. 5s solver, fits the 120s cadence) + `ottoq-wave-admit` (commits gate admissions). Plans were evaporating for a week (no production run). **FIXED 2026-07-01 (Chase-authorized): production run `35aa33e3` started via `ottoq_sim_run_scenario('normal_day', NULL, 'production_live')`** — cuOpt plans now flow through `ottoq_shield_and_log` into `ottoq_decisions` (18 rows on the first tick) + shadow recommendations. Stale stuck run 8eecdb37 closed. **Coexistence rule: one depot = one fleet → production and operator demo runs are mutually exclusive; a demo launch supersedes production (restart = that one SELECT).**
- **Shadow semantics (verifier-proven):** `ottoq_shield_and_log` NEVER actuates in either mode — only INSERTs decision rows; the sole p_shadow difference is recommendation status 'shadow' vs 'admitted' (advisory in PULSE, human-confirmed). Flipping the cron to `'shadow',false` commits nothing physical.

**TIGHTENING BATCH SHIPPED + VERIFIED (2026-07-02, Chase-authorized "Apply now"):** (1) ✅ **`ottoq-cuopt-propose` v16 — CONSTRAINT-AWARE**: only healthy chargers offered (station_state='Available' + heartbeat<90s), reserved/occupied excluded, connector/inlet compatibility hard-constrained (BIG cost + fallback filter + belt-and-braces output filter), `requested_kw` = min(connector,inlet)×SoC-taper (the brain's exact E3 formula — was flat 150/19 → mass deferrals), assignments TRIMMED to the live energy cap (`ottoq_active_charge_cap_kw` rpc, accumulated lowest-SoC-first = the brain's own admission order), nearest-wash `relative_y` shaped into the cost, TTL 300→90s. Deploy-verified live (clean response, 45 healthy stalls found; full cuOpt path exercises on the next operator demo run). (2) ✅ **shadow→ADMITTED**: cron posts `'shadow',false` — 37 admitted recommendations landed first tick (bounded, 300s expiry; nothing physical commits). (3) ✅ **debounce 40s→25s** (proposals refresh every 30s tick). (4) ✅ **stale-proposal sweep**: recurring in cron_tick + one-time cleanup (0 stale pending left, was ~1000). Migration `cuopt_tightening_shadow_debounce_sweep`. **✅ OVERRIDE RATE MEASURED (2026-07-07, run `af8a6b20` aggressive_fleet_turnover, driven ~15min via background advance_due loop): cuOpt v16 = 25.0% override (9 enacted/3 overridden) vs the old 72% — ~3× better — AND BEATS the deterministic ranker under identical contention (37.1%: 22 enacted/13 overridden). Headline: "under load, the genetic optimizer's plans survive the safety shield BETTER than the hand-tuned rules."** Tapered kW confirmed in proposals (170, 19.2 — not flat 150). Caveats: modest sample (12 shield-evaluated cuOpt decisions); one `requested_kw=0` proposal seen (a vehicle/stall with 0 inlet/connector kW — minor data quirk, follow up). **KEY FLOW FINDING: on normal_day the gate now drains in 1 TICK** (arrivals straight to chargers, energy-deferred park in staging, promoted next tick, queue ~0) → cuOpt's async proposals arrive post-assignment and it's correctly IDLE on calm days — its value concentrates under contention (turnover/waves/outages). Pitch framing: deterministic speed on easy days, genetic optimization under pressure. **MEASUREMENT MECHANICS (reusable): drive a run headlessly via `for i in $(seq 1 45); do curl -s -X POST .../otto-twin-control/advance_due; sleep 20; done` (advance_due respects tick cadence; control API verify_jwt=false).** Chase's cockpit click never reached the backend that day (local dev server was down — restart it before he demos). Production run restarted after measurement: `237029f1`. **STILL OPEN:** (a) cockpit "decided by cuOpt" badge (`ottoq_decisions.proposed_action->>'source'`); (b) re-run CRN A/B + energy-dial monotonicity BEFORE re-quoting the ~42%/$10k headline; (c) the requested_kw=0 quirk. Nemotron 3 Ultra decision-copilot unchanged. **GPU spend NOT needed for cuOpt** (hosted API) — Chase's $10k stays parked for Isaac/Cosmos.
