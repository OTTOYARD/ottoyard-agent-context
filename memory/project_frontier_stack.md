---
name: project_frontier_stack
description: "FRONTIER mandate (Chase 2026-07-14): replace every OTTO-Q heuristic with a real optimizer/learned model — the 5-layer intelligence stack (forecast→optimize→orchestrate→shield→learn). AWS $100k, ship 1-2 wks. Energy MPC built + verified (+38% vs heuristic)."
metadata:
  node_type: memory
  type: project
  originSessionId: b5cc1e94-2201-451a-a46f-c770df70e99a
---

**CHASE'S FRONTIER MANDATE (2026-07-14):** FRONTIER = state-of-the-art, optimizers + intelligence engines on EVERY decision surface (no hand-tuned heuristics), leverage NVIDIA + open-source, seek every ounce of alpha. **~$100k AWS credits (NVIDIA Inception), maximum resources ("never go short or easy, ALL THE WAY"), product shippable in 1-2 weeks.** I sequence the build; each layer must beat the heuristic it replaces under a CRN A/B (the bar that retired the fake 7×/54% headlines this session).

**PLAN OF RECORD:** `~/Desktop/OTTO-Q V1/OTTOQ_FRONTIER_ARCHITECTURE.md` (validated by a 110-agent adversarial deep-research pass — every layer is the literature's SOTA). The 5-layer stack "forecast → optimize → orchestrate → shield → learn":
1. **Forecast** — TFT + quantile nets (NeuralForecast/Nixtla), probabilistic; AWS GPU tier. [FR-2]
2. **Optimize/Energy** — forecast-driven ROBUST MPC (Bertsimas-Sim budget) + MILP: 15-min demand ratchet + TOU + degradation; HiGHS (or cuOpt PDLP). [FR-1 — BUILT+VERIFIED]
   **Optimize/Assign** — OR-Tools CP-SAT via PyJobShop (charge→wash→service→stage flow-shop) + NVIDIA cuOpt VRP. [FR-3]
3. **Orchestrate** — SINGLE central Nemotron conductor (NIM) dispatching optimizers as tools (OptAgent/COOPA; centralized beats committee). [FR-4]
4. **Shield** — keep deterministic + separately authored (NeMo Guardrails only wraps LLM NL). Advisory-only AI.
5. **Learn** — Bayesian/bandit tuning of optimizer params, CRN A/B validated. [FR-5]

**BUILT SO FAR:** `~/Desktop/ottoq-intelligence` (git repo, FastAPI intelligence service = the AWS backbone). **FR-1 energy MPC LIVE+UNIT-VERIFIED:** rolling-horizon LP (HiGHS/highspy) minimizing demand-charge ratchet + TOU + wear. On a realistic wave vs the live heuristic (cert_03): **MPC 771 kW / $16.8k/mo vs heuristic 1250 kW / $27.2k/mo = 38% MORE shave ≈ ~$125k/yr additional alpha/depot**, optimal + SoC-feasible. `python tests/test_energy_alpha.py`. Dockerfile + AWS App Runner playbook (`deploy/AWS_PLAYBOOK.md`) ready. Endpoints: /optimize/energy [live], /forecast /assign /orchestrate [stubs].

**FR-0 AWS SERVICE LIVE (2026-07-14):** `ottoq-intelligence` FastAPI deployed on **EC2 `http://100.53.130.57:8080`** (App Runner closed to new customers → EC2 + user-data bootstrap; repo made PUBLIC to fix git-auth). Bearer token `ottoq-frontier-a7f3c9d1e5b8` (dev). `/optimize/energy` LIVE (HiGHS); `/forecast /assign /orchestrate` stubs. Warm round-trip ~0.3–2s.

**FR-1 TWIN↔SERVICE SEAM BUILT + UNIT-VERIFIED (2026-07-14):** closed loop wired into the twin.
- **KEY ARCH FINDING:** Supabase DB `pg_net` **CANNOT** reach the raw EC2 (TCP/SSL handshake timeout — DB egress is HTTPS/edge-fn only). FIX = **edge-function bridge** `ottoq-energy-mpc` (verify_jwt=false + `x-bridge-token`; Deno fetch → AWS). Same path cuOpt/Nemotron use. This is THE way the twin calls any external compute.
- **Plan storage + async pattern:** table `ottoq_energy_plan`; `ottoq_energy_mpc_replan(run,depot,horizon,tick_min,forecast_load_kw,...)` FIRES the request + inserts a `pending` row; `ottoq_energy_mpc_ingest(plan_id)` / `_ingest_run(run)` COMPLETE it from the landed pg_net response (edge-fn cold-start + pg_net batched worker ⇒ 30–60s latency; fire-and-ingest tolerates it, matches out-of-band production replanning). Pass `p_forecast_load_kw` for the perfect-forecast cert path; else builds a twin-observable v1 forecast (base persistence + decaying EV + `ottoq_predict_arrivals` per step — SAME arrival signal the heuristic uses ⇒ fair fight; FR-2 replaces this term).
- **Follow hook:** `ottoq_energy_orchestrate` follows `plan.bess_setpoint_kw[current_step]` when policy **`energy_mpc_follow`>=1** (new lever, registered in `ottoq_policy_param_catalog`, DEFAULT 0 = heuristic, so production untouched). Doctrine-safe: only shapes BESS dispatch (+discharge/−charge, SAME sign as heuristic — no flip), charging never throttled (charge_cap advisory), thermal + EN.003 shield still pre-empt/clamp. VERIFIED: flag on ⇒ source=`otto_q_mpc`, emitted setpoint == plan setpoint (755.8 @ step 19); flag off ⇒ heuristic fallback.
- On the real logged wave (forecast peak 1092 kW) the MPC schedule shaves to **336 kW with the depot's REAL BESS** (3 MWh @ 81%, 1500 kW) — vs heuristic 1093 billed. Counterfactual (test_real_wave_counterfactual.py) = 267 kW w/ a fuller BESS.
- Migrations (named, in Supabase history): `fr1_energy_plan_table_and_replan`, `fr1_replan_fire_and_ingest`, `fr1_orchestrate_follow_mpc_plan` + catalog insert. Edge fn `ottoq-energy-mpc` v1.
- **REMAINING FR-1 = the full in-sim closed-loop A/B cert:** arm wave → run heuristic (flag off) + log per-tick net load → replan with that logged load (perfect-forecast) → re-arm → run MPC-follow (flag on) → compare BILLED peak. Isolates the OPTIMIZER's in-sim value from forecaster error (FR-2's job). Watch: sim non-determinism (seeds from random sim_run_id) ⇒ certify via REPLICATION not CRN; metronome cron footgun (keep cert runs non-`running` at commit boundaries). Bridge token + AWS URL are hardcoded in the edge fn + replan defaults — move to Supabase secrets before real productionizing.

**FR-1 IN-SIM A/B CERT — HONEST RESULT (2026-07-15).** Ran the closed-loop cert on the benchmark depot. Custom driver: `ottoq_fr1_cert_arm(run,seed,group,ticks,start,mpc)` (one-txn, cron-isolated, pre-generated run id) + `ottoq_energy_mpc_replan(...,p_forecast_from_run)` + `ottoq_fr1_reactive_min_peak(run,precharge)` (best-tuned reactive baseline, binary-searched). Billed peak measured as run-scoped `MAX(grid_import_kw)` (NOT the contaminated `ottoq_ab_runs.energy_peak_kw`, which is an unfiltered depot MAX — benchmark_reset does NOT clear site_energy_snapshots).
- ✅ **Execution fidelity: EXACT.** On the live MPC-follow arm, `bess_output` == plan setpoint every tick (verified 40/40), 0 gap, SoC feasible, charging never throttled (doctrine-safe).
- ⚠️ **CRN does NOT hold:** two identical-seed/-T0 arms differ 16% in peak (sim seeds RNG from the random sim_run_id, not random_seed). So single live paired runs are noise-dominated + open-loop MPC is fragile to forecast/peak-timing mismatch (a live MPC arm got 742 on an 821 wave). ⇒ certified via **counterfactual on the same recorded wave** (both policies applied to the identical real load; rigorous BECAUSE execution is verified exact). LP predicted peak == peak recomputed from recorded load+setpoints, to the kW (airtight).
- **vs SHIPPED heuristic (5 real waves): ~54-64% peak shave (mean ~57%), ~$11k/mo (~$131k/yr)/depot, 0-unsafe.** BUT this is vs an IDLE baseline: the shipped heuristic's shave target = service_max×0.5 = 1250 kW > these waves' peaks (785-974) ⇒ it does nothing. So the $131k is "the shipped system is MIS-TUNED and leaves money on the table," captured automatically by the optimizer (or a re-tune).
- **vs a PERFECTLY-TUNED reactive clip-to-target controller (single-objective peak): MPC edge ≈ 0** on these big-battery (3 MWh), energy-UNconstrained waves — simple clipping at the right level ties the LP to the kW (360=360, 426=426...). HONEST: the optimizer does NOT beat a well-tuned heuristic on pure single-objective peak-shaving here.
- **MPC's UNIQUE, durable edge = MULTI-OBJECTIVE.** Same wave + realistic TOU price: same peak (~358) but MPC arbitrages TOU (charge cheap/discharge expensive) ⇒ ~$15k/yr/depot lower TOTAL bill vs the tuned reactive controller (which ignores price). Grows with price volatility/LMP, DR events, solar, degradation-aware cycling, + auto-tuning per site/season (no manual target). `scratchpad/tou_multiobjective_demo.py`.
- **INDEPENDENT ADVERSARIAL AUDIT (5-agent workflow, 2026-07-15): all 5 claims CONFIRMED** — measurement integrity (grid_import recompute matches to 0.1 kW, battery netted, run-scoped), execution exact (bess_output==plan setpoint to 0.00 kW over 40 ticks, charge never throttled, SoC in bounds), shave 54-64%, baseline-fairness ("refutation FAILED: could not find any wave where MPC beats the tuned reactive baseline"), 0-unsafe. Audit validated the CAVEATS, didn't break the numbers.
- **BUG found+fixed by the audit:** `ottoq_score_run` wrote `ottoq_ab_runs.energy_peak_kw` as an UNFILTERED depot MAX → every run showed a stale 3646.7 kW (from an unrelated 2026-07-11 run), a dashboard-misleading unfiltered-MAX artifact. FIXED (migration `fix_score_run_energy_peak_run_scoped`): run-scoped `MAX(grid_import_kw) WHERE sim_run_id`; historical rows backfilled. [[feedback_gap_ownership]]
- **NET honest pitch:** optimizer WORKS + executes exactly + 0-unsafe (independently audited); the demand-shave headline is real vs the shipped system but re-tune-recoverable; the frontier value is multi-objective co-optimization + auto-tuning robustness. **Next to prove it hard: multi-objective cert (TOU+demand+degradation+solar) + energy-constrained-battery + LMP/DR-volatility scenarios — that's where the MPC separates.** Also: fix determinism (seed from random_seed) for true live CRN; receding-horizon replan for live robustness. Migrations named `fr1_*` in Supabase history + `~/Desktop/otto-q-core-export` (edge fn + FR1 doc committed df6a0ef).

**FR-1b MULTI-OBJECTIVE TOTAL-BILL CERT (2026-07-15) — where the optimizer earns its keep.** Since FR-1 showed the MPC ties a tuned reactive clip on single-objective peak-shave, FR-1b tests TOTAL MONTHLY BILL (demand + TOU/LMP energy + $0.02/kWh wear) vs the BEST hand-tuned reactive controllers, on 3 real twin waves (noise-free counterfactual; execution already proven exact).
- **Optimizer extended:** added a SOFT DR/feeder grid-cap constraint (honored when the battery can comply → 0 violation; reports minimal unavoidable violation when it physically can't — NEVER holds vehicles, doctrine-safe) + `dr_violation_kwh` + `total_bill_usd`. Committed `ottoq-intelligence` 0ad4a64 (energy_mpc + main + tests/test_multiobjective.py + cert/fr1b_multiobjective_ab.py). ⚠️ PUSH BLOCKED by GitHub email-privacy setting on Chase's account (commit local only); EC2 service NOT yet redeployed (still runs pre-DR-cap optimizer — fine, twin doesn't send DR params yet). Redeploy = Chase-side (rebuild container from repo).
- **RESULT vs `reactive_combined` (a WEAK baseline — ⚠️SUPERSEDED, see REFUTED below):** MPC "saved" ~$20k/yr (TOU) / ~$36k/yr (constrained). This gap VANISHES against a stronger hand rule (next bullet) — do not cite. Still-valid sub-finding: a naive TOU-only rule (charge-when-cheap) BLOWS the peak to 866-2236 kW ("one rule can't serve two masters") — but a *reserve-aware* rule serves both masters as well as the LP.
- **🚨 REFUTED (adversarial attack, verified 2026-07-15): the ~$20-36k/yr multi-objective edge was an ARTIFACT of a WEAK baseline (`reactive_combined`).** An attack-workflow agent hand-built a reserve-aware reactive controller (`scratchpad/adversary4.py` `reactive_reserve`: bisection peak-target + BACKWARD-induction min-SoC reserve protecting every future forced clip + price-percentile arbitrage of only the truly-surplus energy — NO solver) that **MATCHES the LP TO THE PENNY on ALL scenarios** (independently re-run: flat 22863.4=22863.4, TOU 23882.2=23882.2, LMP 18771.3=18771.3, constrained 35733.1=35733.1 — identical peak+energy+wear). ⇒ **On this single-battery demand+TOU+linear-wear problem the optimizer's $ edge over the BEST hand controller is $0, multi-objective included.** My earlier "$20-36k/yr" and "$10-16k/yr realistic" numbers to Chase were WRONG (weak-baseline artifact) — corrected. The LP's ONLY genuine value here is GENERALITY/TRACTABILITY (write objective+constraints once; add DR-cap/solar/multi-asset/V2G/network without re-deriving a bespoke control law — the reserve controller is expert-derived + brittle to problem changes) — a SOFT, non-$ engineering-leverage argument that only pays off IF that complexity materializes. NOT yet shown that the LP beats a (harder-to-derive) hand rule even on DR-cap (untested by the attacker).
- **⚠️ FORECAST-ERROR ROBUSTNESS (the decisive finding, `scratchpad/fr1b_forecast_robustness.py`):** the multi-objective edge is REAL but CONTINGENT. Swept multiplicative load-forecast noise vs the forecast-free reactive baseline (TOU, receding-horizon = re-solve each tick w/ true-so-far + noisy-ahead, apply setpoint[0]): **OPEN-LOOP MPC is fragile — it LOSES to a simple reactive controller at just 10% forecast error** (full battery: +$1,976→−$350/mo; this IS the 742-vs-425 twin failure). **RECEDING-HORIZON MPC holds the edge to ~20-30% error** (feedback corrects drift: +$1,369 @10%, +$848 @20%, +$396 @30%) but the edge SHRINKS — realistic 10-20% error roughly HALVES the perfect-foresight number to ~$10-16k/yr. ⇒ **the optimizer's value REQUIRES (a) receding-horizon deployment (NOT the current open-loop plan-follow) AND (b) a decent forecaster (FR-2) — both now PROVEN mandatory, not optional.** Engineering implication: async bridge latency (30-60s) blocks per-tick receding-horizon in the fast twin → needs cached/co-located optimizer or slower real-time ticks.
- **REPLAN-LAG tolerance (`scratchpad/fr1b_replan_lag.py`, TOU @20% err):** receding-horizon is SAME-TICK-fresh sensitive — instant (lag 0, plan solved this tick from measured state) keeps +$903/mo edge, but a 1-TICK-STALE plan collapses to −$3,267/mo (WORSE than reactive; the demand charge is a max, so one mistimed discharge spikes the peak). ⇒ requirement: closed-loop, bridge round-trip < tick interval, AND FALL BACK to the reactive/heuristic when no fresh plan (following a stale plan is net-negative). Production ticks (minutes) ≫ 30-60s bridge latency → lag~0 → works; only the sub-second benchmark needs a co-located optimizer. **⚠️ The FR-1 twin hook is OPEN-LOOP (follows a pre-solved plan) = the fragile mode; must convert to closed-loop same-tick + staleness fallback before any live energy deployment.**
- **STRATEGIC TAKE FOR CHASE:** the bulk of capturable value (~$131k/yr) is FIXING THE MIS-TUNED SHIPPED HEURISTIC (lower its 1250 kW shave target = a config change, ~free, no optimizer/forecaster/real-time-control needed). The optimizer adds a MARGINAL ~$10-16k/yr (realistic) multi-objective edge but demands FR-2 forecaster + closed-loop real-time control + fallback. Pragmatic sequence: (1) re-tune the heuristic NOW (most of the money, trivial), (2) build the optimizer path for the marginal multi-objective edge when TOU/LMP/DR/solar complexity justifies it.
- **Frontier pitch (net across FR-1+FR-1b, CORRECTED HONEST):** optimizer WORKS, executes exactly, 0-unsafe, audited — but on the CURRENT single-battery energy problem it is worth **~$0 in $ terms over a well-designed controller**. The ONLY real, capturable energy money is **~$131k/yr from RE-TUNING the mis-tuned shipped heuristic** (lower its 1250 kW shave threshold — a config change; a reserve-aware reactive rule captures it with zero AI/forecaster/real-time-control). The LP optimizer's value is GENERALITY for FUTURE complexity (DR + solar + multi-asset + V2G + network), unproven to beat a hand rule until that complexity exists. Open-loop / stale-plan is a NET-NEGATIVE trap (worse than a rule under any forecast error or ≥1-tick staleness). **DO NOT quote a multi-objective $ edge.** Honest sequence: (1) re-tune heuristic = the money, trivial; (2) treat the optimizer as future-proofing, justified only when TOU/DR/solar/multi-asset complexity lands. FR-2 forecaster + closed-loop only matter IF the optimizer path is pursued. EC2 redeploy/push blocked (email-privacy / Chase console).

**#127 ENERGY QUICK-WIN — THE RE-TUNE WORKS (2026-07-15).** The pragmatic capture, validated on the benchmark:
- Built a causal water-fill controller (`ottoq_bess_reserve_target` + `energy_reserve_shave` policy, gated default-off) that sets the demand target each tick from the twin's forecast. **It only shaved 3%** — the twin's `ottoq_predict_arrivals` forecast is too ERRATIC (target jumped 1373→352→274, idle at a load spike) to drive a water-fill. HONEST: forecast-driven control is a trap here; a forecast-free reactive clip is the answer. (Reserve controller left in place, off by default; needs FR-2 forecaster to be useful.)
- **The actual fix = lower the shipped fixed target.** `energy_demand_factor_peak` 0.5→0.25 (target 1250→625) on a real wave: **raw 809 → billed 478 kW = 40.9% peak shave, 0-unsafe, BESS ended 32% (healthy).** Shipped 0.5 = idle = 0%. So a CONFIG CHANGE captures a large, robust, forecast-free demand shave.
- ⚠️ CAVEATS (do not over-quote 41%): (1) the catalog CLAMPS `energy_demand_factor_peak` to min 0.25 (a guard — lowering it shaves deeper, a policy call); (2) billed 478 < target 625 = the controller OVER-discharged, because orchestrate sizes dispatch off `v_desired_ev` (forward unconstrained charge RATE) which exceeds the actual metered charger load → dispatch oversized. So the CLEAN shave holding-at-625 is ~23%; the observed 41% is dispatch-oversizing-inflated. Real clean number needs the dispatch sized to metered load (or desired_ev run-scoped) + re-cert. Flagged as a task.
- **NET #127:** re-tune (factor down, per-site, off the mis-tuned 1250) = the real ~$50-100k/yr capture (23% clean → ~$50k, 41% observed → ~$100k; NOT the $131k which assumed 54% perfect-foresight), config-only, robust, 0-unsafe — RECOMMENDED. Migrations `fr1b_bess_reserve_target` + `fr1b_orchestrate_reserve_shave`.
- **FORECASTER RABBIT-HOLE (CTO-instinct dig, 2026-07-15) — decisive:** tried to fix the erratic forecast so the causal water-fill would shave deep. `ottoq_predict_arrivals` already uses the schedule (telemetry-ETA analog) but only counts per-window NEW arrivals (spiky, misses continuing sessions). Built `ottoq_forecast_net_load` (cumulative: active sessions + scheduled returns × charge-duration). It **OVER-forecasts ~2.5×** (target 2059 vs actual peak 832) because it assumes every arrived vehicle charges IMMEDIATELY + CONCURRENTLY, but real depot charging concurrency is ~5-6 (gated by the gate→assignment→service→charge FLOW, not chargers). **An accurate site-load forecast requires modeling the depot's flow/queue dynamics = essentially re-simulating the depot = genuinely hard = the exact job of FR-2 (learned forecaster on twin data).** Hand forecasters can't do it. ⇒ water-fill/optimizer are BLOCKED on FR-2; the **forecast-FREE reactive clip (#127) is the deployable energy answer** and needs none of it. `ottoq_forecast_net_load` + reserve controller left as gated-off scaffolding for FR-2. **ENERGY PILLAR now exhaustively characterized + closed** — money = the re-tune; everything fancier waits on FR-2 (hard) and only matters under multi-objective complexity (which ties a hand rule anyway).

**NVIDIA:** cuOpt + Nemotron/NIM are HOSTED APIs (no GPU, Inception key). GPU tier (AWS g5/SageMaker/Triton) = self-host TFT forecaster + RL. Physical-AI (Cosmos/Isaac) = separate track (parked GPU spend).

**NEXT:** (a) deploy service to AWS (Chase, ~10 min via playbook) OR build Deno edge-fn path → (b) wire energy MPC closed-loop into the twin + cert the +38% holds in-sim → (c) FR-2 forecaster → FR-3 → FR-4 → FR-5. **KEY RISK to fix:** twin seeds RNG from random sim_run_id (not random_seed) so CRN never held — fix for true paired A/B. See [[project_vehicle_first_doctrine]] (the energy heuristic this optimizer replaces + the cert harness `ottoq_cert_arm`), [[reference_nvidia_ai_integration]], [[project_frontier_core]].
