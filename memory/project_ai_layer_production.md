---
name: project_ai_layer_production
description: "FR-3 + FR-4 production build of the OTTO-Q AI layer (2026-07-16): cuOpt now FIRES rapidly + DRIVES assignments (solve-window + deterministic poll + milp_local yields → 17 cuOpt-enacted in 8 ticks, was ~0), and Nemotron auto-actions an expanded lever set with human-approval routing. Full seam map + the exact fix."
metadata:
  node_type: memory
  type: project
  originSessionId: b5cc1e94-2201-451a-a46f-c770df70e99a
---

**Context:** Chase 2026-07-16: "make cuopt fire rapidly + extremely quickly, and Nemotron auto-action its directives; make it FUNCTION; production, nothing held back." Approved ceiling: Nemotron auto-actions **dials + whitelisted ops actions**; out-of-whitelist → Pulse approval queue; all clamped + shield-gated + audited. Plan doc: `~/Desktop/OTTO-Q V1/OTTOQ_AI_LAYER_PRODUCTION_PLAN.md`.

**THE LIVE SEAM (verified from source):** decide cycle = `ottoq_sim_decide_and_dispatch` → (otto_q) `ottoq_cuopt_refresh` (async pg_net fire to edge fn `ottoq-cuopt-propose`, REAL NVIDIA `optimize.api.nvidia.com/v1/nvidia/cuopt`) + `ottoq_l2_optimize_assignments` (**synchronous greedy in-tick assigner, source='milp_local'** — this already drove every tick) → `ottoq_decide_tick` (consumes via `ottoq_l2_external_proposal`, which **already prefers source='cuopt'**, 35-min freshness) → shield. **Root cause cuOpt never drove:** cuopt_refresh fired in the SAME txn as decide → pg_net only sends after COMMIT → cuOpt physically can't answer before decide → milp_local (instant) wins, cuOpt proposal lands late → 26/26 expired. Enacting loop driver = `ottoq_demo_metronome` (per-tick COMMIT). Levers: `ottoq_policy_get(run,key,default)` / `ottoq_policy_set(scope,scope_id,key,val,by)`; catalog `ottoq_policy_param_catalog`; approval queue table `ottoq_ops_approvals`; Nemotron sees `ottoq_agent_board(run)`.

**FR-3 BUILT + VERIFIED (cuOpt fires rapidly + drives):** SOLVE WINDOW in the metronome — after world-advance, if contention (≥`cuopt_contention_min` gate vehicles w/ soc<85), fire cuopt_refresh → COMMIT (pg_net sends) → **deterministically POLL `net._http_response` for that request id** up to `cuopt_solve_window_ms` (default 4000, capped 6s) → then decide. Plus `ottoq_l2_optimize_assignments` now **YIELDS**: skips any vehicle with a fresh pending cuopt proposal (cuOpt owns it; greedy fills gaps). Debounce 25s→tunable `cuopt_debounce_s` (4). New clamped catalog params: cuopt_solve_window_ms/contention_min/debounce_s (window=0 → instant revert to greedy). **RESULT (run 93befd1b, aggressive_fleet_turnover, 8 ticks): cuOpt enacted 17 stall_assignments (milp_local 11) = 61% of the optimizer-seam under contention, 0 unsafe deploys.** Was ~0 enacted / 26-expired. Migrations: fr3_cuopt_solve_window_params_and_debounce, fr3_metronome_deterministic_poll_window, fr3_milp_local_yields_to_cuopt. HONEST: milp_local greedy is near-optimal on simple cost; cuOpt's edge is under real contention (waves/outage). For 100×50 assignment an in-DB Hungarian is a future optimal+network-free option; cuOpt's true home = larger depot-wide/routing.

**FR-4 BUILT (Nemotron auto-actions, awaiting live receipt):** `ottoq-orchestrator-agent` v14: KNOBS expanded 3→6 (added deploy_surge_catchup, forecast_horizon_min, energy_reserve_shave; binary knobs set-direct, continuous ±30% drift), NEVER deploy_floor_soc. New `ops_action` type → dispatcher `ottoq_apply_ops_action` (auto-exec whitelist: raise_deploy_surge / extend_forecast_horizon / enable_energy_reserve, each clamped) → **anything else + non-whitelisted set_policy → `ottoq_ops_approvals` (pending) NOT silent drop.** Event-trigger `ottoq_orchestrator_trigger(depot)` (gate rush ≥5 / holding backlog ≥25) wired into decide gate: fires on 3-tick heartbeat OR trigger. Audit → ottoq_decisions {applied,queued,rejected,rationale}. Migrations: fr4_ops_action_dispatcher, fr4_orchestrator_event_trigger, fr4_wire_event_trigger_into_decide. **v15 VERIFIED**: disabled Nemotron reasoning (`<think>` traces overran max_tokens → 60% fell to fallback) + `response_format:json_object` + robust multi-span parser → parse rate 40%→83% (5/6 fires), 13 actions applied across dials+ops_actions, smart doctrine-compliant rationale, all audited, graceful fallback. TODO for full #125: FR-5 CIL (learn from outcomes → tune policy); approval-routing built but not exercised live (model stayed in-whitelist); orchestrator fire-debounce for prod cost; extend solve-window to production driver (`ottoq_world_advance`); optional lower cuopt edge `time_limit` 5→2.

**DEPLOY-CHAIN FUNCTIONAL SOUNDNESS (2026-07-16, found via Nemotron's own signal — it flagged deviation/holding backlog).** Two real bugs found+fixed+verified under aggressive_fleet_turnover at peak hour (run 93befd1b), 0-unsafe throughout:
1. **Wash-lane deploy-starvation** (migration `deploy_pressure_defer_optional_wash`): `ottoq_sim_advance_service_flow` forced EVERY `charge_complete_holding` vehicle through the 3-bay wash lane before deploy → wash = fleet-wide chokepoint under turnover → only 30% deployed vs ~100% peak target. Wash/detail are `must_do=false` (deferrable). FIX = STEP 1.5 fast-track: under deploy pressure (`deployed < ottoq_deploy_target_fraction(hour, deploy_peak_fraction)*fleet`), a charged vehicle (soc≥floor) with NO open must-do atom besides charge/readiness skips the wash lane → deploy-prep; deferrable wash carries over. VERIFIED: 8 fast-tracked, holding 36→29.
2. **Under-charged stranding DEADLOCK** (migration `recharge_stranded_underfloor_vehicles`): vehicles reached staging UNDER-charged (avg 66% SoC, some <10%), 80% deploy floor correctly blocked deploy, but NOTHING routed them back to charge → 23 stranded ~4 sim-hrs with open charge needs. FIX = STEP 0 re-charge sweep: any post-charge staging vehicle below `deploy_floor_soc` with an open charge atom (not on active dispatch) → `arrived_at_gate` so cuOpt/milp re-charges it. VERIFIED: 19 re-charged, stranded 23→4, charging ~7→30, deployed 33→37, 0-unsafe. Both doctrine-aligned.
3. **Deploy-target global-fleet bug** (migration `deploy_target_depot_scoped_fleet`): `ottoq_sim_auto_dispatch_tick` computed the deploy target off `COUNT(*) autonomous` across ALL depots (163) not the run's depot (116) → target > depot fleet, permanent over-stated pressure. FIX: scope `v_total_fleet` to `home_depot_id=v_run.depot_id`. VERIFIED: target 163→87 (sane), 0-unsafe. (Latent follow-up: the vehicle-PICK loop in the same fn is also un-depot-scoped — only bites under concurrent multi-depot runs, which don't happen today.)
**ROOT-CAUSE of #2 RESOLVED (honestly): there is NO charge-completion bug.** Investigated `ottoq_sim_advance_charge_sessions` + `ottoq_sim_stop_charge_session`: the completed path only fires at `soc>=target-0.5`; the fault path re-queues under-target vehicles (S3a). My initial "fault-guard mismatch" hypothesis was DISPROVEN by data — all fault modes are named `fault.*` so `LIKE 'fault%'` matches and S3a fires. Under-floor staging vehicles arrive via NON-charge paths (emergency_staged, tow, run-init scatter, 0-9% depleted edge cases) that had no re-charge safety-net → STEP 0 sweep is the correct universal fix. **AI-TIE-IN VERIFIED (run 93befd1b): the queuing loop = fault/under-charge → recharge sweep → arrived_at_gate → cuOpt charge-assignment (20 enacted) → charge → deploy-pressure fast-track (18, reads Nemotron deploy_peak_fraction) → dispatch; Nemotron 56 actions applied. Deployed 37/87 target = remaining gap is REAL service-throughput capacity under 2× turnover (3 wash + 2 svc bays), not a bug — Chase's robot/sensor thesis. See [[project_depot_ops_model]], [[project_robotics_automation_direction]].
4. **Null-timer bay DEADLOCK** (migration `selfheal_null_timer_bay_deadlock`): vehicles found stuck in wash/service bays with `service_ends_at=NULL` → STEP 1 completion (needs a non-null elapsed timer) never fired → they occupied the scarce bays (the fleet bottleneck) PERMANENTLY. FIX: STEP 1 now also completes NULL-timer bay vehicles (self-heal each tick, COALESCE timestamps), robust to whatever produced the null. VERIFIED: null_timer_bays 5→0, 5 self-healed, bays back in circulation, deployed→40, 0-unsafe. (Root of the null admission not traced — the per-tick self-heal recovers it regardless; worth a look if it recurs on a fresh run.) NOTE: run 93befd1b was paused/resumed ~7× during verification so its absolute state is messy; each fix was verified by its own delta + event receipts, not absolute counts.

**AI INTO SERVICE→REDEPLOY (2026-07-18, A1+A3 done) — follows the service→redeploy logic rebuild in [[project_depot_ops_model]].** Audit finding RC-8: the optimizer was locked out of service→deploy — only `ottoq_decide_tick` loop 3 (stall_assignment) consulted `ottoq_l2_external_proposal`; redeploy (loop 2) + service-order (loop 5) were pure heuristics; cuOpt only matched charge stalls; Nemotron inert. **A1 (migration `ai_A1_A3_extend_proposal_seam_redeploy_service`):** loops 2 & 5 now `COALESCE(ottoq_l2_external_proposal(run,'redeployment'|'service_sequencing','vehicle',id), <heuristic>)` — same proven pattern as stall_assignment; heuristic stays the safe fallback, shield untouched. **A3:** decide_tick now closes the proposal lifecycle each tick (consumed→'enacted', past-freshness→'expired') so cuOpt/Nemotron utilization + override rate are measurable (`ottoq_external_proposal` was STABLE/read-only, never mutated status → 32 pending/3 enacted invisible before). VERIFIED: decide_tick rewrite is error-free end-to-end (metronome run to tick 5: 153 decisions, 0 errored, 15 redeploys enacted via the seam, 0-unsafe). **A4 VERIFIED WORKING (2026-07-18):** OTTO-Q PRIME (`ottoq-orchestrator-agent` v15, Nemotron 3 Ultra `nvidia/nemotron-3-ultra-550b-a55b`) is NOT inert — the audit's "0 events" was a measurement artifact (it writes `ottoq_decisions` resolved_action_context='orchestrator_agent', not events). LIVE TEST: fires via the metronome heartbeat (2 fires by tick 4, real model not fallback), returns board-grounded actions, and SET `deploy_peak_fraction`→1.0 (drives redeploy pacing — decide_tick + auto_dispatch both read this dial) + energy dials, 0 approvals/0-unsafe. So Nemotron already drives the service→deploy STRATEGY (deploy pace) + energy; the seam map is `~/Desktop/OTTO-Q V1/OTTOQ_SEAM_MAP.md` (bidirectional TWIN⇄OTTO-Q⇄{cuOpt,Nemotron}, verified). **PRODUCTION GAP → CLOSED (A5-prod, migration `ai_A5prod_unify_production_brain`, 2026-07-18).** Analysis found `ottoq_world_advance` (the production_live cron path via `ottoq-depot-tick`) was **"WORLD PHYSICS ONLY (no decider)"** — production advanced energy/telemetry/charge but NEVER ran OTTO-Q's brain (no decide_tick / auto_dispatch / cuOpt / Nemotron). The demo path (`ottoq_sim_advance_tick → ottoq_sim_decide_and_dispatch`) had the full brain; production was a lesser world-only loop. production_live is DORMANT (4 runs, last completed 2026-07-07) so this was architectural, not live. FIX: `world_advance` now (a) also advances `ottoq_sim_advance_service_flow`, and (b) calls `ottoq_sim_decide_and_dispatch` after the world physics — the SAME brain as demo; PLUS relaxed the Nemotron-fire gate in decide_and_dispatch from `NOT IN ('benchmark','production_live')` → `<> 'benchmark'` so the conductor fires on production too (benchmarks stay AI-free for clean A/B). Dormant-safe (no-op until a production run is live). **VERIFIED on a test production_live run (4 ticks via world_advance): 186 brain decisions, Nemotron fired 4×, 45 charge assigns, 0 errored, 0 unsafe.** Demo + production now run IDENTICAL intelligence. Seam map: `~/Desktop/OTTO-Q V1/OTTOQ_SEAM_MAP.md`.

**A2 DONE + VERIFIED (migration `ai_A2_service_sequencing_priority_proposer`, 2026-07-18).** decide_tick loop 5 ordered the SCARCE service bay by FIFO (last_state_change) — blind to *why* a vehicle waits. New `ottoq_service_priority_propose(run)` writes real `service_sequencing` proposals into the optimizer seam from a multi-factor priority model: **must_do service atom (60) + dwell_min (1.0/min) + urgency (tech_hold 80 / immediate_dispatch 40) + downstream deploy value (soc×0.2)**, top-K where K = free staff-gated bay slots; wired into `ottoq_sim_decide_and_dispatch` next to `ottoq_l2_optimize_assignments` (so it's fresh for the same tick, on BOTH demo + production paths). VERIFIED live: `service_sequencing/ottoq_service_priority/**enacted:2**` (emitted → consumed by decide_tick → enacted → A3 lifecycle closed), 0 errored, 0 unsafe. **HONEST ENGINEERING NOTE (deliberate, tell Chase if asked):** service bays are INTERCHANGEABLE, so top-K-by-cost is *provably optimal* — shipping that to a solver would be theater. cuOpt is kept where the combinatorics are real (heterogeneous charger assignment: connector compatibility × power × position × energy cap). The A2 win is replacing FIFO with a genuine priority MODEL + closing the optimizer seam, not a solver round-trip. Redeploy selection was deliberately NOT solver-optimized either: doctrine is "deploy everything ready" (minimal parking), so selection adds ~nothing over SoC-desc.

**REMAINING AI (optional polish):** cuOpt emission for `redeployment` (low value per doctrine) (cost matrix over service urgency×dwell×downstream-deploy-value + redeploy selection) so the seam gets real AI input, not just heuristic fallback; A4 = make the Nemotron orchestrator (`ottoq-orchestrator-agent`) actually FIRE on the run (audit: 0 ops_approvals/0 orchestrator events) and tune the reconciled dials (deploy_peak_fraction, service staff cap, drain cap). Then safety re-cert. See [[reference_nvidia_ai_integration]], [[project_vehicle_first_doctrine]].

Links: [[reference_nvidia_ai_integration]], [[project_demo_loop_ops]], [[reference_ottoq_real_edge]], [[project_vehicle_first_doctrine]].
