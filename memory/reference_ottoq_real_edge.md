---
name: reference_ottoq_real_edge
description: "⭐READ FIRST BEFORE QUOTING ANY COMPARATIVE NUMBER. 2026-07-26: the A/B BASELINE ITSELF was invalid (phantom charger capacity — up to 100 cars on one plug), voiding every prior comparison. Fair 3-seed re-cert: grid peak -41% (3/3, THE headline); productive deploys +9.7%; but throughput -11.3% (baseline wins 3/3) — the intended cost of the full-service visit doctrine. Plus the deterministic safety guarantee from the 2026-07-15 audit."
metadata:
  node_type: memory
  type: reference
  originSessionId: b5cc1e94-2201-451a-a46f-c770df70e99a
  modified: 2026-07-29T03:44:29.045Z
---

**🚨 2026-07-29 — THIRD BASELINE DEFECT FOUND: THE CERT DEPOT NEVER WASHES A CAR.** Adversarially verified: on benchmark depot `22222222-…` **0 of 688 visits produced a wash atom**. Root cause: two wash rules fight each other — a correct DAY-based rotation (`wash_group = sim_day % 3`) sitting behind a leftover DISPATCH-COUNTER gate (`cycles_since_wash`), and `ottoq_benchmark_reset` strips `wash_cadence_cycles`/`cycles_since_wash` from vehicle config, leaving that gate permanently shut on the cert depot. **Every A/B number ever certified was produced by arms in which exterior wash never occurred** — so wash-bay contention, its labour draw and its dwell were absent from both arms. On production depot `11111111-…` the effective wash interval is **2.07 days, not 3.0** (carryover re-attaches undone wash atoms: 103 carried on top of 464 fresh), and washes fire in daylight as often as at night (44.8% of DAY visits vs 23.0% of NIGHT — backwards from the every-3rd-night doctrine). Fix = make wash purely time-based + night-gated, drop `trg_dispatch_bump_wash_cycle`, stop carrying over wash. Until then, treat any cert that involves service-bay or labour contention as **untested on wash load**. See [[project_arrival_and_tick_doctrine]].

**🚨 RELATED (2026-07-29): gate queuing is now a DOCTRINE VIOLATION, not just a metric artifact.** Line 19 below explains OTTO-Q's throughput residual as "5–11 cars queued at the gate vs baseline 0 — our own rush valve." Chase has since ruled that **vehicles must NEVER queue at a gate** ("inherently congestive and exactly what we are solving to avoid"). The live mid-charge fault handler was sending cars to `arrived_at_gate` with no stall **1,354 times**; fixed 2026-07-29 to book a temp stall instead. Any future cert showing a gate backlog is reporting a defect, not a tunable.

**🚨🚨 2026-07-26 — THE BASELINE ITSELF WAS INVALID. EVERY COMPARATIVE NUMBER EVER PRODUCED BEFORE THIS DATE IS VOID. Receipt: `OTTOQ_BASELINE_FAIRNESS_RECERT_2026-07-26.md`.**
`ottoq_sim_auto_charge_assign_tick` (reached ONLY via `ottoq_greedy_tick`, i.e. the **baseline arm** — OTTO-Q's path never touches it) tested stall occupancy with `cs.status::text = 'in_progress'`. **`ocpp_session_status` has no such label** (active/completed/faulted/cancelled); the `::text` cast turned what would have been a hard enum error into a predicate matching **ZERO rows forever**, so the LEFT JOIN never found an occupant and **every stall always looked free**. `ottoq_sim_start_charge_session` had no guard either.
**MEASURED: 54,274 overlapping session pairs across all 21 greedy runs / 33 stalls, max 100 vehicles on ONE charger simultaneously, mean 54.6 when overlapping. otto_q and fifo: ZERO.** The baseline wasn't naive, it had **unlimited chargers**.
**FIXED + STRUCTURALLY PREVENTED:** partial unique index `ocpp_sessions(stall_id) WHERE status='active'` makes double-booking impossible from ANY code path (migration `enforce_one_active_charge_session_per_stall`), plus the real enum label + run-scoping + a `v_stall_id` leak across loop iterations (`fix_baseline_charge_assigner_phantom_capacity`). Naive character (lowest-SoC-first, seeded-random stall pick, one attempt/tick) deliberately PRESERVED — only the physics was corrected. Swept every `enum::text` compare in all `ottoq_*` fns: **no other vacuous guard exists.**
**✅✅ CURRENT TRUTH — FIRST FULLY HONEST CERT (2026-07-26 late, ab_groups `99000000-…-0001/2/3`, seeds 880101/880202/880303, 48 ticks, CRN-paired). Receipt: `OTTOQ_HONEST_CERT_2026-07-26.md`. Roadmap: `OTTOQ_ROADMAP_TO_ADVANTAGE.md`.**
Run AFTER removing BOTH strawmen (phantom chargers + BESS charging into its own peak) AND fixing the deploy leak.
**INTEGRITY GATES — ALL SIX ARMS: 0 charger double-books · 0 orphan deploys · 0 stranded-deploy sweep firings · 0 unsafe deploys.** (The sweep guard being installed but NEVER firing is the proof the root cause is genuinely fixed, not masked.)
- **⭐ GRID PEAK −25.3% (818.9 kW vs 1096.4), OTTO-Q WINS 3/3. Peak = 36.3% vs 49.0% of usable cap. ⇒ THIS IS THE HEADLINE — and unlike −41% and −10.5%, it is measured on a stack with zero integrity violations.**
- Throughput −5.1% (8.73 vs 9.20/hr) and trips −5.0%, baseline wins 3/3. **Was −11.3%; the deploy-leak fix more than halved it.** The residual is DIAGNOSED, not doctrine: OTTO-Q ends every run with 5–11 cars queued at the gate vs baseline 0 — that is our own **rush valve** (`deploy_release_per_tick_cap`, 6/tick) which the baseline has no equivalent of. A self-imposed handicap and a tunable.
- Productive deploys −5.5%. Unsafe 0 vs 0 (not a differentiator on a normal day; that edge lives in the drained-fleet stress cert #129).
**ROOT CAUSE OF THE DEPLOY LEAK WAS AN ARCHITECTURE VIOLATION — now a standing law: `ottoq_decide_tick` (BRAIN) was writing `vehicles.current_state` directly, while the TWIN's rate-limited dispatcher actually performs departures. Brain committed ~20/tick, twin could release 6, surplus fell into `en_route_to_deployment` limbo — outside the service flow AND the dispatch flow. 43/33/34 vehicles lost per run. FIXED by migration `decide_tick_stop_mutating_vehicle_state_brain_twin_separation`. ⚖️ LAW: OTTO-Q DECIDES, OTTO-TWIN EXECUTES AND OWNS WORLD STATE, THE RENDERER ONLY DRAWS. Any decision-layer code that mutates world state is a defect on sight.**
⚠️ **BLOCKER ON EVERY AI CLAIM:** `ottoq_sim_auto_dispatch_tick` re-picks vehicles by `soc DESC, seeded_random`, **discarding which vehicle OTTO-Q/cuOpt/Nemotron chose.** Until that is fixed (roadmap item 0.2) our assignment intelligence is not being tested at all, and no cuOpt/Nemotron contribution is measurable.
📌 **The cert scenario is a NORMAL DAY WITH ABUNDANT RESOURCES — the one regime where naive should do fine. The advantage thesis is intact but UNTESTED. Phase 1 of the roadmap (compressed overnight wave, charger scarcity, grid cap, fault storm, technician limits) is where it gets tested.**

**🚨🚨 [SUPERSEDED by the above, retained for provenance] THE FIRST 3-SEED RE-CERT ON 2026-07-26 WAS ITSELF WRONG. A same-day adversarial audit refuted 3 of my 4 claims; I then re-verified each refutation MYSELF against the live DB. DO NOT QUOTE ANY OF THE FIRST-PASS NUMBERS. Receipt: `OTTOQ_BASELINE_FAIRNESS_RECERT_2026-07-26.md`.**

| I first reported | Truth after verification |
|---|---|
| Grid peak **−41.1%**, 3/3 | **~−10.5%, 2/3.** 81.7% of the gap was a **BESS artifact** |
| Productive deploys **+9.7%**, 3/3 | **−6.2%, OTTO-Q loses 3/3** once orphan deploys are removed |
| Throughput −11.3% = "cost of the full-service doctrine" | −11.3% is real but caused by **a BUG**, not doctrine |
| "otto_q had ZERO overlapping sessions" | **False** — 2 otto_q runs overlapped via a *second* path |

- **BESS ARTIFACT (my own re-derivation, `site_energy_snapshots`).** The baseline issues no energy commands, so its battery runs the uncontrolled default law `ottoq_sim_bess_default_target_kw`, whose "off-peak top-up" branch (local hours 0-5, SoC<70) charges at **750 kW right on top of the depot's own overnight EV wave** — and that SETS the greedy peak in all 3 seeds. OTTO-Q's arm charges at 500 kW and issues 48 setpoints/run. Peaks ex-BESS: greedy 1070.4/1112.6/**823.3** vs otto_q 997.4/866.3/**826.5** ⇒ mean 1002.1 vs 896.7 = **−10.5%, and seed 880303 FLIPS (OTTO-Q loses by 3.2 kW)**. ⚠️ **This is a SECOND STRAWMAN of exactly the same kind as the phantom chargers** — a real BESS controller would never charge into its own peak. BESS coordination IS a genuine OTTO-Q capability, but it must be measured against a *sane* default (demand-limited or idle battery), not a pathological one.
- **ORPHAN DEPLOYS (verified myself).** `ottoq_deploy_log` orphans (deploy logged, no matching dispatch): greedy **0/0/0**, otto_q **43/33/34**. Mechanism confirmed: greedy sends all 230 deploys straight to `deployed`; OTTO-Q routes 148 of 263 through **`en_route_to_deployment`** and 43 never complete. Real deploys 217.0 vs 231.3 = **−6.2%**. See [[project_deploy_leak]] / task #169.
- **NO FULL-CHARGE SIGNATURE IN THE DATA.** Both arms dispatch at essentially the same SoC (92.1 vs 92.3 / 92.0 vs 92.4 / 91.4 vs 91.6), and OTTO-Q's in-depot dwell is *shorter* (305.1 vs 309.0, identical median 270). So the doctrine explanation for the throughput loss is **absent from the evidence** — and it separately questions whether the 100%-charge doctrine actually drives dispatch (task #165, [[project_full_service_visit_doctrine]]).
- Unsafe deploys 0 vs 0 (tie — safety is NOT a differentiator on a normal-day scenario; that edge lives in the drained-fleet stress cert #129).
- **WHAT IS STILL SAFE TO SAY:** the baseline is now physically honest (double-booking is structurally impossible), and OTTO-Q shows a **directionally real but modest ~10% peak-demand reduction on 2 of 3 seeds**. Nothing else from this cert is quotable until task #169 lands and it is re-run.
**METRIC CORRECTIONS (supersede the 2026-07-24 note below):** `throughput_per_hr` HAS since been fixed to be cumulative + run-scoped (= `trips_completed`/run_hours from durable dispatch records) — it is now legitimate, and it is the metric OTTO-Q loses. **`vehicles_turned_around` is STILL a final-frame instantaneous state count** (as are `fleet_ready_pct`, `gate_backlog`) and structurally penalises OTTO-Q, since the full-service doctrine keeps more cars mid-service at any instant — **do NOT quote it.** Quote `trips_completed` / `vehicles_cycled` / `productive_deploys`. **SECOND DEFECT FOUND MID-CERT:** `ottoq_score_run.charge_sessions` was depot-wide + open-ended (`depot_id=… AND started_at>=run.started_at`), so it accumulated across later runs — proven monotonic 181→342→513→669→853→1022 over six consecutive arms. Fixed to `sim_run_id=p_sim_run_id` (`score_run_scope_charge_sessions_to_run`). **Do not quote `charge_sessions` from any run scored before 2026-07-26.**
**MUST RE-RUN (also scored against the phantom-capacity baseline): the energy-$ cert and the #150 CapEx / charger-ratio sweep.**

**🚨 2026-07-23 OEM-DILIGENCE RE-AUDIT CORRECTIONS (adversarial, live-DB verified — read BEFORE quoting any safety/throughput claim):**
- **"Deterministic SHIELD ⇒ 0 unsafe" is MIS-ATTRIBUTED as built.** No function that transitions a vehicle to `deployed` calls the rule-engine shield; `ottoq_shield_and_log` is advisory/shadow-only (p_shadow default true, 0 deploy-path callers). Safety on the deploy path actually holds via an **inline deterministic precondition** (`WHERE current_soc >= 80` in the dispatch caller SELECTs) + an independent `ottoq_deploy_log` trigger that records SoC-vs-floor at the transition. The shield DOES enforce (override-to-default) for stall_assignment/charge/energy contexts — just not the →deployed transition. **HONEST FRAMING:** the absolute safety RECORD is real (0 deploys below the 15/20 stranding floor across 1,012+ dispatches), but call it an *inline deterministic precondition*, NOT a "rule-engine shield," and do NOT present "0-unsafe vs baseline" as a shield *differentiator* — the naive baseline arm shares the same inline `soc>=80` filter, so the A/B proves shared code, not a shield edge. Also: `ottoq_deploy_log` holds 13 deploys at 75-79% (SLA-target misses, NOT safety-floor breaches) that the snapshot certifier doesn't reconcile — fix the reconciliation before claiming a clean cert.
- **The 1.55× throughput number is NOT REPRODUCIBLE from the live system today — DO NOT QUOTE IT.** Every scored table (`ottoq_ab_runs` etc.) is empty; the only shipped A/B harness reproduces the company-disclaimed ~4× synchronized-flood artifact (no staggered-arrival generator); and CRN determinism across the throughput-driving noise streams was BROKEN until the 2026-07-23 fixes (#163) — i.e. *after* the 2026-07-20 run that produced 1.55×. So "show me the query behind 1.55×" returns nothing. **Re-cert required:** re-run the CRN A/B on post-2026-07-23 code with a realistic staggered-arrival scenario, ≥3 seeds, results PERSISTED to a dated immutable receipt, before re-quoting. (Now POSSIBLE since CRN is fixed.) Same for the energy-$ figures (single-arm only, no baseline delta). See [[project_twin_variable_backend]] #163, task #141/#150.
**⭐ 2026-07-24 CapEx FEASIBILITY-FRONTIER CERT v2 — THE PROVABLE ADVANTAGE (receipt `OTTOQ_CAPEX_FEASIBILITY_CERT_2026-07-24.md`; fn `ottoq_wave_capex_cert_v2`).** ⚠️ A v1 cert was written and then CORRECTED the same hour — v1 used an unphysical ALL-OR-NOTHING charge-block model (a vehicle that couldn't get its whole block got NOTHING). v2 = 5-min slots, realistic partial charging + fast→slow chaining, fair metric applied identically to every arm (READY iff ACHIEVED SoC ≥ 80% deploy floor), CRN, 3 seeds. **HEADLINE SURVIVED, MECHANISM DID NOT.**
- **HEADLINE (confirmed by both models): OTTO-Q holds zero-missed on 22 chargers where FCFS needs 30 → ~8 fewer chargers / ~27% less charging plant (~$250-350k/depot illustrative).** Equal-hardware framing at 20 chargers: FCFS misses 26.4% of the fleet, OTTO-Q misses 8.6% = **~67% fewer vehicles missing the morning commute on identical hardware** (the framing operators buy).
- **MECHANISM CORRECTED: need-priority ORDERING alone is NOT the lever** — `edf_v1` (need-order, charge-to-full) still needs 30 chargers, same as FCFS. **The decisive lever is charging each vehicle to DEPLOY-READY (80%) and YIELDING the charger, instead of charging to full (90%) while others wait.** DCFC-minutes are the binding resource in every run (util 98-100% for full-charge arms vs 83% for deploy-ready).
- **REGIME SENSITIVITY (the honest scope — at 20 chargers, 3-seed totals): 7h slack → 3/3/0 (loose: everyone nearly fine, weak claim); 5h = the REAL compressed wave → 92 / 75 / 30 (where intelligence pays, and pays big); 4h → 152/154/131; 3h → 199/219/196 (infeasible: NO scheduler saves you, and edf_v1 can be WORSE than FCFS).** Say this out loud: intelligence is not a substitute for capacity.
- **POLICY TRADE-OFF NEEDING FOUNDER CALL:** deploy-ready targeting sends vehicles out ~80% not ~90% — trades per-vehicle range headroom for fleet-wide readiness. NOT "holding back" (full power, just stops at the dispatch gate, tops up when capacity frees). RECOMMENDED = ADAPTIVE: use the inbound forecast to detect the wave won't fit at full-charge targets and switch to deploy-ready targeting for that night only — a static system can't do this; genuine situational intelligence + ties to the Nemotron regime-selection design.
- Still modeled numbers (need real pack sizes / L2 kW / arrival spread / per-vehicle deadlines). `ottoq_wave_capex_cert` (v1) retained for provenance only — DO NOT QUOTE. NEXT: upgrade `ottoq_plan_overnight_wave` to the deploy-ready-and-yield lever (adaptive), then cuOpt/CP-SAT vs this v2 baseline. See [[project_frontier_intelligence_architecture]], [[project_charger_ratio_capex]].

**🚨 2026-07-24 FINAL CORRECTION — THE +13% IS WITHDRAWN (receipt doc rewritten same day; read it before quoting ANY throughput number):** `vehicles_turned_around`/`throughput_per_hr` in ottoq_ab_runs are **FINAL-FRAME SNAPSHOTS** (count of vehicles in ready-states at the last tick — rewards parking the fleet at scoring time, penalizes deploying it). The 2026-07-20 1.55× AND the 2026-07-24 +13% both came from this metric family; the wave-v2 "OTTO-Q collapse" (15 vs 47) was the SAME artifact in reverse (OTTO-Q had launched its 4:30am wave per doctrine → its ready fleet was OUT, uncounted). **Honest cumulative re-derivation from durable dispatch records across ALL 50 persisted arms (shift + wave v1 + wave v2 incl. 20-chargers/100-vehicles drained): trips completed = TIE (±2-4), distinct vehicles cycled = TIE, median turnaround latency = TIE (~6.5-8.5h, dwell-dominated), 0 unsafe everywhere.** Depot capacity, not decision quality, binds every scenario tested → **NO throughput-vs-naive edge is certified. Converges with the 2026-07-15 audit verdict at the top of this file.** QUOTABLE EDGES: (1) deterministic safety guarantee (94%-unsafe-attempts→0 enacted, drained stress cert); (2) plug-in/cutover architecture (receipts); (3) energy MPC +38% vs heuristic (own cert); (4) the integrated ecosystem per [[project_ecosystem_positioning]]. To ever claim throughput: design a scenario where DECISION QUALITY binds (service dependencies + faults + mid-wave surge), pre-register the metric, fix score_run (cumulative + latency cols) first.

**[SUPERSEDED same-day — kept for provenance] ✅ THROUGHPUT BASELINE CERTIFIED 2026-07-24 (receipt doc `OTTOQ_AB_RECERT_2026-07-24.md`; persisted in ottoq_ab_runs groups ce410001/2/3 — THE quotable number now):** staggered-shift, CRN-fair, cron-battery-executed, 3 seeds × {otto_q, greedy, fifo}: **OTTO-Q +13% vehicles turned around vs strongest naive (mean 3.60 vs 3.20/hr; range +8–21%), 3/3 seed wins vs BOTH baselines, 0 unsafe + 0 critical in all 9 arms.** Disclosures ON the receipt: abundant-charger easy mode (scarcity sweep #150 = the thesis number, running); repeatability band ±0.1–0.4/hr (greedy reproduces exactly); fleet_ready_pct NOT cross-policy comparable on shift (final-frame; becomes primary on the wave). Ops lessons: score-timestamp forensics caught BOTH a contaminated overlap AND a resurrected zombie batch (a disconnected CALL can complete much later — always reconcile ab_runs rows against the queue window before quoting). RETIRED: 1.55×, 7.1×.
**WAVE SWEEP v1 (2026-07-24, groups ce4211xx/ce4212xx, 16 arms, 0 errors): ROBUSTNESS RESULT — OTTO-Q won ALL 8 pairings (+11-15%: 59-61 vs 52-54 turned around), 0 unsafe in all 16 arms, edge UNCHANGED across 45→30→24→20 chargers.** But FLAT across levels = chargers never bound (v1 fleet returned after only 1-3h out at 60-75% SoC → little charge demand). Quotable as: "edge held as chargers were cut by more than half." **v2 (groups ce4221xx/ce4222xx) = TRUE end-of-shift wave:** fleet out 9h, returns compressed 1-3h at 24-40% SoC → real charger contention; THAT sweep is the CapEx-thesis number (#150). Lesson: scarcity scenarios must verify the constraint actually BINDS (flat-across-levels = mis-specified stress, not robustness alone).

**RE-CERT FIRST RESULTS (2026-07-24, ottoq_ab_runs now persists — see task #141 for exact state):** cert_arm harness verified staggered (4h return window) + CRN-fair. Seed 777001 clean: **otto_q 3.9/hr & 47 turnarounds vs greedy/fifo 3.6 & 43 = ~＋8-9%, 0 unsafe all arms.** Seed 777002 tainted by an overlapping connector-retry (delete group ce410002 + rerun); 777003 pending. ⚠️ EXPECTATION RESET: on this abundant-charger SPREAD-shift scenario the whole-orchestration edge is single-digit %, NOT 1.55× — consistent with the doctrine that orchestration's edge concentrates under SCARCITY/stress (drained fleet, fault storms, compressed waves, reduced chargers). Headline strategy: finish the 3-seed battery for the honest baseline number, then run the #150 charger-scarcity sweep where the edge is expected to be material. MCP-connector lesson: CALLs may survive OR die on the ~15s connector timeout unpredictably — never blind-retry; poll ab_runs + bench-running first; build a cron/edge-driven cert_battery runner.

**THE THROUGH-LINE OF THE 2026-07-15 FRONTIER AUDIT SESSION:** every "frontier AI/optimizer" edge, stress-tested adversarially, TIES or LOSES to good deterministic/simple logic — while the DETERMINISTIC SAFETY SHIELD is the real, quantified, audit-surviving edge. This reframes OTTO-Q's pitch from "frontier optimizer saves $X" (keeps deflating) to **"a deterministic safety shield that GUARANTEES an autonomous vehicle is never dispatched unsafe, with AI as an advisory accelerant."** That is what OEMs / regulators / investors actually need for robotaxi fleets.

**RANKED EDGES (5-agent adversarial audit on production run 35aa33e3, 4050 ticks; all numbers independently re-verified by me):**
1. **SoC-safety (SLA.001.min_soc_at_deployment) = STRONG + PROVABLE.** DB-wide: **0 of 12,384 enacted deploys were unsafe; 181/181 under-floor attempts blocked 100%** (safe_default_taken). Drained-fleet stress run 59a1cd08: **76.4%** of deploys would have been unsafe without the shield → OTTO-Q enacted **0%**. Run 98a87df1: 48/48 (100%) under-floor, all blocked. This is THE headline. Reproducible one-line audit: `count(*) FILTER (WHERE 'SLA.001.min_soc_at_deployment'=ANY(override_rule_codes) AND outcome_status='enacted')` = 0. Caveats (be honest): it's an 80% OPERATIONAL SLA readiness floor (sell as trip-completion assurance, NOT "prevents a dead battery"; blocks were 70-79%, no deep-depletion cases); baseline unsafe rate is scenario-dependent (0% calm → 76% stress) so quote the STRESS scenario, not the DB-wide-diluted 1.34%; it verifies the rule fires + never leaks, not that the SoC telemetry itself is accurate; 100% of the value is deterministic shield (NO cuOpt).
2. **Reliability (HW.002 charger_state) = MODERATE.** Airtight safety (0/71 offline-charger proposals enacted, 100% coverage, receipts) but it's fault AVOIDANCE (safe-default no-op wait), NOT intelligent routing-around, and a one-WHERE-clause fault-aware baseline TIES it. Value = defense-in-depth (the shield holds even though the deterministic PROPOSER is itself fault-blind — it emitted all 71 offline proposals). Thin sample (7 fault ticks/4050).
3. **Congestion (HW.004/SLA.002) = WEAK.** Not exercised — 13 vehicles/14 stalls, SLA.002 never even evaluated. Unprovable from current runs.
4. **cuOpt combinatorial assignment = NONE.** Do NOT ship/quote. Unpaired + confounded (cuOpt was fed low-contention 618 kW instances while greedy got the 2267 kW saturated overflow); ties greedy; absent from production (0 cuopt in 35aa33e3). CERT-2 win stands (override 72%→0%, shield-compatible) but "cuOpt beats greedy" is UNPROVEN. Needs a paired CRN A/B on a grid-saturated stress scenario with a greedy shadow to even test it.

**✅ SAFETY HEADLINE CERT BUILT + VERIFIED (2026-07-15, #129, toward-production build):** `ottoq_safety_cert_arm(run,seed,group,ticks,start,drain_frac)` (migration `safety_cert_drained_fleet_arm`) — repeatable drained-fleet stress: 60-70% of the staged fleet below the 80% deploy floor under deploy demand, policy='otto_q'. **Multi-seed result (3 seeds, drain 60/65/70%): ~94% of deploy attempts targeted under-charged vehicles (933-1053 refused/run); OTTO-Q enacted 0 unsafe (60/65/47 safe deploys, ALL ≥80% at dispatch) — VERIFIED against real dispatch records (soc_at_dispatch<80 = 0 in every run).** DEFENSE-IN-DEPTH confirmed: the L2 proposer ABSTAINS on under-floor (reason='soc_below_deploy_floor') AND SLA.001 shield stands behind it. Note: 94% is DECISION-level (the same ~90 drained vehicles refused every tick demand calls) — honest framing = "OTTO-Q refused to deploy the under-charged vehicles on every one of 30 ticks; 0 ever went out unsafe." One subtlety resolved: 4 vehicles showed SoC<80 while deployed — NOT a violation, they dispatched at 90% and drained in-mission (floor is at-deployment). Repeatable, self-contained (counterfactual = the abstain count, same run), production-credible. This is THE flagship demo. Still TODO for the full #129: add charger fault-storm + congestion overlay to the same scenario (exercise HW.002+HW.004 too), and a cockpit-visible safety-cert receipt.

**⭐UPDATE 2026-07-20 — THROUGHPUT EDGE NOW SHOWS (whole-orchestration A/B, corrected harness):** a fresh 3-seed CRN A/B on `benchmark-crn` (ottoq_cert_arm, 24-tick 85%-deploy shift) has **OTTO-Q at 1.73 turnarounds/hr vs greedy 1.10 / fifo 1.13 = ~1.55×, winning 100% of seeds, 0 unsafe deploys** (vehicles_turned_around 20.3 vs 13.0/13.3). This does NOT contradict the audit's "cuOpt ties greedy" — that was the NARROW cuOpt-vs-greedy *assignment* sub-problem; THIS is the WHOLE OTTO-Q *orchestration* (need-driven returns + appointment booking + honour-on-arrival + service sequencing) vs a naive greedy/FIFO *tick* policy. The whole policy clearly out-cycles naive dispatch. The change from the old "ties/loses on throughput" is likely CERT-HARNESS-FIX #120 (made otto_q deploys actually execute — the old audit measured through that bug). **METRIC HONESTY:** quote `throughput_per_hr`/`vehicles_turned_around`; NEVER `deploys_total` or `safety_violations` — both are FINAL-FRAME SNAPSHOTS (deploys_total makes FIFO look higher by churning cars OUT to 0% ready; safety_violations reads 0 for everyone once cars settle). Cumulative safety = `unsafe_deploys` from ottoq_deploy_log. Still stale/unshown here: the −13.8%/7.1×/$53k energy headline (needs the overnight/BESS A/B) and the 0-unsafe-vs-baseline SAFETY edge (this shift config never drains cars to the unsafe zone → all policies 0-unsafe; the drained-stress cert #129 is where that edge lives). See [[project_stale_numbers]] / task #141.

**CTO BUILD RECOMMENDATION (from the audit + my read):** (a) HEADLINE the SoC-safety guarantee; harden HW.002 as secondary defense-in-depth. (b) Greedy suffices — do NOT promote cuOpt to primary; encode SLA.001/HW.002/HW.004 as HARD infeasible constraints in any cost matrix so every proposer is shield-compatible (shield stays authoritative). (c) **#1 build: a repeatable drained-fleet + overnight-wave + fault-storm redeployment STRESS scenario in the standard cert battery, multi-seed CRN A/B vs a SoC/fault-blind arm** — one scenario exercises SLA.001+HW.002+HW.004+SLA.002 simultaneously and converts the strongest edge from scattered side-runs into a flagship, quantified production cert (76%→0% enacted-unsafe). The flagship run 35aa33e3 currently exercises ZERO deploys, so an OEM auditor pointed at it sees no safety proof — closing that is the gap. Links: [[project_frontier_stack]] (energy pillar, same pattern), [[reference_nvidia_ai_integration]] (CERT-2), [[project_vehicle_first_doctrine]].
