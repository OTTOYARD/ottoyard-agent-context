---
name: project_architecture_separation
description: "The TRUE OTTOYARD architecture (live Supabase inventory 2026-07-13) + the 4-product isolation plan Chase greenlit; supersedes loose 'OTTO-Q lives on gxdrc' notes"
metadata: 
  node_type: memory
  type: project
  originSessionId: b5cc1e94-2201-451a-a46f-c770df70e99a
---

**2026-07-13 — External audit (Chase's OTTOYARD-Codebase-Audit-Prompt) triggered a full architecture reckoning. Canonical doc: `~/Desktop/OTTO-Q V1/OTTOYARD_ARCHITECTURE_TRUTH_AND_SEPARATION_PLAN.md`. Live-verified via Supabase MCP (org "OTTOYARD" gdqqjpxbgpperhgyfkes).**

**TRUE STATE (live, not disk):**
- **`otto-q-core` (gxdrc...) ACTIVE** = THE FUSED CORE. ~130 tables, ~950k events, 288k decisions, 392k rule-evals, 52 shield rules, full ML/audit stack, ALL the twin data (telemetry/OCPP/weather/solar/grid), + OTTOW. Live cron: `ottoq_cron_tick()` /2min (orchestration), `ottoq_demo_metronome(50)` /1min (twin advance), retention+ingest. ~25 edge fns (otto-q-api gateway, otto-twin-control, ottoq-orchestrate-tick, ottoq-orchestrator-agent, ottoq-cuopt-propose, ottoq-nemotron-copilot, energy/assign/sequence-optimize, amend, progress, feed-agents…). **OTTO-Q brain AND OTTO-Twin are FUSED here — this is the #1 thing to separate.** 28 tables RLS-off (security deferred to pre-pitch).
- **`OTTOYARD MVP` (ycsis...) ACTIVE = OrchestraAV.** Separate older fleet SaaS: own simpler `ottoq_cities/depots/resources/vehicles/jobs/schedules` (NAME-COLLIDES with core but is a different, simpler system), Stripe billing, `intelligence_events` (1.35M) news/threat scanners, `ottoq_ps_*` membership, its OWN mock `ottoq-simulator`. Chase: this was fake-sim scaffolding for the OrchestraAV UI from BEFORE the twin/real OTTO-Q. Plan: rewire to CONSUME OTTO-Q via API; salvage only anything genuinely frontier/recent-billing into the real OTTO-Q, else ignore.
- **`OTTOYARD Fleet Dashboard` (sovyx...) INACTIVE** — abandoned; candidate OTTO-Pulse or archive.
- **`hfjaofyfxsyniohdfacg` DOES NOT EXIST** in the org — the twin repo's config/.env points at a dead project id; twin actually deploys to gxdrc.

**⚠️ PIVOTAL REPO FINDING (worse than the audit caught):** the GitHub repo **`OTTOYARD/ottoyard-OTTO-Q` is actually OrchestraAV** (its `supabase/config.toml` project_id = `ycsisvozzgmisboumfqc`, 35 ycsis fns incl Stripe+intelligence, 0 brain tables). **The REAL OTTO-Q brain (gxdrc) has NO proper repo** — only `otto-q-api` source sits loose in the non-git `~/Desktop/OTTO-Q V1` folder (which is Chase's Lovable-PROMPTS workspace, not code); the other ~24 fns + all 130-table schema + decide/shield/cron logic live ONLY in the live DB. **Crown-jewel IP was un-versioned.** Repos found: OTTO-Twin=`ottoyarddepot-sim`, OTTO-Pulse=`ottoyard-field-ops`, "OTTO-Q"(mislabeled)=`ottoyard-OTTO-Q`(=OrchestraAV, PUBLIC). Real OTTO-Q/gxdrc repo = UNKNOWN (ask Chase which Lovable project edits otto-q-core). GitHub token in osxkeychain (x-access-token) works for the 4 repos but can't enumerate the org (fine-grained scope).

**DECISIONS (Chase, 2026-07-13):** (1) Split approach = **schema-split first** (clean isolated schemas + API seams inside gxdrc now; separate physical Supabase projects later once proven — keep live demo running). (2) OrchestraAV = **rewire to consume OTTO-Q**. (3) Sequencing = **core engines first** (separate+version OTTO-Twin & OTTO-Q, wire ALL NVIDIA + prove it fires, THEN the two UIs). Target = 4 isolated products each own schema+repo, API between: OTTO-TWIN, OTTO-Q, OrchestraAV, OTTO-Pulse.

**PLAN:** P0 inventory DONE. **P1 = protect IP: ✅ DONE 2026-07-13** — faithful read-only export of live gxdrc (279 DB functions incl decide_tick/shield/cron/metronome, 149 tables+220 FKs+40 views, all 22 edge fns, 145 policies, 164 migrations) at `~/Desktop/otto-q-core-export`, committed + pushed to **private repo `OTTOYARD/otto-q-core-snapshot`** (commit 9be089e). This is now the CANONICAL OTTO-Q backend repo — Chase confirmed he NEVER edited OTTO-Q via Lovable (it was built as backend code via Supabase/Claude Code), so no Lovable divergence; I own it via git + Supabase MCP going forward. GitHub token = scoped integration token (can push to existing repos, CANNOT create org repos — Chase must create empty repos OR grant the app all-repos access for me to create the other product repos). **P2 = separation blueprint DONE** (workflow wf_8995403e → `~/Desktop/OTTO-Q V1/OTTOQ_TWIN_SEPARATION_BLUEPRINT.md`). HEADLINE: LOOSE coupling, not fused — 149 tbl = 49 twin / 71 otto_q / 13 shared_physical / 6 ottow / 10 infra; only **12 of 220 FKs cross twin↔otto_q** (concentrate on 4 twin tables: vehicle_schedules, schedule_tasks, waves, ottoq_visit_needs); the seam is ~24 fns in 3 shapes (READ=ottoq_build_decision_frame `Twin.getState`; DECIDE=ottoq_decide_tick+energy_orchestrate; COMMAND→EXECUTE=brain writes ottoq_energy_commands/ottoq_vehicle_commands, twin consumes). Production loop (ottoq_cron_tick) ALREADY runs world-advance in-DB then brain over HTTP; only the demo loop (ottoq_sim_advance_tick) was fused.
**P3 DE-FUSE (demo loop) ✅ DONE + LIVE-VERIFIED 2026-07-13** (Chase green-lit "de-fuse on live"; migrations in `otto-q-core-snapshot` commit 3a0cd28 + DB migration history). Split `ottoq_sim_advance_tick` → `ottoq_sim_advance_tick_world` (twin physics) + `ottoq_sim_decide_and_dispatch` (cuOpt+L2+decider+auto-dispatch); kept `ottoq_sim_advance_tick` as a thin single-txn WRAPPER so all 6 callers (metronome, 2 MPC lookaheads, run-to-completion, benchmark, otto-twin-control edge fn) are byte-identical; flipped ONLY `ottoq_demo_metronome` to world→COMMIT→decide→COMMIT. Verified: 34-tick soak (test run 79566b0a, normal_day) = 3060 decisions, 0 metronome failures in postgres logs, fused 11.7s single-txn → 2 short txns (less lock contention). ⚠️ RESIDUAL: `ottoq_energy_orchestrate` (1 brain call) stays coupled to `ottoq_sim_energy_controller` inside world-advance (tight signal→execute pair, CERTIFIED energy numbers) — its de-fuse is a separate step needing energy A/B re-cert. **P3 SCHEMA ISOLATION — done the SAFE way (Chase chose "proceed safe" 2026-07-13):** KEY FINDING — a physical in-place schema move on the live otto-q-core is NOT low-risk: 79 of 149 tables are REST/RPC-exposed (edge fns + otto-q-api 9.4k-line gateway + cockpits, ZERO `.schema()` usage), so moving them = full data-access-layer rewrite + PGRST_DB_SCHEMAS change, AND it's throwaway (redone at the separate-projects split). DECISION: deliver isolation LOGICALLY now + do physical relocation as a clean migration at the separate-projects arc, NOT in-place surgery on the live demo. DELIVERED: (1) `OTTOQ_TABLE_OWNERSHIP_MANIFEST.md` — authoritative 149-table→domain+target-schema+REST-flag map (70 internal-only / 79 REST-exposed). (2) **Formal API seam v1.0 BUILT+LIVE-VERIFIED** (migration seam_01/seam_01b, commit 38d1122, doc `OTTO-Q V1/OTTOQ_TWIN_API_CONTRACT.md`): 3 versioned facade fns `ottoq_api_twin_get_state`(READ→{bess,energy,stalls,sessions,vehicles})/`ottoq_api_otto_q_decide`(DECIDE→writes vehicle+energy commands)/`ottoq_api_twin_apply_commands`(EXECUTE); tested live = get_state tick8 ✓, decide 23 built/19 enacted/19 cmds queued ✓, apply clean ✓. ⚠️ search_path detail caught: facades need `public, extensions` (pgcrypto digest() in nested decision-snapshot hash) — same class that governs any future physical move. Additive, no behavior change. (3) FK decision: cross-schema FKs stay in one DB; break to id-refs only at project-split. **Physical separation = deferred arc (task SI-NEXT #111): stand up separate OTTO-Twin + OTTO-Q Supabase projects, migrate per manifest, wire over the contract — needs Chase cost approval.** NEXT recommended = P5 wire ALL NVIDIA + prove it fires (Chase's stated sequence, investor-facing), then P6 depot motion. All OTTO-Q changes now = versioned migrations in `otto-q-core-snapshot` git + DB migration history. P3 separate twin⟂otto-q schemas + build Twin→OTTO-Q world-state feed + OTTO-Q→Twin decision read-back. P4 isolate UIs (rewire OrchestraAV, stand up Pulse, retire orphans). P5 wire ALL NVIDIA end-to-end + receipts. P6 back to depot motion/visual ([[project_depot_motion_timing_realism]]).

**Chase (verbatim intent):** frustrated — "promised frontier build and this is the result... confused code bases, loss of time... rebuild architecture and foundation of our IP... make sure we NEVER miss details and issues like this." I OWN not surfacing this earlier. See [[feedback_gap_ownership]]. Supabase MCP + GitHub access confirmed — Chase wants me to build/label the isolation directly. Confirm cost before creating new Supabase projects. Security (RLS, public OTTO-Q repo, Mapbox token) = separate track, deferred to pre-pitch per Chase. Relabel/supersedes the casual "OTTO-Q core = gxdrc" framing in [[project_ottoq_logic_completeness]] and others — gxdrc is OTTO-Q **+ Twin fused**.
