---
name: project_cards_integration_map
description: "OTTO-Q vehicle cards build (task #98, Chase-approved 2026-07-16: BOTH cockpits one drop, minimal+expand): verified integration map of both cockpit repos (REAL paths under ~/Desktop/OTTOYARD/), the card RPC design, and honesty rules (prune skipped legs, omit corrupt deviation_s, atoms key='svc')."
metadata:
  node_type: memory
  type: project
  originSessionId: b5cc1e94-2201-451a-a46f-c770df70e99a
---

**Scope (Chase 2026-07-16):** per-vehicle OTTO-Q "work-order cards" in BOTH cockpits, one drop. Card at rest = owner badge (Pulse only) + current step + progress bar + next step; click-to-expand full sequence (done ✓ / current ▶ / upcoming, with times). Streamlined, not bulky. Pulse = ALL owners (depot ops eyes); Orchestra = ONLY the logged-in fleet owner's vehicles, inside each vehicle's Fleet-tab card.

**Backend (gxdrc):** new RPCs `ottoq_depot_cards(p_depot_id, p_fleet_operator_id DEFAULT NULL)` (one call feeds a whole cockpit; operator filter = Orchestra's scoping) + `ottoq_vehicle_card(p_vehicle_id)`. Draft migration in session scratchpad `ottoq_cards_migration_draft.sql`. HONESTY RULES baked in: prune `skipped` legs (55% are speculative never-ran blocks), scope to current visit (newest itinerary this run; last 3 done + active + next 5 planned — no 235-leg balloons), OMIT `deviation_s` (corrupt baseline, B2.2 never landed — show planned+actual times instead), atoms use key **'svc'** (contract doc says 'atom' — doc is stale). Card data itself is LIVE (needs/itineraries/legs update every tick); only the readout was missing. `ottoq-progress` edge fn is orphaned on dead `vehicle_schedules`/`schedule_tasks` (stale since 2026-06-19) — retire/repoint.

**Pulse (repo `/Users/chaseballenger/Desktop/OTTOYARD/ottoyard-field-ops`** — NOT plain Desktop; branch `claude/prime-ops-panel` has the newest patterns, 1 commit unmerged): Vite+React+shadcn+TanStack. Client to use = `src/lib/supabase.ts` (hardcoded gxdrc) — NEVER `src/integrations/supabase/client.ts` (points at dead odhpb project). Mount = new sub-tab in `src/components/tabs/OttoQTab.tsx` SUB_TABS; copy hook pattern `use-ottoq-frontier.ts` (rpc + refetchInterval ~10s; polling only, no realtime). House card style = hand-rolled `div.rounded-lg.border.bg-card`, status-chip + timeline-dot patterns in `VehiclesTab.tsx`. DEPOT_ID hardcoded `11111111-...` in 6 files (accepted convention). Honest empty states (PrimeOps pattern), no mock fallback.

**Orchestra (repo `/Users/chaseballenger/Desktop/OTTOYARD/ottoyard-OTTO-Q`** = OrchestraAV, single squashed commit on main): Fleet tab is ALREADY on gxdrc — `ottoqInvoke("ottoq-fleet-vehicles")` returns real otto-q-core vehicle UUIDs (no ID mapping needed) and the edge fn ALREADY accepts a `fleet_operator_id` filter. gxdrc access via existing `src/lib/otto-q-api.ts` helpers (`ottoqRpc` = PostgREST rpc — exactly what cards need; no new client). Vehicle card = inline JSX `src/components/OTTOQFleetView.tsx:627-774`, slot the sequence section ~line 724; sequence-render precedent = `VisitReportCard.tsx`. Auth = ycsis project (legacy); `useOTTOQRealtime` subscribes to ycsis tables = dead for gxdrc views (poll instead). Live fleet_operators on gxdrc: Waymo Nashville (88 veh), Tesla Robotaxi TN (65), Zoox Southeast (63), Local EV Fleet Co (0). **Gap:** user→operator binding is client-side only (anon key, no JWT) — spoofable; fine for demo, real tenancy = OQ-7 security gate. Links: [[project_demo_loop_ops]], [[project_ottoq_logic_completeness]], [[project_itinerary_contract_a2]].

**OUTCOME 2026-07-16 — SHIPPED, awaiting Chase merges:** backend RPCs LIVE on gxdrc + verified over the cockpit REST path (3×200, ~290KB/116 vehicles; operator-scoped call = 46/46 Waymo-only). ⚠️ the Supabase MCP migration runner MIS-PARSES $$-quoted function bodies ("syntax error at or near FROM") — create functions via raw execute_sql with $fn$ quoting, then apply_migration for the ledger. Pulse PR branch `claude/ottoq-vehicle-cards` (base = `claude/prime-ops-panel`, merge that first/together); Orchestra PR branch `claude/ottoq-sequence-cards`. GitHub REJECTS pushes authored as chase@ottoyard.com (email-privacy) — both repos now commit as the noreply identity.
