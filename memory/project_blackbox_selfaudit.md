---
name: project_blackbox_selfaudit
description: "OTTO-Q Black Box (2026-07-16, Chase-directed): a founder-side FLIGHT RECORDER — each sim run records the exact code + all raw data that executed; on Stop it freezes into a downloadable bundle; only the latest run is kept; Chase drops it into Claude Code and CLAUDE does the forensic audit (NOT in-product AI). Backend BUILT+VERIFIED; frontend panel is the remaining spec."
metadata:
  node_type: memory
  type: project
  originSessionId: b5cc1e94-2201-451a-a46f-c770df70e99a
---

**Chase's reframe of "self-learning" (2026-07-16):** NOT a customer-facing continuous-improvement feature — the orchestration should already be optimal in its final form. The improvement loop is on the FOUNDER/build side: the software records every production run (exact code + all data), and Chase downloads that log + drops it into Claude Code where **Claude finds the holes/errors and proposes/builds fixes** he PRs+merges. No in-product AI analysis. "Silicon Valley ready." This is the automation of exactly the manual gap-hunting Claude did all session (wash-lane bottleneck, two deadlocks, scope bug were all found by querying run data).

**BACKEND BUILT + VERIFIED (migrations: `blackbox_run_export_engine`, `blackbox_stop_reset_and_purge`; + search_path fix to `public, extensions`):**
- `ottoq_run_blackbox(run) → jsonb` — the forensic bundle: run metadata + `executed_code` = pg_get_functiondef of ~100 decision-path fns (regex on proname) + `data` = every run-scoped table (34 tables, raw, LIMIT 200k) + vehicles/stalls/ocpp_sessions + edge-fn AI source references (cuOpt/Nemotron code lives in the edge-fn store, noted by name). VERIFIED: 100 fns + 34 tables, ~24MB for a bounded run.
- `ottoq_sim_stop_and_reset(run, reason)` — the real STOP (old `ottoq_sim_end_run` only flipped a status flag + left depot state lingering — confirmed gap). Marks run `completed`, cancels active sessions, frees stalls, closes dispatches, resets the PERSISTENT fleet to empty/idle (`offline`, unplaced — fleet is persistent, 0 owning_sim_run_id, seed_fleet RESETS not creates), and PRESERVES the historical log so the Black Box stays downloadable. VERIFIED: 116 offline / 0 stalls / 0 sessions, log intact (3909 decisions), blackbox works. NOTE: status enum = aborted/completed/paused/running (no 'stopped' — use 'completed').
- `ottoq_purge_prior_runs(keep_run)` — bounded storage: deletes every non-production, non-running run's data EXCEPT the kept one, across all sim_run_id tables + the run rows. WIRED into `ottoq_start_demo_run` (Play purges prior). VERIFIED: cleared **1.4M rows / 373 stale runs** (huge reclamation — also permanently cures the [[project_demo_loop_ops]] DB-bloat incident); production_live runs PROTECTED (4 survived). After a big purge the tables need vacuum (autovacuum handles it; a full count(*) timed out immediately after).

**DOWNLOAD EDGE FN DONE + VERIFIED (v2):** `ottoq-run-blackbox?run=<uuid>` (verify_jwt=false, apikey/anon ok; defaults to latest operator_demo run if no ?run). v1 (one giant 25MB RPC) hit the API gateway's ~22s ceiling → 500. v2 fix: calls the light `ottoq_run_blackbox_meta(run)` header RPC (325KB: run + 102 fn sources + data-table list), then fetches EACH run-scoped table paginated (1000/page, beats PostgREST's 1000-row cap) + vehicles/stalls/ocpp_sessions, assembles + streams as a `.json` attachment. VERIFIED: HTTP 200, 21.8MB in 12s, 102 fns + 34 tables, full rowcounts intact. Also added `set_config('statement_timeout',...)` to the monolithic `ottoq_run_blackbox` and VACUUMed the heavy tables post-purge (dead tuples were slowing scans). URL pattern: `https://gxdrcyphqjzjsuhxuqtg.supabase.co/functions/v1/ottoq-run-blackbox?run=<uuid>`.

**FRONTEND PANEL DONE + PUSHED (awaiting Chase merge):** repo REAL path `/Users/chaseballenger/Desktop/OTTOYARD/ottoyarddepot-sim` (nested under OTTOYARD/). Branch `claude/blackbox-panel` (commit 936b799), PR-create: github.com/OTTOYARD/ottoyarddepot-sim/pull/new/claude/blackbox-panel. tsc 0 + vite build 0. KEY REPO FACT: the cockpit's generated client (`src/integrations/supabase/client.ts`) points at a DIFFERENT project `hfjaofyfxsyniohdfacg` (the app's own .env backend) — the gxdrc/OTTO-Q backend is reached ONLY via `src/lib/ottoTwin.ts` (raw fetch to `otto-twin-control`, no rpc). So the agent added a gxdrc-scoped supabase-js client `src/lib/ottoQClient.ts` (uses OTTOQ_SUPABASE_URL+OTTOQ_ANON_KEY exported from ottoTwin.ts — the anon/role=anon key, RLS-gated, safe in bundle; SECURITY-VERIFIED not service_role). Lifecycle centralized in `src/lib/blackbox.ts` (startDemoRun/stopAndReset/downloadBlackbox = fetch→blob→`<a download>`); hook `use-blackbox.ts` (idle/recording/stopped phase machine, polls 3.5s); `BlackBoxPanel.tsx` + new `blackbox` tab in TabBar/SidePanel/simulationStore. OperatorConsole's start/stop REWIRED through blackbox.ts = ONE authoritative Play/Stop path (Play=start_demo_run purges+seeds, Stop=stop_and_reset empties + adopts/clears twinStore.activeSimRunId). Chose EMPTY/idle reset. FEATURE COMPLETE end-to-end (backend verified + panel pushed). Links: [[project_demo_loop_ops]], [[project_ai_layer_production]], [[project_cards_integration_map]].

**POST-MERGE FIX ROUND (2026-07-17) — Chase reported Play/Stop broken + panel not findable. All 4 root causes found + fixed + browser-verified end-to-end:**
1. **Play 500'd (THE big one):** anon role `statement_timeout=3s`, but `ottoq_start_demo_run` = seed 3.8s + purge 3.0s = ~6.8s → every anon Start timed out (HTTP 500), so no run was created and the transport stuck on a disabled Play. Service-role MCP masked it (20s). FIX: `ALTER ROLE anon SET statement_timeout='15s'` + `NOTIFY pgrst,'reload config'` (Supabase-supported; service_role already 20s; private-link demo backend). Anon Start now 200 in ~6.7s, Stop 200 in ~3s (both curl-verified on the real REST path).
2. **"Stop doesn't fully stop" (accumulation):** `ottoq_purge_prior_runs` spares `status='running'` rows but never aborts them → pressing Start again left a zombie the metronome kept ticking. FIX (migration `blackbox_start_single_live_run_guarantee`): start_demo_run now aborts ALL prior running/paused non-production runs before seeding → single-live-operator-run invariant.
3. **Panel stuck on "idle" / no Download after remount:** anon SELECT on `ottoq_sim_runs` is RLS-blocked (returns `[]` + 200), so `fetchLatestRun` always saw null; the panel only showed a run via optimistic state in the SAME mount (lost on tab-switch/resize/reload or when started from the Operator Console). FIX (migration `blackbox_latest_run_discovery_rpc`): narrow SECDEF RPC `ottoq_blackbox_latest_run()` returns the latest operator_demo run summary; `blackbox.fetchLatestRun` calls it via `ottoQ.rpc`. No broad table exposure.
4. **Frontend UX (branch `claude/blackbox-transport-fixes`, commit e9495bd, pushed — PR: github.com/OTTOYARD/ottoyarddepot-sim/pull/new/claude/blackbox-transport-fixes):** TabBar `flex`→`flex flex-wrap` (9 tabs overflowed the fixed 420px `overflow-hidden` panel → rightmost tabs incl. Black Box were clipped off-screen = "can't find it"); Black Box tab gets a red dot. OperatorConsole transport CONSOLIDATED to one state-driven control (no-run→Start; running→Pause+Stop; paused→Resume+Stop) — kills the stray always-visible Resume button; Stop now `setActiveTab('blackbox')` so Download is right there. useTwinControl play/pause/setSpeed read `activeSimRunId` fresh from the store (was a stale null closure at Start-click → twin.resume/setTimeScale never fired). BROWSER-VERIFIED at :8080: Start→depot fills+RUNNING, Black Box→RECORDING(live ticks), Stop→depot empty + "RUN RECORDED — DEPOT RESET" + Download; 0 console errors, tsc+build clean. **Where the Black Box tab is: side panel (right), tab bar row 2, rightmost = "● BLACK BOX".** Chase merges the branch → Lovable republishes.

**NEXT (Chase-requested, after this merge):** depot TRAVEL LANES / right-of-way so vehicles taxi in their own lane without head-on conflicts, with paths/rails mounted to the lanes (ties to RAILS P2 #97). See [[project_depot_motion_timing_realism]].

**FLIGHT-RECORDER AUDIT #1 — "zero vehicles charging" (2026-07-17, run 6ae43f2c).** Chase downloaded a Black Box bundle and asked why he saw zero charging — the FIRST real use of the founder-audit loop, and it WORKED. Verdict: **charging was real; the display lied.** Backend truth (from the bundle + live DB): OTTO-Q enacted 53 charge-stall assignments (11 DCFC + 42 L2, milp_local, 0 shield overrides), 43 OCPP sessions (16 completed to 90% SoC, 26 cancelled at the tick-6 operator stop, 1 faulted, ~454 kWh); `ottoq_sim_start_charge_session` correctly flips vehicles to `charging_dcfc`/`charging_l2`; concurrency rose 0→7→19→26 mid-run. Ran a 9-agent verified Workflow (charge-visibility-audit) — it REFUTED my renderer-name-mismatch hypothesis (TwinMotionDriver.mapState already maps `charging_dcfc|l2`→`'charging'`; VehicleDot/Vehicle3D depict it) and the "too fast to see" theory. **Real bugs found + FIXED (migrations `twin_snapshot_scope_fleet_to_run_depot`, `stop_reset_backfill_run_counters`):** (1) `ottoq_twin_snapshot` fleet.counts/total/vehicles had NO depot filter → cockpit + renderer counted BOTH depots (Nashville 116 + depot-2 `2222...` 100 = **216 phantom cars**); scoped to `home_depot_id = v_depot` → verified 116. This is the [[project_architecture_separation]] two-depot pollution biting the live cockpit. (2) counters.charge_sessions was scoped by `depot_id + started_at` not `sim_run_id` → over-counted **4484 vs true 43**; fixed to sim_run_id → 43. (3) run-row counters (`charge_sessions`,`events_generated`,`tasks_completed`,`vehicles_simulated`) were DEAD (never written) → the bundle's run metadata showed `charge_sessions:0` (the literal thing that misled the audit); now BACKFILLED at `ottoq_sim_stop_and_reset` from run-scoped tables (run 6ae43f2c re-backfilled: 43/203/17/94). (4) frontend `TopBar.tsx` rendered literal `0` for a missing/errored fleet frame → now shows `—` (branch `claude/blackbox-transport-fixes`, pushed). Most-likely reason Chase literally saw zero = the deploy-heavy opening (charging ramps only in the back half of a 3-min run) COMPOUNDED by 100 phantom depot-2 cars cluttering the scene; can't fully reconstruct the exact pixels post-hoc (a dropped feed frame leaves no trace) — hence the `—` honesty fix. **OTTO-Q's charging DECISION logic is healthy; this was entirely a snapshot/counter/display-seam problem.**
