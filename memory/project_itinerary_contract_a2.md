---
name: project_itinerary_contract_a2
description: "A2 itinerary contract — twin-owned per-vehicle timed legs; B1 shipped, B1.5-B4 remaining; OTTO-Q reactive layer rides on leg deviations"
metadata: 
  node_type: memory
  type: project
  originSessionId: b5cc1e94-2201-451a-a46f-c770df70e99a
---

**A2 ITINERARY CONTRACT (started 2026-07-08, Chase greenlit + demanded OTTO-Q be REACTIVE to timing on an agentic layer).** Canonical docs: `~/Desktop/OTTO-Q V1/OTTOQ_ITINERARY_CONTRACT.md` (design + internals map + status) and `OTTOQ_TIMING_CALIBRATION.md` (58 sourced real-world timing claims: charge curves, L2, servicing, yard movement, ETA variance — READ BOTH FIRST).

**Core design:** one per-vehicle timeline of LEGS (taxi/charge/wash/settle/stage/depart) in `ottoq_vehicle_itineraries` + `ottoq_itinerary_legs` — planned_start/end on the sim clock + `duration_basis` jsonb (physics grounding) + actual_start/end + `deviation_s`. Legs = SUB-TICK truth (the 30-min tick state machine stays coarse truth); renderers play legs on the continuous sim clock (also the A4 Isaac lockstep mechanism). Reactive pass (B4) = deviation threshold (FC-0 knob) → existing amendment handler (OQ-11) + cuOpt re-propose; the twin's smart-charging throttle means OTTO-Q's OWN energy decisions extend charge legs → it must re-sequence downstream — the flagship reactive demo.

**B1 SHIPPED** (migration `itinerary_contract_b1_and_janitor_v2`): tables + `ottoq_estimate_charge_minutes(start_soc, target, charger_kw, vehicle_kw, pack_kwh, temp, soh, rate_mult)` which integrates the LIVE `ottoq_sim_compute_charge_rate` physics in 1% steps — plan and world share one formula, so deviations only come from variance/faults/throttle.

**KEY REALISM GAP (B1.5, measured):** sim charge curve holds full power to 78% SoC → Model Y-class 20→80% = 11 min vs 25–30 measured (2.5× too fast); I-Pace 29.8 vs ~40; cold derate too mild (research: ×0.65 power at 0–10 °C, INL); L2 missing ~88% AC efficiency. Fix = platform taper shapes (Tesla ~193 kW @20% → ~42 kW @80% near-linear; I-Pace flat 104 kW) — **but it changes demand peaks → must ride with the energy A/B re-certification, never silently.** Weather: current 2.4× ETA multiplier only matches worst heavy snow; FHWA says rain +5–25%.

**Internals map (verified):** service durations ALREADY have sub-tick end timestamps (`vehicles.config->>'service_ends_at'`, wash 8/detail 25/maintenance 40 min × knob × `ottoq_twin_deal` card) in `ottoq_sim_advance_service_flow`; charge sessions = `ocpp_sessions` + `ottoq_sim_advance_charge_sessions` (no predicted end); arrivals = `ottoq_vehicle_dispatches.scheduled_return_at/arrival_jitter_min/soc_at_return_pct` (ETA skeleton, no rolling updates). Pilot-parallel ETA feed target: 10–60 s refresh, ~11–19% MAPE shrinking with distance (OEM telemetry cadence per Tesla Fleet API docs).

**B2 SHIPPED (2026-07-08 late):** `ottoq_itin_leg_open/close` helpers; charge legs (start/stop_charge_session) with physics-planned ends + deviation; wash/detail/service legs (service_flow) opened with exact `service_ends_at`, closed at the TIMER's end not the observing tick (B2.1 — deviations = real variance only). LIVE-verified run 4d6f8bf2: 50+ legs, sane durations (L2 ~95min, wash ~8, service ~41). **FINDING: deviation_s exposed the twin's 30-min tick quantization** — fixed for services; charge legs need B2.2 (interpolate SoC-crossing in advance_charge_sessions). Variable feed-agent AUDIT complete same night → **`~/Desktop/OTTO-Q V1/OTTOQ_VARIABLE_FEED_DOSSIER.md`** (the canonical agent-per-variable design doc; READ IT before touching any twin variable). Headline findings: (1) **two RNG regimes** — card engine is run-seeded CRN but MANY vars draw on depot+clock/fixed salts (7/42/17) = identical across runs, breaking A/B; (2) **"fitted but unwired" is the dominant defect** — charger_reliability (40k rows, 4 grids), ACN, NREL idle_fraction, TLC dow_multiplier all have ZERO runtime consumers while the sim runs on adjacent literals; (3) **silent uniform fallback** in ottoq_twin_deal (cloud cover = uniform(0,100)!, all service durations uniform ±30%); (4) **LMP (electricity price) is fully synthetic** yet gates orchestrator decisions; (5) **the $120k/yr headline rests on a DEFAULT 10 function arg** + 6 hand-typed tariff rows → #1 ranked agent = tariff feed from OpenEI URDB; (6) **repair time constant 50 sim-min vs fitted 7.3-DAY mean** (~200× gap, kills reliability-routing visibility); (7) **service manifest frozen per vehicle per run** (constant seed, never cleared — correctness bug); (8) NO seasonality (Dec 21 @ 24.6°C). Top-10 ranked agents + uniform agent contract (plan-not-draw, CRN-only entropy, explicit fallback, provenance on every card, validation loops) all in the dossier. eta_delay card = reference implementation.

**Remaining:** B2.2 charge sub-tick close + taxi/stage/depart legs from decide_tick → B1.5 curves+re-cert → B3 snapshot+renderer playback → B4 reactive (deviation monitor + rolling ETA + amendments; threshold > residual charge quantization until B2.2). Related: [[project_depot_motion_timing_realism]], [[project_energy_e4_charging_fix]], [[reference_nvidia_ai_integration]].

**DB SURGERY 2026-07-08 (Chase-authorized, DONE): otto-q-core 11GB → 1.9GB.** Events table-SWAP (new table with 48h keep-set 481k rows, 5.7GB→722MB; 12 FKs re-added NOT VALID incl. self-FK; sequence re-owned; 2 triggers + 3 RLS policies + grants + 3 views recreated; fns `ottoq_entity_history`/`ottoq_causation_chain` had rowtype deps → rebound) + VACUUM FULL rule_evaluations (1.5GB→208MB) + incident_reports (885MB→140MB) + dropped orphan scratch `_events_keep`/`_rules_keep` (1.1GB). Ticks: 11s+timeouts → ~7s; remaining time is decide_tick LOGIC (algorithmic, not IO) — future optimization target. **HARD LESSONS:** (1) this small instance (60 conn, 6 workers) cannot absorb multi-million-row DML in one statement — delete-in-place saturated it for 17 min, Chase had to dashboard-restart; keep-set table-swap is THE pattern; (2) pg_cron context has ~2-min statement cap + jobs launching at second :00 often fail "job startup timeout" (delayed retries succeed); (3) VACUUM/VACUUM FULL must be a SINGLE statement per execute_sql call (multi-statement = implicit txn) — lane: `ALTER ROLE service_role SET statement_timeout='600s'` then single-statement calls, then revert to 20s; (4) execute_sql statements CONTINUE server-side after the connector's ~60s HTTP timeout — fire long ops, poll pg_stat_activity; (5) retention worker v2.2 (`ottoq_retention_purge_worker`, block-cursor + micro-COMMITs + advisory lock + `ottoq_retention_state`) scheduled NIGHTLY-ONLY '0 8 * * *' (=3am Chicago, per Chase: never overlap work hours); depot-tick cron = `SELECT public.ottoq_cron_tick()` every 2 min (re-armed).

**FEED-AGENT FLEET — SUBSTRATE + AGENT #1 SHIPPED (2026-07-09, migrations `feed_agent_substrate_and_charger_reliability` + `feed_agent_repair_payload_wiring`):** `ottoq_feed_plans` (versioned sampling plans + provenance + rationale + hard clamps) + `ottoq_feed_activations` (per-run agent record) + `ottoq_feed_plan(var_key)` reader. **Charger fault/repair agent LIVE + verified** on run 4d6f8bf2: 22 per-charger MTBF cards (9.0→145.1 days — lemons + golden chargers from the fitted 10k-sample grid), fault cards under plan v1 with hazard 0.20–3.28×, mode-dependent repair draws avg ~84 min (long tail to 48h) replacing the constant 50-min heal; chain = fitted grid → fault card (deal_fault_card v2) → stop_charge_session payload → recover_chargers v2. Declared assumptions (depot_grade_fault_discount 0.25, depot_staffed_factor 0.02) are IN the plan provenance, tunable. **NEMOTRON RUNNER LIVE (2026-07-09): edge fn `ottoq-feed-agents` (v2) + otto-twin-control v1.7.0 hook — every run start auto-fires the fleet.** Verified end-to-end on run 9c9e35fa: Nemotron 3 Ultra (`nvidia/nemotron-3-ultra-550b-a55b` via integrate.api.nvidia.com, key chain NVIDIA_API_KEY_NEMOTRON→CUOPT→GENERIC) reviewed the evidence dossier (fitted corpus + realized 0.037 fault rate/1k sessions), computed ~1.9 SE within binomial noise of planned 0.05 (95% CI 0.025–0.049), KEPT the plan with citable rationale, ~7-10s latency, run start never blocked. Safety: whitelist-only paths + plan clamps + ±30% max drift/activation + deterministic fallback. GOTCHA: Nemotron reasoning output wraps JSON in think-text — use balanced-brace extractJson, never greedy regex. Sources versioned in depot-sim repo (branch claude/true-world-pause). Doctrine locked: **the model proposes, SQL disposes; card engine executes deterministically.**

**TARIFF AGENT LIVE (2026-07-09, migration `tariff_agent_v1_real_nes_rates` + runner v3): the $120k/yr headline now prices at REAL Nashville rates.** `ottoq_depot_tariffs` = NES GSA Part 3 (the 1–3 MW schedule): summer $21.40 first-1,000kW / **$21.78 marginal**; winter+transition $20.34 / **$20.73 marginal**; NCP 30-min basis (depot shaving its OWN peak directly cuts the bill); provenance = NES tariff PDFs (Apr 2025 rev, eff Oct 2024) + URDB links, KUB-cross-checked. `ottoq_demand_charge_per_kw(depot, at)` = seasonal marginal reader; `ottoq_energy_cost_for_run` default 10→NULL=real lookup (explicit values still win for A/B). **VERIFIED: same 291 kW peak reprices $2,908→$6,334/mo (2.18×) — the old $10/kW UNDERSTATED Nashville ~2×; a 1,000 kW shave ≈ $21.8k/mo summer ≈ ~$250k/yr at real rates. DO NOT quote until CRN A/B re-certification.** Tariff rates are FACTS → agent whitelist EMPTY (audits freshness/schedule-fit/EVC-crossover only; verified live doing exactly that). **Strategic wrinkle to model: NES Schedule EVC = zero demand charge at 21.773¢/kWh flat — GSA-3 vs EVC crossover depends on load factor = an OTTO-Q optimization output.** Unconfirmed TVA ~4.5% Oct-2025 increase flagged MEDIUM in provenance.

**PRICE AGENT LIVE (2026-07-09, migration `lmp_agent_v1_miso_calibrated`): wholesale price now MISO-settlement-calibrated.** Research agent downloaded 912 daily MISO CSVs (docs.misoenergy.org/marketreports — KEYLESS live refresh, proven 912/912) and computed exact stats from 21,888 hourly ARKANSAS.HUB prices (TVA-proxy, declared): 2025 mean $33.44, seasonal 31.48/38.67/31.93 (S/W/Sh — WINTER-peaking!), per-season 24-pt hourly shapes (winter = DOUBLE peak, morning HE8-EST dominant 1.49× — legacy single-evening curve missed it; summer single evening 1.745×), p95 $51.08, p99 $101, storms = 3 straight Januaries 280→393→929 $/MWh (Jan-2026 >$200 for ~100h), VOLL ceiling $10k. Generator v2 = plan-driven + run-seed CRN + day-regime & winter-storm-day cards; advance_grid call-site surgeried (anchored); BESS thresholds 70/35 → computed p95/~p25 (51.08/22) via plan. VERIFIED run aeaad9d2: prices $43.5-50.3 (base×shape×1.088 day card — plausible), correct July storm-gating, 3 agents auto-activated, price agent audited citing corpus. TODO: runner evidence builder for lmp (realized-vs-plan) + unlock whitelist tunables (spike p, sigmas) next runner deploy.

**TEMPERATURE AGENT LIVE (2026-07-09, migration `temperature_agent_v1_noaa_normals`): Dec-heat-wave bug DEAD.** NOAA 1991-2020 normals for Nashville BNA (dataset `noaa_normals_1991_2020` registered — NOTE: ottoq_calibration_distributions FKs to ottoq_calibration_datasets, status enum pending/ingested/stale/failed/partial): 12 monthly quantile grids (Normal(mean,σ) from normals, clamped to 1873-present records; σ = NOAA's PUBLISHED DLY-TAVG-STDDEV, cross-validated ±0.2°C) as segments month:01..12 — `ottoq_twin_deal`'s existing 7th p_segment param + sample_calibrated's global-fallback made this one-arg surgery. Weather generator surgeried (anchored): (1) monthly segment on the daily deal; (2) **AR(1) day-to-day persistence φ=0.72** (GHCN-computed, n=10,958; variance-preserving blend of fresh draw with yesterday's realized anomaly; card value updated so tomorrow chains; idempotent via ar1 meta flag — cold snaps/heat waves are now multi-day episodes); (3) diurnal amplitude = month's real (high−low)/2 from plan (was hardcoded ±5). VERIFIED: month:12 draw 3.6°C / month:01 −3.2°C / month:07 23.6°C (old global same-seed: 13.3°C season-blind); live July run 31.8–32.6°C afternoons off a 26.9°C daily card (July mean 27.1) with ar1 flag set; **4 agents fired on run start**. GHCN QC warning: always filter QFLAG (bogus −40°C without).

**RAIN AGENT LIVE (2026-07-09, migration `rain_agent_v1_unified_ghcn`): the two-contradictory-rain-systems defect is DEAD.** Computed LOCALLY from the scratchpad GHCN file (10,956 QC-filtered days 1991-2020): monthly Markov wet-day chain (p01 0.18-0.31 / p11 0.42-0.52 — Feb wettest p_wet 0.381, Sep driest 0.259) + 12 EMPIRICAL wet-day amount grids (n=233-349/mo, May 2010 flood 184mm in tail) under dataset `ghcn_daily_bna`. Architecture: **`ottoq_precip_daily_mm(run, clock)` = the ONE dealer** of the 'precip_mm' day card (Markov occurrence + monthly amount, run-seed CRN, A.10 precip knob on probability); hourly `ottoq_sim_sample_precip_state` v2 DISAGGREGATES that budget (wet-window by size 2/4/6/9h, summer 13-17h convective bias, intensity=budget/hours×jitter — declared assumptions); weather fn surgeried (7-arg call + rain→cloud≥75 coupling before GHI); `ottoq_twin_climate_stress` rewired to the same dealer. **BONUS LATENT-BUG KILL: climate_stress's ambient read was silently NULL (epoch mismatch: callers pass date-diff sim_day but its v_clock reconstruction assumed run-relative) — heat/cold stress NEVER fired from real temps; fixed via DATE '2020-01-01'+p_sim_day reconstruction — cascade now reads ambient_c 26.9 ✓.** VERIFIED run fdeb2e43: dry July day card (markov_p 0.331 = exact corpus value), all hourly snapshots dry in agreement, cascade precip_stress 0 from the same card, **5 agents fired**. **SERVICE MANIFEST AGENT LIVE (2026-07-10, migration `service_manifest_agent_v1_pervisit`): the frozen-manifest correctness bug is DEAD.** Root cause was worse than audited: seed = hash(vehicle+run) AND the manifest was NEVER cleared (vehicles.config persists) → one manifest per vehicle FOREVER, across runs. v2: run-seed CRN + per-VISIT salt (last_state_change timestamp — stable within visit, fresh across); manifest+meta cleared at deployment on BOTH dispatch paths (`ottoq_sim_dispatch_vehicle` + `ottoq_sim_prime_deployment`, anchored surgery — verified no duplicate config assignment); 8 probabilities moved into plan `service_manifest` (declared tunable assumptions); **camera-triggered cleaning now couples to the unified rain card** (tidy/sensor-clean/wash/deep × (1+precip_stress×0.6), clamped ≤0.90); run resolved from depot when caller passes NULL (one-running-run-per-depot makes it unambiguous). VERIFIED run 059934a8: 57 manifests, 57 CRN-seeded, **57 DISTINCT visit salts**, 0 deployed vehicles holding manifests, precip coupling inert on the dry day (correct). meta in `vehicles.config->service_manifest_meta` = per-manifest provenance. TODO next runner deploy: whitelist service_manifest probabilities + precip_soil_coupling (evidence: realized service rates) + lmp tunables + evidence builders.

**ALL 6 CRITICAL AUDIT DEFECTS RESOLVED.**

**ENERGY A/B RE-CERTIFIED (2026-07-10, CRN seed 777, benchmark depot 22222222, 40 ticks = 20 sim-hr, real NES rates + MISO price curve + new BESS thresholds; ab_group 45d5096b):** baseline = **greedy (uncontrolled charging — the industry-standard managed-charging comparison; NOT 'manual', which starves throughput and trivially has a low peak)**. RESULT: **peak 2,396 → 1,303 kW = 1,093 kW shaved (−45.6%) WHILE delivering +52% more energy (6,841 vs 4,510 kWh) and 25× the turnarounds (99 vs 4), 0-unsafe on both.** Priced at NES GSA-3 marginal: **$52,189 → $28,375/mo summer = $23.8k/mo saved; seasonal blend (4 summer × $21.78 + 8 × $20.73 per kW) ≈ $276k/yr per depot.** NEW HEADLINE: **"~$276k/yr, −46% peak, while turning around 25× more vehicles — zero unsafe"** (supersedes 42%/$120k). CAVEATS: (1) ottoq_ab_runs.energy_peak_kw column is a scoring artifact (2365.9 on BOTH policies — policy-independent read; the certified peaks come from per-run-tagged site_energy_snapshots via cost_for_run — fix the scorer read later); (2) B1.5 calibrated charge curves NOT yet in — re-run the cert when they land; (3) 20-hr window peak treated as monthly NCP (CRN pairing keeps it apples-to-apples). Benchmark depot got flagship tariff rows + TOU windows copied (energy avg rate 0.092 real, was 0.10 fallback).

**CHASE'S PRIORITY LOCK (2026-07-10): motion → OTTO-Q agentic audit → everything else; SECURITY LAST (demo-only for months; investor/OEM talks in WEEKS; pilots much later). ARCHITECTURE SEPARATION STANDING ORDER: the feed-agent fleet lives ONLY on the twin/world-sim side; the agentic audit must ONLY strengthen OTTO-Q's EXISTING agent layer (copilot/orchestrator-agent/OTTOCommand) — never blur the seam. EVERYTHING = validating OTTO-Q V1 for investors/OEMs → pilot partnerships.**

**MOTION BUG REPORT (Chase 2026-07-10, screenshots): vehicles still pile up + ALL STALL OUT after a few minutes; sim AUTO-STARTS without selecting anything (auto-attach auto-play — he wants attach-frozen, explicit Play); movement still somewhat aimless, degrades over time. His hypothesis to check: is it renderer motion or the OTTO-Q↔vehicle communication depth? "Want to make sure OTTO-Q is communicating correctly and deeply enough" = THE final goal; flag comms findings during motion work.**

**ROADMAP TO LIVE RUNS (Chase 2026-07-10 — "make sure it all comes together"; tasks #86-89):** (1) twin MOTION completion #88 (B3 legs playback in renderer + B2.2 + choreography polish + merge claude/true-world-pause); (2) OTTO-Q AGENTIC strengthening #87 (central-vs-specialist architecture audit) + runner v4 #89 (tunable whitelists + evidence builders); (3) LIVE-RUN GATES: OQ-7 security RLS (deploy gate, needs Chase sign-off), OQ-6 action-API wiring, production_live cadence; (4) B1.5 charge curves + cert refresh; (5) Isaac lockstep A4 + Omni-3/4 for the visual demo. **Energy A/B re-cert = one pass covering tariff ($21.78/kW) + thresholds + price curve together.** Chase framing: the genetic layer is a MASSIVE talking-point edge; each variable deep + accurate.

**BACK POCKET (Chase 2026-07-09, do NOT forget):** fresh audit of OTTO-Q's agent layer — decide ONE central agent vs MANY specialist agents acting in unison on one layer, each with own parameters, sitting ATOP the deterministic safety filter. Queued after the twin variable work.

**CHASE'S AGENT-PER-VARIABLE VISION (2026-07-08, engage next):** each sim variable gets its own feed-agent that fires at run activation — owns its data source (corpus/API), analyzes sample size/distribution, decides the sampling plan for that run, pushes its slice in with provenance; clusters with baked-in parameters, possibly NeMo-trained per variable. Same pattern on OTTO-Q side: per-domain intelligence (vehicle/energy/charging/service) on Nemotron watching its own variable stream into the decision core. FIRST STEP (Chase's words): audit every ingested dataset — sample size, distribution, current sampling method — then design each agent around that dossier. Keep in-run draws deterministic/CRN (card engine); agents decide PLANS at activation, never per-tick randomness (preserves A/B + 0-unsafe).
