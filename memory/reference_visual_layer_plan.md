---
name: reference_visual_layer_plan
description: Where the Isaac Sim / Omniverse hyper-real visual-layer plan lives and its headline
metadata: 
  node_type: memory
  type: reference
  originSessionId: b5cc1e94-2201-451a-a46f-c770df70e99a
---

Visual-layer (hyper-real depot movement/visualization) plan: **`~/Desktop/OTTO-Q V1/OTTOQ_VISUAL_LAYER_PLAN.md`** (drafted 2026-06-19).

**Render path DECIDED (2026-06-20, Chase's steer): Isaac Sim on Omniverse** for Tier B — strongest long-term frontier (photoreal RTX + physics-AI motion/sensor sim + cuOpt-for-Isaac), not the fastest. UE5 build = the high-fidelity **USD asset source** (UE5 authors the look; Isaac/Omniverse does the sim + the WebRTC stream). Goal: 100% tight before showing, both visual spectacle AND frontier-technical so OEM engineers greenlight a telemetry pilot. Fallbacks (not chosen): UE5 Pixel Streaming (fastest, viz-only), plain Omniverse Kit (photoreal, no physics-AI). Two tiers: **Tier A** = cockpit Three.js motion polish off the backend twin ($0, no GPU — lane paths, spacing, move-times, congestion, variability dropdown); **Tier B** = Isaac-on-Omniverse **Kit App Streaming** via **WebRTC** (`web-viewer-sample` AppStreamer React component). UE5 depot → **native OpenUSD** (UE 5.4+; Omniverse Launcher deprecated Oct 2025) → Omniverse/Isaac. Deploy: **RunPod** for cheap dev, **AWS** (Inception ~$100K credits) for the demo — both ready now (provisioning gate lifted). Validated by **Cyngn × NVIDIA Isaac Sim (Feb 2026)** running AV fleet-mgmt in a warehouse twin with cuOpt-for-Isaac — OTTO-TWIN's exact thesis.

Already done: Omni-1 (depot USD), Omni-2 (provisioning playbook). Next: Omni-3 (web-viewer scaffold, frontend-only, ready now) → Omni-4 (Kit app on RunPod→AWS). Sequence gate: do the [[project_twin_arrival_cycle]] charge-assignment fix first. See [[project_sim_visual_movement]], [[feedback_scout_external_tools]].

**PLATFORM DECISION ✅ (CTO call, 2026-06-27, Chase deferred — "you have decision-making ability, only positioning questions"): STAY ON LOVABLE, no migration.** Research-grounded: Lovable = standard React/Vite + full GitHub sync + npm + Supabase wired + native Three.js; NVIDIA ships the photoreal embed as a **React component** (`AppStreamer` from the `omniverse-webrtc-streaming-library` npm pkg) made to drop into exactly this kind of app → Lovable hosts BOTH Tier A (Three.js) and Tier B (WebRTC stream). Twin stays on Supabase; GPU render farm (RunPod/AWS) runs OFF-platform, streams in as WebRTC video + data channel. Fully decoupled, NVIDIA's own supported pattern. Only caveat: Lovable's AI-gen degrades >300-line components, so the complex visual code is HAND-AUTHORED in the repo (via GitHub sync) — which is how Claude already works; non-issue. (Sources: docs.lovable.dev, github.com/NVIDIA-Omniverse/web-viewer-sample.)

**THE MOTION CONTRACT (keystone, build first, $0/no-GPU):** hyperreal motion (Chase: leave charging slot 15 → pull into wash bay 2 → dwell per SoC → next bay → park in spot X → final approval → egress) needs the twin to expose a spatial layer the renderer animates. OTTO-Q already owns the DISCRETE truth (which stall/bay, durations, what's next); the renderer interpolates the smooth motion. Build once → Three.js (now) + Isaac (later) both consume it. **Spatial assessment 2026-06-27:** `ottoq_site_structures` ALREADY holds the MACRO layout (canopies/gates/office/wash-bldg/BESS/fence/sign at origin_x_ft/origin_y_ft + width/length/height/rotation_deg + absolute_lat/lng). GAP: fine-grained per-resource coords (each charger stall, wash/service bay, staging/parking spot) + the LANE NETWORK are NOT in the twin (live only in the renderer/UE5). **Step 1 = add the fine-grained spatial map (place every stall/bay/spot at x/y within the macro structures + lane waypoints) into the twin + a per-vehicle move-order feed (from→to→path→duration), sourced from the depot layout (depot-sim app / site plan / UE5 USD).** Then Tier A Three.js consumes it ($0, de-risk), then Tier B Isaac/Omniverse (photoreal, GPU) consumes the SAME contract. **REFINED SEQUENCE (CTO, 2026-06-27): (1) motion contract [backend] → (2) Tier A Three.js in Lovable → (3) Tier B Isaac/Omniverse on RunPod→AWS + finish UE5 hyperrealism (MCP artifact build) in parallel.** Audit each step before the next (Chase's standing). The depot-sim app on the Desktop (`~/Desktop/ottoyarddepot-sim`, the "OTTOYARD Depot Simulator" Three.js app) is the likely Tier-A host + a layout source.

**ASSESSMENT 2026-06-27 (analyze-first — AVOIDED a rebuild): most of the "motion contract" ALREADY EXISTS.** (1) **Spatial map ✅ in the twin** — `ottoq_twin_depot_layout(depot)` returns 150 stalls (dcfc/l2/wash_bay/service_bay/staging) each with x/y/heading/zone/canopy/connector_kw + 12 macro structures. (Positions are computed/served by that fn, not a plain column — why the column-search missed them.) (2) **Lane network + full flow choreography ✅ client-side in `~/Desktop/ottoyarddepot-sim/src/lib/sitePlan.ts`** (349 lines, user-locked 2026-06-10): logical 2D coords (x0-300,y0-220) → 3D world map; gas-pump charging flow (flanking pull-out GAP_LANES → sidestep into stall → exit N to collector), pull-through wash/service bays (front-in/rear-out into a 30ft apron → WEST_LINK or east aisle → parking/egress), two-way collectors (NORTH_LANE_Y/SOUTH_LANE_Y) + one-way aisles (WEST/EAST_AISLE_X), CANOPIES (A dcfc / B,C l2), PARK_RUNS, INGRESS/EGRESS/QUEUE_Y. (3) **Motion engine ✅** `src/engine/SimulationEngine.ts` + `Vehicle{position,targetPosition,waypoints}` in `engine/types.ts`. (4) **Twin client ✅** `src/lib/ottoTwin.ts` → `otto-twin-control` edge fn → reads real `ottoq_twin_snapshot` + `ottoq_twin_depot_layout`; `useTwinSceneBridge` is the scene bridge. **THE REAL GAP (narrow, mostly client-side):** the app's motion runs off a LOCAL sim (`ArrivalGenerator` flood, flagged for retirement) not the live twin → rewire to drive the existing motion engine off OTTO-Q's real snapshot (diff snapshots → per-vehicle move-orders → animate along the existing sitePlan lanes), then polish to hyperreal. May need a small twin add (explicit move-order/transition events) only if snapshot-diffing is insufficient. Repo: `github.com/OTTOYARD/ottoyarddepot-sim` (bun; has `unreal/` + `supabase/` dirs). NEXT: audit `useTwinSceneBridge` (twin-driven vs local) → rewire + motion polish.

**TIER-B POD BUILD IN PROGRESS (2026-06-27, RunPod):** Chase activated the RunPod L40S; access recipe CONFIRMED working: `ssh root@<ip> -p <port> -i ~/.ssh/id_ed25519` (Mac pubkey `ssh-ed25519 …EJAb chase@ottoyard.com`). Pod is EPHEMERAL → after any restart, Chase must re-add my pubkey via the RunPod **Web Terminal**: `mkdir -p ~/.ssh && echo "<pubkey>" >> ~/.ssh/authorized_keys`. Box: NVIDIA **L40S 46GB, driver 580**, 256 cores, 1TB RAM; **NO docker** (RunPod is itself a container → use Kit `repo.sh`/packman + Isaac via pip, not docker); `/`=40GB overlay (build here), `/workspace`=slow MooseFS net vol (don't build there), `/dev/shm`=58GB (noexec). Build dir: `/root/ottoq/`. **KEY FIX — kit-app-template MUST use branch `107.3`, NOT `main`:** latest main (branch 110.1.2) pins unpublished bleeding-edge packman tooling (repo_docs@2.8.1, repo_codesign@7.5.0) NOT on the public CloudFront mirror → "Package not found"; `git clone --depth 1 -b 107.3` bootstraps cleanly (107.3 also version-matches web-viewer-sample 1.5.2 for the embed). **NGC key NOT needed for the Kit SDK pull** (packman remote = public `d4i3qtqj3r0z5.cloudfront.net`, no auth) — the `nvapi-…` key Chase gave is parked for later (NVCF deploy / Isaac `nvcr.io` containers); recommend rotating it (came via chat). **WRINKLE:** `./repo.sh template new` is TUI-only (`--generate-playback` only, no direct --name flags) → can't drive blind over SSH; plan = Chase runs `template new --generate-playback /root/ottoq/depot.toml` in the Web Terminal (Application→USD Viewer, name `ottoq.depot_twin`, "OTTO-Q Depot Twin", v0.1.0, **add Streaming layer**), then Claude does `template replay` + `build` + load `ottoyard_depot.usda` + path-tracing + WebRTC + expose RunPod TCP port + wire `web-viewer-sample` 1.5.2 into the cockpit, all non-interactively. Pod bills ~$1/hr — STOP when idle (resume = restart + re-add key). Isaac Sim (cuOpt-for-Isaac motion) = the layer after the Omniverse Kit photoreal render works.

**POD BLOCKER FOUND (2026-06-28) — Tier B stalled on PROVISIONING, not code.** This RunPod L40S is **compute-only**: CUDA/nvidia-smi work, but the NVIDIA Vulkan ICD returns **"Found no drivers!" / ERROR_INCOMPATIBLE_DRIVER** → Omniverse Kit can't start the RTX renderer. Root cause: pod created without graphics — `NVIDIA_DRIVER_CAPABILITIES` empty + `/dev/dri/renderD130` (the GPU render node) owned by `nobody:nogroup` mode 660, and container-root **can't `chmod` it** ("Operation not permitted" → user namespace). NOT fixable in-container: loader upgraded to 1.4.313 (was 1.3.204), ICD complete + exports all three `vk_icd*` symbols, all NVIDIA Vulkan libs present — the driver simply has no graphics **device access**. Diagnosis complete + the Kit app is **BUILT** + `ottoyard_depot.usda` **staged on the pod** → once graphics is enabled, launch = ~1 command + shader compile. **FIX (Chase, async): recreate the pod with graphics enabled.** Best = official NVIDIA **Isaac Sim image** (`nvcr.io/nvidia/isaac-sim`; needs NGC login on RunPod; IS the next layer + bundles Kit + WebRTC streaming) with `NVIDIA_DRIVER_CAPABILITIES=all`. Quick-try = redeploy with env `NVIDIA_DRIVER_CAPABILITIES=all` AND confirm the pod actually passes `/dev/dri` with accessible perms (RunPod graphics support varies by host/template). **CTO call: PIVOTED to Tier A (web motion, $0/no-GPU, sequenced first) while the pod is re-provisioned — Tier B resumes instantly once graphics is on.**

**TIER A BUILT + VERIFIED (2026-06-28).** Branch `claude/tier-a-twin-motion` in `~/Desktop/ottoyarddepot-sim` (NOT yet committed — offer commit+PR). NEW `src/engine/TwinMotionDriver.ts` + rewired `src/hooks/useTwinSceneBridge.ts`: the old bridge TELEPORTED vehicles to stalls each 1.5s poll (cursor re-assignment → jitter, `targetPosition:null`). The driver now keeps a **stable per-vehicle stall assignment** + on each backend state change routes the vehicle along the real lanes (`sitePlan.routeToStall`/`routeToEgress`) while a rAF loop interpolates ~14mph; `Vehicle3D` already faces travel + settles to stall angle. Clean split: twin owns discrete truth (which stall/state), renderer owns ONLY interpolation — zero world logic client-side. Verified: tsc clean, eslint clean, `vite build` exit 0, **6/6 vitest behavioral tests pass** (`TwinMotionDriver.test.ts`: routes-not-teleports, stable assignment, no double-book, frees old stall on lane-change, egress-on-drop, arrival-waits-at-gate). **DECIDED + SHIPPED (Chase 2026-06-28): operator-triggered + curated scenario QUICK-LAUNCH** (NOT auto-attach — Chase: don't burn GPU running in the background; have a few select demo runs as one-click chips, "normal day = randomness from the hat" + named extremes). Launcher = **PR #10** (`claude/scenario-quick-launch`): 6 featured chips in OperatorConsole Run Control (Normal Day / Heat Wave / Winter Storm / Demand Charge / Charger Outage / Peak Turnover) → one-click `twin.start(code)` → cockpit shows Tier A motion; full deck stays in the dropdown; running scenario highlighted. Verified: tsc clean, dev server renders all 6 chips, no console errors (1440×900 snapshot+screenshot). **SCENARIO AUDIT — 8 decks ALREADY exist in `ottoq_sim_scenarios`** (did NOT rebuild): normal_day, heat_wave, winter_storm, **dr_event_cascade = the demand-charge/energy headline**, charger_outage_morning_rush, grid_brownout_at_peak, solar_underperformance_partly_cloudy, aggressive_fleet_turnover. Deck override schema: `weather_overrides{ambient_peak_c,ambient_floor_c,cloud_max_pct,humidity_floor_pct}`, `grid_overrides{lmp_multiplier,dr_required_cap_kw,force_dr_call_hour,force_bess_starting_soc_pct}`, `fleet_overrides{dispatch_rate_multiplier,target_deployed_fraction}`; engine ALSO exposes `_rates` knobs (arrival/dispatch, ETA-delay, DR-likelihood, charger-fault, incident, staffing) via the variability profile — but those are NOT in the deck override JSON, so a "traffic/incident" deck needs research on how a scenario seeds `_rates` (open question before building those decks). **FOLLOW-UP (not yet done):** add 3 decks via apply_migration on otto-q-core — `traffic_surge` (high dispatch + ETA delay), `mass_incident_response` (incident spike), `overnight_wave_prestage` (day/night ops) — to round out Chase's demo set; optional Yuka steering polish. Tier A motion = **PR #9**; the live motion screenshot needs PR #9 merged + a churning run. Run selection: OperatorConsole start (keyless on private link) / History open; poll `useTwinFeed` @1500ms; ResponsiveGuard requires ≥1200px.

**SIM TROUBLESHOOT + RELIABILITY (2026-06-28, PR #11 `claude/sim-empty-state`).** Chase reported empty depot + error log + no movement on the "Lovable auto twin simulator." DIAGNOSIS (drove the live app on main, both PRs #9/#10 merged in): the sim WORKS — a running scenario shows 216 vehicles, twin advances (watched 7:55am→6:55pm, tick climbing), **0 console errors, 0 failed requests.** Root causes + fixes: (1) **operator-triggered → an idle depot looked broken** (no active run = empty lot). Chase's call: keep operator-triggered + a clear prompt → FIXED with `DepotEmptyState` overlay ("Depot idle — pick a scenario from Quick Launch"); hides when a run is live/vehicles appear. (2) **intermittent "Start failed" when launching a scenario over an already-running run** — race on the one-run-per-depot lock (old 400ms wait too short) + a transient empty `/sim_runs` read skipping the stop. FIXED — `startScenario` now **polls until live runs are actually cleared** (survives a flaky list) + **retries start with backoff**; leftover runs self-heal. Both verified live (idle overlay → launch clears it → depot fills; run dbf216ae running). Chase's error was on the **deployed Lovable site specifically** (couldn't repro locally) → most likely this start-race, or the 2-project split below. **KEY FINDING — the app uses TWO Supabase projects:** twin data (snapshots/scenarios/layout) → `gxdrcyphqjzjsuhxuqtg` (otto-q-core, hardcoded BASE in `ottoTwin.ts`); the AI-helper tabs (Copilot/Scorekeeper/SwapTest/history/cuopt) → a DIFFERENT project via the **committed** `.env` (`VITE_SUPABASE_URL` = `hfjaofyfxsy…`). Consolidating those onto otto-q-core is a follow-up (violates single-source-of-truth). **OTHER ISSUES (not yet fixed):** gate over-clump (~49 `arrived_at_gate` but `gatePos` only fans ~24 slots → overlap) + over-subscription (216 vehicles vs ~160 stalls → permanent gate backlog → tune scenario fleet size / check decide_tick assignment starvation). **CTO PLAN (sequenced):** P1 make-it-work [empty-state ✅ PR#11; gate queue-line + fleet right-size NEXT] → P2 Yuka motion polish → P3 decks (traffic_surge/mass_incident/overnight_wave) → P4 Tier B [pod re-provision + UE5→USD + Omniverse stream + Isaac]. **Deeper fix:** make `/scenarios/start` server-side auto-abort the depot's existing run (then the client needs no stop-then-start at all).

**TIER B ON AWS (2026-06-28) — path VALIDATED, one version-skew blocker left.** Chase chose AWS over RunPod (RunPod's container security blocks the Vulkan device → confirmed dead end). Box: **EC2 g5.2xlarge, NVIDIA A10G 24GB, driver 595, Ubuntu 24.04** @ `3.235.62.21`, user `ubuntu`, key `~/Downloads/ottoq.pem` (Mac). **EBS persists on Stop** (unlike RunPod) → Chase can Stop to save ~$1.20/hr + I resume intact (I have NO AWS console access — can't stop it myself). Credits applied. **WINS:** (1) **Vulkan WORKS** — `vulkaninfo` shows NVIDIA A10G as a real device (the full VM gives real `/dev/dri` access, `card0`/`renderD128` = `root:video`, not RunPod's `nobody:nogroup`). Installed `libvulkan1`+`vulkan-tools`; NVIDIA ICD present. (2) **Kit app generated NON-INTERACTIVELY** — reverse-engineered the `repo_kit_template` playback TOML format (keyed by template_id: vars + `add_layers` + `selected_layers`=presentation-name `[omni_default_streaming]: Omniverse Kit App Streaming (Default)` + nested `[omni_usd_viewer.extensions.<id>]`); EULA pre-accept = `touch .omniverse_eula_accepted.txt` (Chase approved the NVIDIA Omniverse license). Playback at `~/ottoq/playback.toml` + scratchpad. (3) **Both apps BUILD** (`define_app("...streaming.kit")` added to premake5.lua; streaming kit rendered from `templates/apps/streaming_configs/default_stream.kit` via sed). (4) **WebRTC streaming server STARTS** ("Streaming server started."). USD staged at `~/ottoq/ottoyard_depot.usda`; setup ext loads `--/app/auto_load_usd=<path>`; launch needs `--no-window` (headless, no X) + `XDG_RUNTIME_DIR`. **BLOCKER:** RTX renderer **segfaults in `librtx.scenedb.plugin.so`** when building the scene headless — BOTH base + streaming app → it's the render stack, not livestream. Root = **Kit 107.3 core (carb CudaInterop v0.15) vs registry extensions built for v0.17** version skew. **FIX OPTIONS (CTO, next):** (a) rebuild on the CURRENT Kit release (110.x) so core+extensions match — quickest, but 110.x had a packman "Package not found" on RunPod (may be transient; retry on AWS); (b) pin 107.3-compatible extension versions (tedious); (c) **NVIDIA prebuilt Isaac Sim container** (`nvcr.io/nvidia/isaac-sim`, Chase's NGC key) — fully version-matched + headless WebRTC built-in + sets up Isaac motion later (most robust, needs Docker on the box). Lean (a)→(c). Build dir `~/ottoq/kit-app-template` (107.3). **Yuka (Tier A polish): installed `yuka@0.7.8` on branch `claude/yuka-motion` in depot-sim — parallel track, no GPU, finish next.**

**DECISION 2026-06-28 (Chase: "most robust + frontier"): pivot Tier B to the NVIDIA prebuilt Isaac Sim container** (abandons the version-skewed hand-built kit-app-template 107.3). Image **`nvcr.io/nvidia/isaac-sim:6.0.1`**; run headless+stream: `docker run --gpus all --network=host -e ACCEPT_EULA=Y -e PRIVACY_CONSENT=Y ... nvcr.io/nvidia/isaac-sim:6.0.1` then `./runheadless.sh -v` (or `runheadless` entrypoint); **WebRTC ports 49100/TCP (signaling) + 47998/UDP (media)** (overridable via ISAACSIM_SIGNAL_PORT/STREAM_PORT). AWS box is Docker-GPU-ready (Docker 29.6, nvidia runtime registered, 134GB free; use `sudo docker`, ubuntu not in docker group). **BLOCKED on Chase NGC `sudo docker login nvcr.io` (user `$oauthtoken` + his nvapi key)** — Claude must NOT enter API keys (boundary); Chase does it. Then Claude pulls (~15GB) + runs + mounts `~/ottoq/ottoyard_depot.usda` + opens the 2 ports in the EC2 security group (Chase, no AWS console for Claude). Custom-USD-load syntax for Isaac runheadless = TBD (research at run).

**✅ ISAAC SIM STREAMING LIVE ON AWS (2026-06-28) — Tier B infra conquered.** AWS `g6e.2xlarge` (L40S 48GB, driver **595.58.03** open-kernel), IP **3.235.62.21**, SSH `ssh -i ~/Downloads/ottoq.pem ubuntu@3.235.62.21` (add `-o ServerAliveInterval=30` — sessions idle-drop). Box is a full VM → real Vulkan/graphics (the RunPod wall is gone). **Container** `isaac` (image `nvcr.io/nvidia/isaac-sim:6.0.1`, 21GB) launched: `docker run -d --name isaac --gpus all -e ACCEPT_EULA=Y -e PRIVACY_CONSENT=Y --network=host -v ~/ottoq:/depot:rw -v ~/docker/isaac-sim/cache/kit:/isaac-sim/kit/cache -v ~/docker/isaac-sim/cache/ov:/root/.cache/ov --entrypoint bash <img> -c "cd /isaac-sim && ./runheadless.sh --/exts/omni.kit.livestream.app/primaryStream/publicIp=3.235.62.21 --/rtx/verifyDriverVersion/enabled=false"`. **FIX that unblocked it:** nvidia-container-toolkit graphics manifest hardcodes egl-wayland `1.1.13`/`1.1.20` but host has `1.1.21` → `sudo ln -sf .../libnvidia-egl-wayland.so.1.1.21 .../libnvidia-egl-wayland.so.1.1.13 && sudo ldconfig` (do `.1.1.20` too for restart-robustness). Confirmed: `omni.hydra.rtx.shadercache.vulkan` ✓, `Isaac Sim Full Streaming 6.0.1-rc.7` ✓, `omni.kit.livestream.webrtc-10.3.2` ✓, listening `0.0.0.0:49100` (signaling, **externally reachable confirmed via nc**) + 47998/UDP. Kit **107** → cockpit embed = **web-viewer-sample 1.5.x** (AppStreamer / omniverse-webrtc-streaming-library). Security group opens 49100/TCP + 47998/UDP + 8210/TCP. OmniHub "Hub failed to launch" warnings = NON-FATAL (Nucleus connector; local USD fine). Depot USD at `~/ottoq/ottoyard_depot.usda` (host) → `/depot/ottoyard_depot.usda` (container). **NEXT (last mile):** (1) load depot USD into the stream (research: runheadless startup-stage flag OR standalone Python `SimulationApp(headless+livestream)` + `open_stage`, OR openStageRequest msg from the web-viewer), (2) build web-viewer-sample 1.5.x embed into depot-sim cockpit (new "Photoreal" view) → connect to 3.235.62.21:49100, (3) Chase visual-confirms (WebRTC needs a real browser — doesn't verify in Claude's headless preview). **STOP the g6e when idle (~$2–4/hr on Activate credits).**

**✅ PHOTOREAL EMBED LIVE IN COCKPIT (2026-06-28) — PR #13 `claude/photoreal-embed`.** depot-sim now has a 3rd view toggle **2D / 3D / RTX**; `OmniverseViewer` (src/components/photoreal/) uses `@nvidia/omniverse-webrtc-streaming-library@5.6.0` in **DIRECT mode** → connects to `<ip>:49100`. Lib is NVIDIA-proprietary, NOT on public npm → `.npmrc` scopes `@nvidia` to `https://edge.urm.nvidia.com:443/artifactory/api/npm/omniverse-client-npm/` (public, no auth) — committed so deploy CI resolves it. IP overridable at runtime via `localStorage('ottoq_omniverse_ip')` (default 3.235.62.21; EC2 public IP changes on stop/start → use Elastic IP). **VISUALLY CONFIRMED**: clicked RTX in the preview → AppStreamer connected to the live AWS Isaac stream → viewport rendered + WebRTC data channel active (PassThruSessionId msgs) + 0 errors. tsc/build clean; viewer = lazy 618KB chunk (only loads on RTX). Verified via Claude_Preview headless Chromium (WebRTC DID render there). **REMAINING POLISH:** (1) load depot USD into the stream — `runheadless.sh` passes `"$@"` through to `isaac-sim.streaming.sh`/kit app, so try `--exec <script that open_stage's /depot/ottoyard_depot.usda>` OR positional/`--/app/auto_load_usd` (verify via the preview render loop — restart = ~6min compile; risk: a bad arg could fail startup, current empty-stage stream WORKS so don't break it blindly); interim = File→Open in the streamed Isaac UI (embed forwards mouse/kbd, /depot is mounted). (2) hide Isaac editor chrome for a clean depot-only view. (3) later: push OTTO-Q assignments over the data channel to move vehicles in the photoreal twin.

**✅ DEPOT RENDERS IN THE COCKPIT (2026-06-28) — full Tier-B pipe done.** The depot USD now **auto-loads + lights + frames** in the Isaac stream, visible in the cockpit RTX tab (verified via preview screenshot — depot canopies/buildings geometry rendering). Mechanism: `runheadless.sh ... --exec /depot/open_depot.py`. **`~/ottoq/open_depot.py`** = async startup script: waits 60 frames → `omni.usd.get_context().open_stage("/depot/ottoyard_depot.usda")` → waits 180 frames → adds `UsdLux.DomeLight` only if none (depot already HAS lights from the dusk pass) → computes stage bbox (`UsdGeom.BBoxCache`, center=(-395,1216,1537) size=(24925,19712,3105) USD units) → `isaacsim.core.utils.viewports.set_camera_view(eye=center+0.85*diag, target=center)`. **KEY GOTCHA:** the Isaac container runs as sandbox user **uid 1234 (isaac-sim)** — scp'd files are mode 600 (owner-only) → "Permission denied" / "Can't find a file to execute". MUST `chmod a+r ~/ottoq/*` (open_depot.py + ottoyard_depot.usda) before launch. **RELAUNCH = one command: `bash ~/ottoq/launch_isaac.sh`** — auto-detects the EC2 public IP (IMDSv2), chmods, runs the container; depot auto-loads ~6min after start. Cockpit IP override if changed: `localStorage.setItem('ottoq_omniverse_ip','<ip>'); location.reload()`. **REMAINING POLISH (cosmetic):** hide the Isaac editor chrome (panels/toolbar show around the viewport) for a clean depot-only view — Kit UI-hide setting on next restart; optionally a tighter cinematic camera. PR #13 (embed) is MERGED. Dev-server gotcha: after npm changes, `rm -rf node_modules/.vite` if the RTX lazy-chunk fails to fetch (prod build unaffected).

**FUTURE DIRECTION — city-map arrival visualization (Chase 2026-06-28, DEFERRED behind motion/realism):** pick a real metro map (e.g., Nashville); at run spin-up, place deployed vehicles at randomized positions AROUND the city (moving dots on roads). Each vehicle's arrival ETA = realistic travel time from its position → the depot. When a vehicle notifies OTTO-Q (servicing/arrival need), its ETA is queued for processing/scheduling; traffic/weather delays (ALREADY built — weather cascade: precip→ETA 2.4× etc.) dynamically flex the ETA → OTTO-Q must react agilely. Show vehicles driving in from the road INTO the depot to begin OTTO-Q assignments. ALL vehicle randomness lives in the twin variability ecosystem (NOT the renderer). EXTENDS the sparse-start + staggered-return model just built (deployed vehicles already carry randomized return ETAs). Chase: **motion/realism FIRST, then "back into" the city-map/placement/ETA logic.** See [[project_depot_ops_model]], [[project_twin_arrival_cycle]].

**TIER-A MOTION QUALITY (in progress 2026-06-28, Chase feedback):** vehicles (a) slide without turning / no real vehicle movement, (b) drag over permanent structures (railings, structural beams, canopy posts). Prime suspect = Yuka `SeparationBehavior` (weight 2.2 vs FollowPath weight 1) pushing vehicles OFF their lane paths → sideways drift (slide) + into structures (clip). Fix plan: keep vehicles strictly ON the `sitePlan` lane paths (reduce/zero separation now that sparse-start means fewer cluster collisions; add `OnPathBehavior` pull-back), ensure Vehicle3D faces travel (already does — verify), smooth corner turns, and confirm routes never cross structure footprints. Files: depot-sim `src/engine/TwinMotionDriver.ts` (Yuka), `src/lib/sitePlan.ts` (routes/zones), `src/components/canvas/three/Vehicle3D.tsx` (facing), the 3D structure components. Verify by watching a live ticking run in the preview 3D view. **YUKA INTEGRATED** into `TwinMotionDriver.ts` (branch `claude/yuka-motion`): replaced linear lerp with Yuka `FollowPathBehavior`+`SeparationBehavior` (path-follow + arrive-ease + anti-overlap/anti-clump); `reconcile` unchanged (its 6 tests still valid); extracted public `tickMotion(dt)` + added a runtime test (steers toward stall). tsc clean (yuka ships NO types → treated as `any`, so runtime test matters).

**✅ MOTION-QUALITY FIX SHIPPED (2026-06-29) — PR #14 `claude/motion-quality`** (https://github.com/OTTOYARD/ottoyarddepot-sim/pull/14). ROOT CAUSE confirmed = TWO bugs, both fixed: (1) Yuka `SeparationBehavior.weight` **2.2 → 0.35** — at 2.2 it was STRONGER than `FollowPathBehavior` (weight 1) so the anti-overlap force shoved cars sideways OFF the lanes (= the "slide / don't turn / drag over railings-beams-posts"); sparse-start means little to de-clump so a gentle nudge is plenty + lane-following wins. (2) `VehicleDot.tsx` (2D) `transition` **0.9s cubic-bezier → 0.12s linear** — the 0.9s ease LAGGED the live per-frame Yuka motion = the "slide." Speed stays tied to sim clock (a `min(simSpeed,4)` cap I tried HALVED speed since store default `simSpeed=10`→`min(,8)`=8, broke the steers-toward-stall test → reverted to 8). 7/7 vitest + build green. **DIAGNOSIS COMPLETE — DON'T re-litigate routing:** the lane network in `sitePlan.ts` is DELIBERATELY structure-aware (GAP_LANES 80/126.5/173.5/220 thread BETWEEN canopies A=88-118/B=135-165/C=182-212; west aisle x=30 clears the carport ending x=22; rear west-link x=66 threads BESS-end-62 ↔ building-start-70) AND the flow is **user-locked 2026-06-10** (the ingress→west-aisle "detour" is intentional one-way circulation, not a bug). 3D `Vehicle3D` already rotates to face travel (`atan2`, line 107) + lerps `rate=delta·2·simSpeed`; twin driver & legacy `SimulationEngine` are mutually exclusive (`useTwinSceneBridge.ts:26`) so they never fight. **IF clipping persists** after Chase judges PR #14 live, the only remaining lever is a per-corner tighten of the 3D lerp / a `sitePlan` no-go-zone at a specific spot — pinpoint visually, don't pre-build. Motion QUALITY still needs Chase's live-run visual judgment (can't fully judge headless).
