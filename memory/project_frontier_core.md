---
name: project_frontier_core
description: "FRONTIER CORE (2026-06-23) — ALL 4 deep orchestration capabilities Chase greenlit before cockpit/visual are BUILT + PROVEN: FC-0 policy substrate, FC-1 twin-in-the-loop MPC, FC-2 self-improving loop (CIL), FC-3 uncertainty/risk-calibration, FC-4 agentic NL command. PLUS the big one: rebuilt OTTO-Q's energy control loop (battery was on autopilot ignoring OTTO-Q) → OTTO-Q now genuinely controls the depot's billed peak (proven 72% dial), 0-unsafe."
metadata:
  node_type: memory
  type: project
  originSessionId: b5cc1e94-2201-451a-a46f-c770df70e99a
---

**Chase (2026-06-23): build ALL genuinely-frontier orchestration capabilities NOW, before cockpit/visual/safety, so we don't have to rebuild deep later.** "Exhaust every potential addition and tool... find any threads to pull." Greenlit all 4 + scan for missing threads. Building on otto-q-core (`gxdrcyphqjzjsuhxuqtg`).

**The 4 frontier capabilities (dependency-ordered):**
- **FC-0 Policy-parameter substrate — ✅ DONE.** `ottoq_policy_params` (scope run>depot>global) + `ottoq_policy_get(run,key,default)` + `ottoq_policy_param_catalog`. The keystone: tunable levers MPC varies + CIL self-tunes. Wired: energy demand factor (`oq_fc0b`), deploy_peak_fraction into decide_tick + auto_dispatch (`oq_fc1e`). Defaults reproduce today's behavior exactly.
- **FC-1 Twin-in-the-loop MPC — ✅ HARNESS BUILT + PROVEN.** `ottoq_mpc_lookahead(run, plans jsonb, horizon, objective)` (general, multi-lever, multi-objective: demand-charge|throughput|AM-readiness|0-unsafe-hard-gate) + `ottoq_mpc_energy_lookahead`. **Mechanism = fork the twin in a PL/pgSQL exception-block SAVEPOINT, apply candidate policy, advance N ticks dry-run, measure, RAISE to roll back; vars survive rollback.** VERIFIED byte-identical rollback (fleet fingerprint + tick_count + dryrun flag + 0 leftover params unchanged after forking 4 branches × ticks). Emits `ottoq.mpc_plan` events. The moat: no twin-less orchestrator can do closed-loop lookahead.
  - **Dry-run speed/FK fix (`oq_fc1c`):** forks set GUC `ottoq.dryrun='on'`; `ottoq_evaluate_rule_core` then EVALUATES the rule (verdict still computed → safety projection intact) but RETURNS before persisting (skips the per-vehicle insert+event+linked_event_id UPDATE that dominated cost + broke FKs). `ottoq_cuopt_refresh` also skips under dryrun (no real NVIDIA calls in forks).
- **FC-2 Self-improving CIL — ✅ DONE + PROVEN (mechanism).** `ottoq_cil_propose` (hill-climb candidates around energy/deploy levers) + `ottoq_cil_tick` (A/B every candidate forward in the twin via the MPC, 0-unsafe HARD gate → adopt the strictly-better winner to depot-scope policy, else keep current) + `ottoq_cil_adoptions` audit table + `ottoq.cil_decision` event. PROVEN: compounds 0.50→0.40→0.30 over two passes (predicted peak 1265→1015→765 kW), each 0-unsafe, rejected the worse 'relax', 3 decisions logged. FOLLOW-UPS: widen readiness/throughput signal + longer horizon so it converges to the best energy↔readiness BALANCE (short horizon → energy dominates → leans max-shave); optional Nemotron-powered propose. Now energy is a real dial (see resolved finding below), the CIL has something real to tune.
- **FC-3 Uncertainty/risk-calibrated — ✅ DONE + PROVEN.** `ottoq_forecast_uncertainty` (0..1 from coefficient of variation of recent EV load) wired into `ottoq_energy_orchestrate`: battery reserve floor = soc_min_floor + uncertainty×20 → holds more reserve when the future is murky, runs lean when steady. PROVEN: erratic ramp → uncertainty 1.00 → reserve floor 10%→30%; steady → 0.00 → 10%; 0-unsafe. EXTENSIONS: deploy-buffer widening, confidence bands on predict_arrivals, feed into MPC scoring.
- **FC-4 Agentic NL command — ✅ DONE + PROVEN end-to-end.** Built on existing `ottoq-ottocommand` (Claude tool-loop, role-scoped, shield-gated) → v4 adds 5 frontier tools: get_status_brief, set_energy_aggressiveness (clamped to catalog safe-range), evaluate_energy_options (MPC on active run), self_improve (CIL tick), get_improvement_log. Safe primitives: `ottoq_active_sim_run`, `ottoq_policy_set` (catalog bound-check — NL can't push a lever out of range; proven 0.05→clamped 0.25), `ottoq_nl_status_brief`. PROVEN: plain-English "how are you / how confident?" → agent called the real tool → replied in plain language with live data + a risk-aware recommendation. Uses ANTHROPIC_API_KEY (set). **ALL 4 FRONTIER CAPABILITIES NOW BUILT + PROVEN + the energy-control-loop fix (the big one).**

**✅ KEY FINDING — RESOLVED (task #60, 2026-06-23). Building FC-1 surfaced that OTTO-Q was DISCONNECTED from the battery — and we fixed the whole energy control loop so OTTO-Q now genuinely controls the depot's billed grid peak.** Root causes found: (1) the battery ran on an AUTONOMOUS default (`ottoq_sim_advance_bess` → `bess_default_target_kw`) and ignored OTTO-Q's `bess_setpoint` command entirely; (2) the one executor that could connect had charge/discharge SIGN REVERSED (OTTO-Q +=discharge vs physics +=charge); (3) `advance_charge_sessions` never throttled active charging to the cap (only `decide_tick` gated new starts); (4) OTTO-Q's strategy DRAINED the battery early (an 'arbitrage' branch) so it was empty at the peak; (5) `energy_orchestrate` read a run-agnostic stale snapshot (wrong solar). **Fixes (all live, OTTO-Q-gated so baselines untouched):** `oq_fc_energy1` active-session smart-charging throttle obeys the cap · `oq_fc_energy2` battery OBEYS OTTO-Q's command (sign corrected, autonomous fallback for baseline) · `oq_fc_energy3` coherent reserve-aware strategy (shave just enough → hold reserve → recharge only within headroom from cheap/solar) + run-specific snapshot read. **PROVEN: same depot/same moment, energy factor 0.12 → billed grid peak 363 kW vs 0.50 → 1313 kW = a 72% swing that is purely OTTO-Q's decision; cars NOT delayed (battery does the shaving), 0-unsafe, 70 deployed, battery reserve held at 91%.** The 42% headline was previously the battery's autopilot roughly helping; it is now genuinely OTTO-Q-steered + finely tunable. **RE-MEASURED HEADLINE (2026-06-23, clean on/off A/B via `energy_orchestration_enabled` toggle, identical forked state at the evening peak, flagship service_max=2500 kW):** unmanaged baseline peak **1465 kW** → OTTO-Q default (0.50) **1252 kW (−15%)** → OTTO-Q aggressive (0.20) **975 kW (−33%)**, all battery-driven (357→732 kW discharge), 0 car delays, 0-unsafe. At ~$10–15/kW-mo demand charge that's **~$2.1k–3.2k/mo (default) to ~$4.9k–7.4k/mo (aggressive)** on THIS depot; scales ~linearly with charger count / utilization (a 2× depot ≈ 2× savings → the old ~$10k range). HONEST NOTE: the prior "42%/$10k" was a larger depot config AND partly the battery's autopilot; this number is genuinely OTTO-Q-driven + tunable + the CIL auto-optimizes the factor. **DEEPENED 2026-06-25 (oq_fc_energy4–8): found + fixed 5 more twin-EXECUTION bugs that had been masking the levers — charge-session freeze (updated_at trigger vs sim-clock), BESS recharge not demand-aware (cap ignored the recharge draw), site-energy snapshot written before the charge throttle (phantom peaks), throttle desired-estimate inexact, and the big one — `start_charge_session` opened new sessions' meters at the FULL un-throttled rate that the loop then skipped (every arrival wave injected un-throttled load defeating the cap). After fixes the dial is monotonic + finely tunable + 0-unsafe (peak grid tracks the demand target smoothly), the EV smart-charging throttle bites even with a depleted battery (1504→495 kW), and high-load CRN A/B = 2509→983 kW (−61%). Details in [[project_energy_e4_charging_fix]].** See [[project_energy_e4_charging_fix]], [[reference_nvidia_ai_integration]], [[project_ottoq_logic_completeness]].

**Additional frontier threads surfaced (architect seams now, full build later):** V2G/depot-as-VPP (energy seam), SLA/priority-tier objective weights, predictive maintenance, adversarial self-test of the shield, causal/counterfactual explainability, carbon-aware + battery-health-aware charging, external feeds (weather/traffic), online twin calibration, anomaly self-monitoring.

**Gotcha:** Supabase `execute_sql` is client-timeout-prone on heavy multi-tick advances (~14 real ticks) — but the server may still COMMIT after the client gives up (saw a run jump 8→22 post-timeout). Forks are fast (dry-run optimized); real multi-tick advances should be batched ≤8.
