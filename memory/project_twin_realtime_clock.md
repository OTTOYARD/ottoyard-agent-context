---
name: project_twin_realtime_clock
description: "⭐Chase 2026-07-25: wants 1 real second = 1 sim second, with 2x/3x as a VIEWING multiplier ('bees buzzing'). Today runs at 60x. Investigated (wf_7f1d52f8), NOT built. Key: the renderer is ALREADY 1:1; 1:1 already exists in the production path; the work is promoting it + ~13 tick-size physics couplings."
metadata:
  node_type: memory
  type: project
  originSessionId: b5cc1e94-2201-451a-a46f-c770df70e99a
  modified: 2026-07-30T23:03:33.638Z
---

**⭐⭐⭐ 2026-07-30 — CHASE RE-ASSERTED THIS AS A PRIORITY AND IT IS NOW THE CRITICAL PATH.** Verbatim: *"I want the ticks to be one second each… one real world second to reflect one twin or simulation second… OTTO-Q, the twin, and actual reality for whoever is watching… all should align together as one and in sync."* Plan doc: `~/Desktop/OTTO-Q V1/OTTOQ_REALTIME_CLOCK_PLAN_2026-07-30.md`.
- **⚠️ TERMINOLOGY TO CORRECT WITH HIM (gently, every time):** "one tick = one second" ≠ "1 real sec = 1 sim sec". The **tick is the update granularity (frame rate); the clock ratio is the thing he wants.** A 1-sim-second TICK = 1,800× more ticks = dead on arrival (finding #1 below). `live` mode gives exact 1:1 with the tick as mere granularity — slow tick ⇒ bigger dt ⇒ **clock never drifts**. Two independent dials: **playback mode = ratio; tick cadence = smoothness.**
- **✅ RE-VERIFIED LIVE 2026-07-30:** `ottoq_set_playback` still present and works — returned *"1 real second = 1.0 sim second(s)"*; run `6bad50c9` **11 ticks advanced only 2.5 sim-MINUTES** (fixed mode would be 5.5 sim-HOURS). Writes `payload.playback_mode` + `payload.speed_x` — **NOT** a `playback` object (my first read used the wrong key and saw null; mine, not a bug).
- **❌ NOTHING USES IT.** Every run in `ottoq_sim_runs` has `tick_interval_seconds=30, time_scale=60` and **no playback key** ⇒ every run incl. today's soak ran at **60× real time**. The fix is to DEFAULT the demo path to `live`, not to build anything.
- **⚠️ MEASURED RATIO = 1.246 sim-sec per real-sec, not 1.000** (84 sim-sec over 67.4 real-sec, 7 ticks). A 48× improvement on 60× but **25% fast — a real residual, CAUSE NOT YET IDENTIFIED.** ❌ **My dt-floor hypothesis is REFUTED** — `ottoq_sim_advance_tick_world` line 24 reads `v_tick_minutes := LEAST(10.0, GREATEST(0.0, (…* speed_x)/60.0))` in the live branch: **floor 0.0, no 15-second minimum.** (The `GREATEST(0.25,…)` floor noted in the 2026-07-25 investigation is on a different path.) Observed separately: the **FIRST tick over-advances** — run started 22:57:52, tick 1 at 22:58:18 = 26 real-sec but 43 sim-sec — so the initial elapsed anchor looks wrong. Needs a dedicated measurement; **do not guess again.**
- **⭐ TICK COST — MEASURED 2026-07-30, RULE 0 protocol (8 warm samples): median ≈ 1,155 ms** (937/993/1135/1140/1168/1207/1211/1408), split world ≈ 2.3 s → decide ≈ 0.55 s on the cold first pass. Historical was 7-22 s, so today's event-write fixes plus the 8 dropped indexes appear to have paid off. **⚠️ MEASURED ON A NEAR-IDLE WORLD** — 220 vehicles but only **1 deployed / 9 charging / 25 in a stall**. The 7-22 s figures were under a loaded 116-vehicle fleet with active dispatch. **Do NOT claim the tick cost is fixed until re-measured under a primed fleet** (`ottoq_tick_invariance_reset_fleet` primes deterministically at 85-99% SoC).
- **⭐ COLD-TICK PENALTY: the first tick in a fresh session is 2.9-5.4 s vs ~1.15 s warm (2.5-4.7×).** pg_cron opens a new session per invocation, so **the metronome pays a cold tick every single beat.** Relevant to cadence design — a long-lived ticking session would avoid it.
- Cadence measured ~8.4 real-sec/tick, but that is **pacing, not compute** (`real_seconds_per_tick = 6/speed`). Compute is ~1.15 s ⇒ **cadence is already a config choice, not a hard limit, on an idle world.**
- **⭐⭐ THIS IS THE PREREQUISITE FOR THE RULE-1 INVERSION, NOT A DETOUR.** The inversion was rejected today because the actuation window is 30 sim-min. `live` mode collapses it to real seconds, which shrinks the reservation-expiry, re-decide, stale-validation and lost-charging-tick problems all at once. **Order: clock → inversion prep → inversion.** Chase spotted the connection himself.
- **❓ OPEN DECISION (blocks nothing in Phase A):** at true 1:1 one sim-day = one REAL day. Certs are 3 seeds × 3 policies. **Recommend `live` 1:1 for the demo/viewing path and keep `fixed` as the default for certs/benchmarks** — `fixed` exists precisely to keep CRN A/B byte-identical. (This is memory's long-standing D2, still unanswered.)
- **Phase B is the real work: tick COST.** ~8-11 s per tick today; you cannot tick every 2 s if a tick takes 11 s. Target ≤2 s.

**⭐⭐ BACKEND SHIPPED + VERIFIED 2026-07-25** (migrations `playback_clock_live_mode_and_speed`, `jump_forward_with_planning_pause_v2`). Chase's refinement: *"Maybe not speed up, but rather FAST FORWARD... we can not keep up with more than 3X for OTTO-Q decision making anyway... we can just JUMP forward and maybe just have a 2-3X max. And when we jump forward, there can be a loading moment where it PAUSES AND PROCESSES what's going on with the queueing sequence from OTTO-Q."*

**TWO MECHANISMS, ONE ENGINE:**
1. **`ottoq_set_playback(run, mode, speed_x)`** — `'live'` = TRUE 1:1 (sim advances REAL elapsed × speed_x); `'fixed'` (DEFAULT, unchanged) keeps `tick_interval_seconds × time_scale` so **every cert / benchmark / CRN A/B stays byte-identical**. speed_x **hard-clamped [1,3]** — the founder-approved ceiling for continuous play; anything faster must be a JUMP so decisions are never outrun by the clock. **VERIFIED: 10× → clamped 3.0; 0.2× → clamped 1.0; bad mode rejected.**
2. **`ottoq_sim_jump_forward(run, sim_minutes, max_seconds)`** — the fast-forward. Forces `fixed` coarse ticks for the skip (nobody watches motion during a jump ⇒ coarse is correct AND cheap), **BOUNDED + RESUMABLE** (each call spends its wall-clock budget then returns progress; client loops until `done=true`, so it can never blow the statement timeout). Publishes `payload->'jump'` = `{status:'planning', target_sim_clock, from_sim_clock, prev_mode}` for the renderer's loading moment + progress bar, and **returns `ottoq_worked {decisions_made, visits_booked}`** — the queueing sequence processed, not a black box. Auto-restores the prior playback mode on completion.

**LIVE-VERIFIED 2026-07-25:** 1:1 proven — **11 ticks advanced only 99 sim-seconds** (in fixed mode the same 11 ticks advance 19,800 sim-sec = 5.5 h). Jump proven — **90 sim-minutes skipped in 0.69 real seconds / 3 ticks**, landed exactly on target, `jump` key cleared, mode restored to `live`.
⚠️ **Mixing coarse (jump) and fine (live) ticks is only safe because tick-size physics couplings are being made invariant** — soil decay done + proven; **the remaining ~12 couplings are NOT yet fixed** (see list below) and the **tick-size invariance harness is still unbuilt**. Do not certify numbers across mixed tick sizes until it is.
⚠️ The cron demo metronome (jobid 12) ticks any running run and will interleave with manual ticks — it delegates to `advance_tick_world` so it honours live mode, but its ~1/min cadence is too coarse for smooth 1:1 playback; cadence should be ~5-15 s.

**⭐ TICK-SIZE INVARIANCE HARNESS — BUILT + FIRST VALID CERT 2026-07-25** (`ottoq_tick_invariance_arm/report/metrics/reset_fleet`, table `ottoq_tick_invariance_arms`). Runs N arms at different tick sizes under common random numbers, diffs outcome metrics, flags anything whose spread exceeds tolerance as a tick coupling.
⚠️ **HARNESS BUG THAT INVALIDATED THE FIRST 3 REPORTS (lesson):** arms inherited the PREVIOUS arm's fleet state. After a few runs depot-1 sat at avg 36% SoC / max 79%, and `ottoq_sim_prime_deployment` requires **≥80% SoC** — so it primed ZERO vehicles and every arm measured an idle yard (visits 0-1, km on ~2 vehicles). The arms were also comparing DIFFERENT starting worlds, so the early "TICK-COUPLED" verdicts (incl. a scary "drive_km 54% coupled") were **artifacts, not bugs**. Fixed with `ottoq_tick_invariance_reset_fleet(depot, seed)` — deterministic seed-derived SoC 85-99% + offline + stalls cleared, so tick length is the only variable. **Always confirm the arms actually LOADED (116 dispatches / 116 wear rows) before believing a verdict.**

**FIRST VALID CERT (seed 812001, 240 sim-min, 116-vehicle primed fleet, ticks 30/10/5 min):**
| metric | 30min | 10min | 5min | spread | verdict |
|---|---|---|---|---|---|
| **drive_km_total** | 15238.7 | 15456.4 | 15352.9 | **1.4%** | **INVARIANT** |
| sim_minutes_elapsed | 240 | 240 | 240 | 0.0% | INVARIANT |
| unsafe_blocks | 0 | 0 | 0 | — | INVARIANT |
| soil_index_mean | 0.1461 | 0.1413 | 0.1371 | 6.4% | near-invariant (decay fix works; small residual) |
| visits_created | 23 | 10 | 17 | 78% | **COUPLED (non-monotonic)** |
| wash_atoms | 11 | 5 | 7 | 78% | **COUPLED (non-monotonic)** |
| decisions | 21 | 38 | 74 | 120% | by design (per-tick cadence) |

**COUPLING #3 FIXED (`return_need_thresholds_ticks_to_minutes`):** `ottoq_evaluate_return_need` held its thresholds in TICKS and multiplied by `p_horizon_min` — which the telemetry caller passes as `p_tick_minutes`. So the **24-hour timer backstop silently became 4 hours** at 5-min ticks (48×30=1440 → 48×5=240), and the contention wait cap 120 min → 20 min. Now expressed in MINUTES (`timer_backstop_min` 1440 / `contention_wait_cap_min` 120, old tick keys honoured ×30 as fallback) — byte-identical at a 30-min tick. **RE-CERT (seed 813500): soil_index_mean 6.4% → 4.5% = crossed into INVARIANT; drive_km 4.4% INVARIANT; 0 unsafe.**
⚠️ **STILL COUPLED after the fix: `visits_created` 13/6/10 (72%) and `wash_atoms` 8/2/6.** The thresholds were not the whole story — the remaining cause is the **per-tick WORK CAPS** (bare `LIMIT n` per tick in `ottoq_sim_auto_dispatch_tick` gate admission and `ottoq_sim_advance_service_flow:153`, plus `ottoq_admit_stranded_vehicles(...,80,12)`), which admit N *per tick* regardless of tick length. Converting those to per-minute rates (or true concurrency limits) is the next fix. Counts are small (2-13) so the percentages are noisy, but the direction is consistent.
⚠️ New policy keys `timer_backstop_min` / `contention_wait_cap_min` are NOT yet registered in `ottoq_policy_param_catalog` — they work via the `ottoq_policy_get` default path but should be registered for tunability.

**COUPLING #4 FIXED (`deploy_cap_per_minute_not_per_tick`):** `ottoq_sim_advance_service_flow` line ~162 `LIMIT v_deploy_cap` (20) was a bare PER-TICK rate — unlike the wash/service caps just above it, which are true CONCURRENCY limits (they subtract `v_in_wash`/`v_in_svc`) and were already tick-safe. As a per-tick rate the depot released 40 vehicles/sim-hour at 30-min ticks but **1,200/sim-hour at 1-min ticks — a 30× free throughput multiplier from shortening the tick.** Now `LIMIT GREATEST(1, CEIL(v_deploy_cap * p_tick_minutes/30.0))`. **MEASURED: 40/120/240/1200 → 40/42/48/60 per sim-hour at 30/10/5/1-min ticks; byte-identical at 30.** Residual 40→60 at 1-min is integer rounding (cannot release a fraction of a vehicle; `GREATEST(1,…)` guarantees no full stall) — exact would need a fractional carry-over accumulator.
**HOW TO TELL A SAFE CAP FROM AN UNSAFE ONE:** `GREATEST(0, cap - count_currently_in_state)` = CONCURRENCY = tick-safe. A bare `LIMIT n` = per-tick RATE = tick-coupled, must be scaled by `p_tick_minutes/30`.
⚠️ **NOT yet re-certified after this fix** — the harness must be re-run (seed ≠ 812001/813500) to confirm `visits_created`/`wash_atoms` come into tolerance. Also unfixed: the `LIMIT v_to_dispatch` / `LIMIT GREATEST(0, v_cap)` caps in `ottoq_sim_auto_dispatch_tick` (needs checking whether that function even receives `p_tick_minutes`), and `ottoq_admit_stranded_vehicles(...,80,12)`.

**MY OWN REGRESSION — CAUGHT BY THE HARNESS, then fixed (`fix_wait_estimate_units_tick_invariant`):** the coupling-#3 migration redefined `contention_wait_cap` from 4 TICKS to 120 MINUTES, but line ~76 still used it to clamp a TICK COUNT (`v_wait_ticks := LEAST(v_wait_cap, <queue rounds>)`), and the comparison then multiplied by `p_horizon_min`. Net: at a 5-min tick the estimated queue wait went from ≤20 min to ≤120 min, so vehicles chose to KEEP WAITING instead of returning → `visits_created` fell monotonically as ticks got finer (21→14→8, seed 901400). **Correct semantics: the CASE yields unitless QUEUE ROUNDS; a round is a SERVICE SESSION (~30 min), not a tick** — multiplying rounds by tick length was the original coupling. Now `LEAST(rounds * 30.0, wait_cap_min) <= max_wait_min`. **RESULT (seed 902700): wash_atoms 80.8% → 13.0% (7/8/8); drive_km 1.7% INVARIANT; 0 unsafe.** Lesson: when converting a variable's UNITS, grep every use of it — not just the site you set out to change.

**COUPLING #5 FIXED (`overnight_recall_cap_per_minute_v2`):** `overnight_recall_max_per_tick` (24) in `ottoq_sim_auto_dispatch_tick` — a per-TICK rate governing **the overnight wave specifically**. 48 recalls/sim-hour at 30-min ticks vs **1,440/sim-hour at 1-min**. It hides most of the time because `LEAST()` is usually bound by the setpoint gap `v_to_recall` or `v_free_intake` — but during a real compressed wave the gap is large and THIS becomes binding, i.e. exactly when it would distort the number we care about. That function has no `p_tick_minutes`, so tick length is derived from `v_run` the same way the decider does (`payload->>'tick_minutes_actual'` first, config second). **MEASURED: 48/144/288/1440 → 48/48/48/60 per sim-hour at 30/10/5/1-min.**

**⭐ KEY MEASUREMENT — the return CYCLE is already invariant.** Seed 902700: **dispatches 116 / 117 / 116, all completed, all with return triggers**, across 30/10/5-min ticks. So `visits_created` (18/10/8) is counting only the small TAIL of vehicles that reached booking before the window closed — a window edge-effect on ~10 rows, **not** a 116-vehicle throughput bug. Do not chase that metric as if it were one; normalise it (visits per completed dispatch) or use a longer window.

**🚨 MEASUREMENT-INTEGRITY BUG — EVERY CERT BEFORE 910100 WAS CONTAMINATED (`fence_invariance_arms_from_metronome`).** `ottoq_demo_metronome` (cron jobid 12, every minute) ticks ANY running run except `run_by IN ('production_live','cert_harness')`. The arms used `run_by='tick_invariance'` — OUTSIDE the fence — so the cron advanced them in parallel with the controlled loop. **Evidence: the 30-min arm of seed 903900 ran 12 ticks / 360 sim-min against a 300 sim-min target — 20% more simulated time than its sibling arms.** Count metrics were therefore not comparable. Fixed by running arms as `run_by='cert_harness'` (already fenced); no metronome change needed. **PROOF THE FENCE WORKS: seed 910100 shows `sim_minutes_elapsed` exactly 240/240/240 (0.0%), vs the overshoot before.** ⚠️ ANY cert harness added in future must use a fenced `run_by`, or the cron silently co-drives it.

**🚨🚨 SEED 910100 IS *NOT* A VALID INVARIANCE RESULT — RETRACTED (audit session local_f1c3569b/b3b38afc, 2026-07-26).** I called it "the first uncontaminated cert". It is not. **`ottoq_sim_decide_and_dispatch` calls the Nemotron orchestrator every tick, and that agent WRITES RUN-SCOPED POLICY PARAMS mid-run.** `ottoq_policy_get` resolves run scope before depot, so they bind. Run-scoped params by arm: **30-min = 0, 10-min = 2, 5-min = 5 — the intervention count SCALES WITH TICK COUNT, the independent variable.** A nondeterministic writer inside a determinism cert. (3 writes landed in-run; 4 landed ~19s after the 5-min arm ended, so they polluted stored scope for replay rather than that arm's behaviour — either way the cert cannot stand.) **Seed 903900 is separately contaminated** (`run_by='tick_invariance'` is NOT in the metronome exclusion list; arm 36ccdbcc got 12 ticks / 360 declared sim-min vs 11 / 300). **Do not quote 910100 or 903900.** A valid cert additionally requires: no run-scoped policy writes while `run_by='cert_harness'`, and `'tick_invariance'` added to the metronome exclusion list.
**ALSO RETRACTED — my "unfenced vs fenced" lock-contention test was mis-evidenced:** run `29b72b2b` was ALREADY `run_by='cert_harness'` (I renamed it in that same migration) and it started 24 min AFTER the cert window. The conclusion (contention is not the cause) happens to be right — the real cause is `ottoq_record_event`, below — but my evidence for it was wrong.

**⭐ FIRST UNCONTAMINATED CERT (seed 910100, 240 sim-min, 116-vehicle primed fleet, 30/10/5-min) — ⚠️ SEE RETRACTION ABOVE; NUMBERS RETAINED FOR PROVENANCE ONLY:**
| metric | 30min | 10min | 5min | spread | verdict |
|---|---|---|---|---|---|
| sim_minutes_elapsed | 240 | 240 | 240 | **0.0%** | INVARIANT |
| unsafe_blocks | 0 | 0 | 0 | — | INVARIANT |
| drive_km_total | 15780.9 | 15703.0 | 15377.1 | **2.6%** | INVARIANT |
| soil_index_mean | 0.1555 | 0.1456 | 0.1393 | 11.0% | over tolerance (was 4.5% on seed 902700 — seed-sensitive, absolute delta is tiny) |
| visits_created | 21 | 14 | 16 | 41.2% | **improved 78%→83%→41% and NO LONGER MONOTONIC** |
| wash_atoms | 12 | 7 | 8 | 55.6% | small counts (7-12) |
| decisions | 18 | 40 | 82 | 137% | by design (per-tick cadence) |
**Read:** the systematic throttles are gone (non-monotonic now = no remaining rate bias); residual spread is scheduling-boundary effects on 7-21 row counts. To resolve properly, NORMALISE the metric (visits per completed dispatch) and/or run a longer window — do not chase raw counts.

**`ottoq_admit_stranded_vehicles` INSPECTED, NOT CHANGED — already tick-safe:** `v_slots := GREATEST(0, LEAST(p_limit, v_free - v_waiting))` is dominated by free capacity (a CONCURRENCY measure); `p_limit`(12) is only a burst ceiling on a deadlock-breaker. Deliberately left alone.

**⚠️ CORRECTION — NORMALISING DID *NOT* EXPLAIN THE RESIDUAL (my hypothesis was wrong).** I added ratio metrics (`visits_per_dispatch`, `washes_per_visit`, `kwh_per_100km`) expecting the window-edge effect to divide out. It did not: `visits_per_dispatch` = **41.5%** spread vs raw `visits_created` 41.2%, while the denominator `completed_dispatches` is **INVARIANT (116/116/119, 2.6%)**. So the same ~116 completed dispatches genuinely produce 21 visits at 30-min ticks but 14-16 at finer ticks — **a real residual coupling in visit CREATION, not measurement noise.** `washes_per_visit` is tight (0.571/0.500/0.500, 13.6%) so triage-per-visit is near-invariant; the variance is upstream. Do not repeat the "it's just edge effects" claim — it was tested and refuted.

**🔎 FUNNEL GAP FOUND (spawned as its own task, `task_5732b5dc`): 116 dispatches complete AND all carry a return_trigger, yet only 14-21 DISTINCT vehicles ever get an `ottoq_visit_needs` row.** Under the appointment doctrine every return should book. Either dispatch `status='completed'` does not imply a depot arrival, or the book→manifest path drops ~95 vehicles. Also found: **arrival events scale with TICK COUNT not arrivals** (33/77/136 over 8/24/48 ticks ≈ 3-4 per tick) — a per-tick emission that inflates the event log at fine ticks. Both need investigating separately from tick-invariance; do NOT conflate them.

**VERDICT: the PHYSICS is tick-invariant; the SCHEDULING is not.** A jump produces a physically correct world (same distance/energy/wear, 0 unsafe), but the exact SEQUENCE of service decisions differs from live playback. Acceptable for a demo; **NOT acceptable for a certified number** until the per-tick work caps + tick-count thresholds (`ottoq_evaluate_return_need` comms_stale_ticks/contention_wait_cap_ticks/timer_backstop_ticks, gate-admission caps in `ottoq_sim_auto_dispatch_tick`/`ottoq_sim_advance_service_flow`) are converted to per-minute.
⚠️ Under a loaded 116-vehicle fleet, ticks are slow enough that a 5-min-tick arm needs `p_max_seconds≈3` and ~9 resumable calls per 240 sim-min.

**FOUNDER ASK (Chase 2026-07-25, verbatim intent):** *"I'm at one second in the simulation to represent exactly 1 second in real world viewing time. If I need to speed up the simulator, I can do that by going 2X 3X and the vehicles will just move faster... like a sped up fast motion version of the depot like bees buzzing around a beehive, which I honestly love."*

**MEASURED TODAY = 60× REAL TIME.** `ottoq_sim_runs`: `tick_interval_seconds=30`, `time_scale=60`, each tick advances **exactly 1800 sim-seconds (30 sim-min)** — verified identical across 6 runs. So 30 sim-minutes per 30 real-seconds → 1 real second = 1 sim minute.

**THE THREE FINDINGS THAT RESHAPE IT:**
1. **Shrinking the tick is NOT the answer.** 1 sim-sec/tick = 1800× more ticks. The 1,113-tick marathon already made a 528MB decisions table ([[project_demo_loop_ops]]). Dead on arrival. **speed_x must multiply dt, NEVER tick frequency** — fine ticks WITH time compression is the catastrophic combination (~14 GB/real-hour at 1-min ticks × 300×).
2. **The renderer is ALREADY 1:1 and needs no correction.** `RailFlow.ts:52 MAX_SPEED=8` logical units/s = 3.83 m/s = **8.6 mph** (a correct depot taxi speed), driven off `performance.now()` real milliseconds. Motion **never reads sim time** (the one `sim_clock` read at `TwinMotionDriver.ts:467-471` is just a day/night stall-pool selector). The viewing multiplier is an ADDITION at one line (mirror `paused` from twinStore, apply at the dt chokepoint `:836`), not a fix. **Must be N fixed sub-steps of `tickMotion(dt/N)`** — `RailFlow.ts:49 SAMPLE=2` means a big dt can step a car THROUGH a body without the leader scan seeing it.
3. **1:1 ALREADY EXISTS in the production path.** `ottoq_world_advance` sets `sim_clock_current = now()` and derives dt from wall clock — self-healing (slow tick → bigger dt, clock never drifts, only granularity coarsens). The demo engine just doesn't use it. **The job is promoting that pattern, not inventing a clock.**

**LIVE BUGS FOUND (fix before any clock work):**
- **15× tick_minutes disagreement on production_live:** `ottoq_world_advance` derives ~2 min from wall clock, then `ottoq_sim_decide_and_dispatch` RECOMPUTES `(tick_interval_seconds * time_scale)/60` = 30 min. Two halves of one tick disagree ~15×. **`ottoq_score_run:43` is wrong by the same factor → any production throughput number from it is inflated ~15×.**
- **15-second dt floor** `GREATEST(0.25, LEAST(10, ...))` inflates drain/wear/BESS/mileage up to 15× at fast cadence.
- `ottoq_sim_advance_clock` is dead AND treats time_scale as minutes (2× bug waiting to be picked up) — delete.
- **Three competing rate controls** (cron jobid 10 production 120s; jobid 12 `ottoq_demo_metronome` which OVERWRITES `next_tick_due_at`; `ottoq_sim_advance_due_runs` on NO cron). The UI slider (`OperatorConsole.tsx:438` → `useTwinControl.ts:23-32` → `PUT /sim_runs/{id}/time_scale`, clamped [15,480] client-side) is **backwards for the goal** — it increases sim-time-per-tick, so 10× = same crawl with bigger jumps, and its floor of 15 pins the minimum at 75× real time. Cannot reach 1:1.

**~13 TICK-SIZE PHYSICS COUPLINGS (the bulk of the work).** Most dangerous: **`ottoq_sim_advance_wear_counters:32` `soil_decay_per_tick` 0.010 is applied per TICK while soil accumulates per-KM** — hand-calibrated at 30-min ticks. Shrink the tick and decay runs 30× fast, soil never accumulates, **wash demand collapses and silently disables the 1/3 rotation just built**. Others: per-tick Bernoulli in `ottoq_sim_bay_fault_handler`/`ottoq_sim_vehicle_exception_handler` (don't even receive `p_tick_minutes`); `ottoq_evaluate_return_need` thresholds in TICKS (24h backstop → 48 min); per-tick work caps that SHAPE THE OVERNIGHT WAVE (gate admission 24/sim-hr → 720); `peak_demand_kw_15min` contains exactly ONE sample at 30-min ticks (an instantaneous reading mislabelled as a 15-min average, feeding `billing_period_peak_kw` — fix before re-quoting any energy dollar figure); **variance collapse** — `ottoq_sim_advance_deployed_telemetry` draws per TICK so trip-level spread shrinks ~√30, **invalidating every CRN A/B baseline** (fix: salt on `dispatch_id`).
**→ Build a TICK-SIZE-INVARIANCE HARNESS first** (same seed, tick sizes 30min/5min/1min/10s; any metric moving more than noise = an unfixed coupling). Highest-value artifact in the workstream.

**NOT AVAILABLE as a fallback: itinerary-driven pacing.** Zero motion legs exist (taxi/arrive/depart/settle = 0 of 24,479), `from_stall_id` 100% NULL (`ottoq_itin_leg_open` has no from-stall param), `to_stall_id` only 22% populated, `duration_basis` has no route/geometry. **The itinerary is a list of dwell intervals, not a route — the global clock is the only pacing achievable from today's feed.** What IS cheap: dwell-phase progress ("wash: 3.5 min remaining") from sub-tick timestamps.

**SEPARATE PROBLEM hiding inside "tick timing":** OTTO-Q **decides every 30 sim-minutes** while events happen on 3-22 min scales (3-min inspection, 5-min tidy, 22-min charge). Decisions land after the fact — likely feeding the known 4-8× plan-vs-actual drift (task #99). **Rendering smoothness ≠ decision responsiveness:** the first needs NO backend change; the second needs a moderately finer tick (~5 min, not 1 sec).

**FOUNDER DECISIONS PENDING:** (D1) top of the motion-faithful band — 1×=8.6mph, 3×=26mph, 6×=51mph, 60×=absurd; recommend hard cap 4-8× with anything above labelled "fast-forward, motion not to scale". A 116-vehicle wave = 2 real hours at 1×, ~20 min at 6×. (D2) does a demo run 1:1 end-to-end? At 1:1 one sim-day = one REAL day (~1.1 GB + a full day of DB CPU) — supersedes the bounded-~1-sim-day doctrine; either demos become windows or there's a fast-forward-between-scenes mode. (D3) does the 3-5 min at-charger tidy replace or coexist with the 20-25 min `interior_deep_clean` bay atom? (D4) split the wash building into fast rinse LANE + deep-clean BAY? (`detail_bay` has 0 stalls yet vehicles enter `in_detail_bay`). (D6) tech numbers + is the DCFC:L2 5-8× ratio per stall or per vehicle-hour (note standing memory: staffing is a FIXED slider, never a shortage model — a zone pool that can starve changes that).

**D7 ANSWERED (was a blocker):** the legacy client `SimulationEngine` is NOT dead (imported in `App.tsx:11`, `useEngineLifecycle.ts`, `useKeyboardShortcuts.ts`) but the two engines ARE properly interlocked — `useTwinSceneBridge.ts:43` clears the twin driver whenever `legacyStatus==='running'`. **The "Demo" button (`App.tsx:19 enterDemo`) runs the FAKE offline engine with 12 hand-seeded vehicles and locally regenerated stalls — not OTTO-Q at all.** Anything judged from that button is a mock. Real behaviour only shows when Demo is NOT active.

Links: [[project_full_service_visit_doctrine]], [[project_depot_motion_timing_realism]], [[project_demo_loop_ops]], [[project_itinerary_contract_a2]].
