---
name: feedback_realism_standalone
description: "STANDING BAR — OTTO-Q must be a genuinely adaptive, self-aware, standalone orchestrator fielding realistic unscripted demand, never pre-programmed"
metadata: 
  node_type: memory
  type: feedback
  originSessionId: b5cc1e94-2201-451a-a46f-c770df70e99a
---

Chase (2026-06-22): everything must be built to **field real-world scenarios with highly realistic simulation data** — NOT pre-programmed/scripted backend items. OTTO-Q must be **standalone and field whatever scenarios emerge**, reading and reacting to:
- **Energy demand** signals (real-time + historical) — peak/TOU/grid.
- **Per-vehicle service MANIFESTS** — each arriving vehicle signals the bundle of services it needs, and bundles VARY: e.g. Vehicle A = charge-to-X% + daily internal clean; Vehicle B = charge + internal + external clean + a small sensor calibration that may be done now, done separately, OR **parked to the side for a later service-stall deployment after normal servicing**. So vehicles arrive with a coded service sequence + deferrable/optional items.
- **Charging / servicing / washing QUEUES** — manage all simultaneously.

**Why:** the demo + product only hold up if OTTO-Q is **self-aware and constantly adjusting** — proposing cues, stalls, and assignments AND managing energy at the same time, reacting to randomness rather than following a script. This is the same separation as always: **OTTO-TWIN generates the realistic varied demand/randomness (service manifests, arrival signals, energy variability); OTTO-Q reacts + optimizes + sequences + defers + assigns.**

**Mid-process exception handling + technician-in-the-loop (Chase, 2026-06-22):** if a flag arises MID-charge or MID-service — charger malfunction, wash/service-bay malfunction, a vehicle major issue — OTTO-Q must detect it and **propose re-routing** the vehicle: to a parking/staging stall, a different charger / wash / service stall, or **offline for inspection** — either **immediately with override** OR **pending technician approval** (a tech selects the vehicle to a new stall). Use cases: charger faults, bay faults, shutdown/temporary-staging before a tech reassigns. This is the "self-aware, constantly adjusting" loop: detect → propose cue → reassign. **Deferral / park-to-the-side (e.g., sensor calibration done later) folds into this same re-route model.** Chase: "whatever the research finds is best" — so calibrate service types/cadences/timings + the exception workflow from the deep research (launched 2026-06-22; sources incl. LinkedIn depot-ops posts, AV fleet-technician job postings, transcripts).

**Built toward this (2026-06-22, verified):** S1 `ottoq_sim_generate_service_manifest` (varied per-vehicle manifests, U8) · S2 manifest-driven adaptive disposition (U8b — parks deferrable + fast-turns must-do; 73 manifests/31 deferred, 0-unsafe) · S3a charger-fault AUTO-reroute (U9/U9b) · **S3b vehicle-fault→technician-approval staging (U9c/d/e — critical auto-stage / major propose→approve; all 3 branches verified, run-isolation fixed)** · **CRITICAL energy fixes (oq_e4a–e4d): EV charging load was invisible to the energy balance (phantom `in_progress` status) — now real $ compute; AND the charge-STARVATION linchpin (demand-cap over-counting EV load by connector nameplate) FIXED → overnight wave now clears 44→0 stuck/57 charged, 0-unsafe held.** **S2b overnight deferred-service drain (oq_s2b1–s2b5, verified):** dedicated overnight tech-crew capacity (3 concurrent, separate from L1 quick-turn bays) batches deferred deep-clean/wash/calibration/PM into the overnight low-demand window — 28 jobs/night drained, backlog 60→35, 0-unsafe. **E4 energy-$ headline DELIVERED:** OTTO-Q shaves the demand peak ~42% = ~$10k/mo/depot, 0-unsafe ([[project_energy_e4_charging_fix]]). OTTO-Q manages varied service demand + energy SIMULTANEOUSLY, adaptively, safely; absorbs the overnight wave (day/night deploy, [[project_depot_ops_model]]); and batches heavy service overnight. Remaining: S2b-arrival (generate manifest at ARRIVAL not charge-complete, for full upfront sequencing); deeper decide_tick §4/§5 manifest routing. Full state in ~/Desktop/OTTO-Q V1/OTTOQ_VISUAL_LAYER_PLAN.md §7.

**How to apply:** for every build, check both sides — (1) is the TWIN generating *varied, calibrated, real-world-shaped* demand (not uniform/scripted)? and (2) is OTTO-Q *adaptively* deciding (sequence, defer-to-later, stall-assign, energy-manage) rather than running a fixed pipeline? Existing machinery to verify/strengthen: per-vehicle coded service sequences ([[project_ottoq]] OQ-8 multi-service sequencer, `ottoq-jobs-request` coded_sequence), `ottoq_l2_propose_service` / `ottoq_sim_advance_service_flow`, wash cadence, and the [[project_energy_orchestration_seam]]. Always do research/analysis as needed; ask Chase if genuinely blocked.
